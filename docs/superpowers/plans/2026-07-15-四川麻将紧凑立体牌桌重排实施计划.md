# 四川麻将紧凑立体牌桌重排实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 把现有牌桌重排为“麻将牌优先、有效区域更大、四家牌零重叠、顶部信息不挡牌、四方向统一立体效果”的紧凑四川麻将界面。

**架构：** 保留现有规则、AI、结算和交互入口，只重做 `MainSceneV2` 的参考坐标、`PlayerUI` 的对手牌轨、`TableDiscardLayer` 的弃牌轨及共享麻将牌视觉。所有位置先由纯布局合同验证，再由固定牌局截图验证，禁止依靠负间距或遮盖塞牌。

**技术栈：** Godot 4.6.2 Mono、GDScript、Godot Control/CanvasItem、现有 C# AI Core（本轮只读）、Pillow 截图像素检查。

## 全局约束

- 唯一可写工程：`/Volumes/AI/Codex/四川麻将工程_20260701_v2`。
- `/Users/chendong/Documents/内江麻将工程_20260502_103823_v2` 与 `/Users/chendong/Documents/New project` 只读。
- 本轮不导出 Android APK，不导出 iOS/Xcode 工程，不修改 AI 和规则逻辑。
- 所有麻将牌显示矩形禁止相交；空间不足时先扩大轨道，再统一缩放，禁止负间距。
- 顶部玩家信息不得进入顶部暗牌、副露或弃牌轨道。
- 自家手牌保持大牌优先；对手副露与弃牌相对当前放大并保持完整可辨。
- 四档验收：`1365×768`、`2048×1152`、`2400×1080`、`2556×1179`。
- 当前工作树已有大量非 UI 改动，不覆盖、不重置、不清理。
- 未经用户确认不启用正式 `2.1.3` 发布身份；本轮恢复为 `2.1.2` 开发基线。

---

### Task 1：建立紧凑牌桌与零重叠失败合同

**文件：**
- Create: `tests/current/SichuanDenseTableLayoutRunner.gd`
- Modify: `tests/current/SichuanTableLayoutContractRunner.gd`

**接口：**
- Consumes: `MainSceneV2._seat_hud_rect(seat)`、`TableDiscardLayer.get_lane_rect(seat)`、`PlayerUI.get_visual_tile_rects()`。
- Produces: 四档分辨率、14 张暗牌、4 组副露、14 张弃牌和顶部 HUD 避让的自动验收门禁。

- [ ] **Step 1：写失败测试**

```gdscript
func _assert_non_overlapping(rects: Array[Rect2], label: String, failures: Array[String]) -> void:
	for left in range(rects.size()):
		for right in range(left + 1, rects.size()):
			if rects[left].intersects(rects[right]):
				failures.append("%s tile overlap %d/%d" % [label, left, right])
```

测试必须创建上家/下家 14 张暗牌、1～4 组副露及每家 14 张弃牌，并验证牌矩形、轨道矩形、SeatHUD 与中心区互不相交。

- [ ] **Step 2：运行并确认旧布局失败**

Run:

```bash
'/Applications/Godot.NET.app/Contents/MacOS/Godot' --headless --path '/Volumes/AI/Codex/四川麻将工程_20260701_v2' --script 'res://tests/current/SichuanDenseTableLayoutRunner.gd'
```

Expected: `FAILED`，至少命中侧边负间距或顶部 HUD/牌轨冲突。

### Task 2：扩大有效牌桌并移走顶部玩家信息

**文件：**
- Modify: `scripts/ui/table/SichuanTableMetrics.gd`
- Modify: `scripts/game/MainSceneV2.gd`
- Test: `tests/current/SichuanDenseTableLayoutRunner.gd`

**接口：**
- Produces: `_compact_board_rect()`、`_opponent_host_rect(seat)`、`_seat_hud_rect(seat)`；均以 `2048×1152` 参考坐标缩放。

- [ ] **Step 1：新增命名明确的参考矩形**

```gdscript
func _compact_board_rect() -> Rect2:
	return _scale_design_rect(304.0, 160.0, 1440.0, 710.0)

func _seat_hud_rect(seat: int) -> Rect2:
	match seat:
		0: return _scale_design_rect(20.0, 858.0, 232.0, 112.0)
		1: return _scale_design_rect(18.0, 250.0, 144.0, 132.0)
		2: return _scale_design_rect(1748.0, 34.0, 252.0, 96.0)
		3: return _scale_design_rect(1886.0, 250.0, 144.0, 132.0)
	return Rect2()
```

顶部 HUD 必须位于顶部暗牌右侧，不得再居中悬浮。

- [ ] **Step 2：让本家手牌使用接近全宽的底部轨道**

```gdscript
func _self_hand_rect() -> Rect2:
	return _scale_design_rect(20.0, 884.0, 2008.0, 262.0)
```

- [ ] **Step 3：运行布局合同**

Expected: HUD/牌轨不相交，有效牌区宽度不低于设计宽度的 68%。

### Task 3：重写上家、下家暗牌和副露轨，彻底取消重叠

**文件：**
- Modify: `scripts/ui/PlayerUI.gd`
- Test: `tests/current/SichuanDenseTableLayoutRunner.gd`

**接口：**
- Produces: `get_visual_tile_rects() -> Array[Rect2]`、`get_track_rects() -> Dictionary`。
- 侧边暗牌超过 7 张时使用两列，最多 `2×7`，不使用负间距。
- 副露按真实手牌数量动态占用内侧列，组间距至少 10 像素。

- [ ] **Step 1：删除负间距策略**

```gdscript
const SIDE_TILE_GAP := 5.0
const SIDE_GROUP_GAP := 12.0
```

删除 `SIDE_MELD_VERTICAL_OVERLAP`，`_side_hand_gap_for_count()` 不再返回负数。

- [ ] **Step 2：实现两列暗牌网格**

```gdscript
var row_count := mini(7, hand_count)
var column_count := ceili(float(hand_count) / 7.0)
var position := Vector2(
	float(index / 7) * (oriented_size.x + SIDE_TILE_GAP),
	float(index % 7) * (oriented_size.y + SIDE_TILE_GAP)
)
```

- [ ] **Step 3：副露使用独立内侧轨道**

每组碰/杠都创建完整矩形；来源方向由牌外箭头表示，不旋转一张牌去压住其他牌。

- [ ] **Step 4：暴露测试矩形并运行失败合同**

Expected: 上家/下家 14 张暗牌、现实合法的 `13/10/7/4/1` 手牌加 `0/1/2/3/4` 组副露全部零相交。

### Task 4：重排四家弃牌轨并禁止弃牌相互覆盖

**文件：**
- Modify: `scripts/ui/table/TableDiscardLayer.gd`
- Test: `tests/current/SichuanDenseTableLayoutRunner.gd`

**接口：**
- Produces: `get_tile_rects(seat) -> Array[Rect2]`。

- [ ] **Step 1：使用视觉尺寸计算步长**

```gdscript
const TILE_GAP := 5.0

func _non_overlapping_step(visual_size: Vector2, horizontal: bool) -> Vector2:
	return Vector2(visual_size.x + TILE_GAP, 0.0) if horizontal else Vector2(0.0, visual_size.y + TILE_GAP)
```

禁止使用 `tile_size.x * 0.78` 等小于牌宽的步长。

- [ ] **Step 2：固定容量**

- 本家：`7×2`。
- 对家：`14×1`。
- 上家/下家：`2×7` 或 `3×5`，按轨道长宽选择，不得重叠。

- [ ] **Step 3：弱化大面积半透明背景**

只保留低透明内框或压线，不再用大块底板强调空白。

### Task 5：统一四方向 2.5D 立体麻将牌

**文件：**
- Create: `scripts/ui/table/SichuanTileStyle.gd`
- Modify: `scripts/ui/TileVisual2D.gd`
- Modify: `scripts/ui/HandCanvas2D.gd`
- Test: `tests/current/SichuanTileAssetRegressionRunner.gd`
- Test: `tests/current/SichuanDenseTableLayoutRunner.gd`

**接口：**
- Produces: 共享色板、牌体厚度、接触阴影、高光、内沿、牌背渐变和状态外框。

- [ ] **Step 1：建立共享牌面色板**

```gdscript
const FACE_TOP := Color("FFF9E8")
const FACE_BOTTOM := Color("DDE8D8")
const SIDE_LIGHT := Color("B9CDB8")
const SIDE_DARK := Color("789B83")
const BACK_TOP := Color("5CC32D")
const BACK_BOTTOM := Color("218C3B")
const CONTACT_SHADOW := Color(0.0, 0.04, 0.02, 0.32)
```

- [ ] **Step 2：绘制牌体厚度与统一光源**

正面牌先画右侧面和底侧面，再画接触阴影、正面、内沿和左上高光；四个座位通过旋转整个完整牌体保持光照一致，不单独旋转符号层。

- [ ] **Step 3：统一本家手牌视觉**

`HandCanvas2D` 使用同一色板与厚度比例；本家 14 张牌的 `front_rect` 不相交，摸牌间距保留。

- [ ] **Step 4：验证花色清晰度**

Expected: 万字数字仍为黑、万字为红，条筒纹理可辨；立体侧面不覆盖符号。

### Task 6：降低空洞感并加强牌桌层次

**文件：**
- Modify: `scripts/ui/TableMaterialOverlay.gd`
- Modify: `scripts/ui/table/CenterTurnIndicator.gd`
- Modify: `scripts/ui/table/SeatHUD.gd`
- Modify: `scenes/ui/table/SeatHUD.tscn`

- [ ] **Step 1：桌布使用低对比毛毡纹理和内框**

去掉会形成大块空洞感的强分区底色；保留 2%～4% 纹理、细铜线和轻微外围暗角。

- [ ] **Step 2：压缩玩家信息**

顶部 HUD 使用横向 `252×96`；左右 HUD 使用紧凑 `144×132`，名字、分数、庄、已胡、缺门均不增加面板高度。

- [ ] **Step 3：强化当前回合**

中心仅突出余牌数和方位；当前方向使用金色边或牌外箭头，避免添加更多大文字。

### Task 7：截图矩阵、视觉复核和全量 UI 回归

**文件：**
- Modify: `tools/capture_main_scene.gd`
- Modify: `tools/capture_ui_matrix.gd`
- Modify: `tools/check_ui_capture.py`
- Modify: `README.md`

- [ ] **Step 1：固定压力牌局截图**

截图状态包含：顶部 13 张暗牌、左右各至少 10 张暗牌、四家副露、四家弃牌、摸牌间隔、最新弃牌标记。

- [ ] **Step 2：生成四档截图**

```bash
'/Applications/Godot.NET.app/Contents/MacOS/Godot' --path '/Volumes/AI/Codex/四川麻将工程_20260701_v2' --resolution 2048x1152 --script 'res://tools/capture_ui_matrix.gd' -- --capture-output='/Volumes/AI/Codex/四川麻将工程_20260701_v2/evidence/ui_dense_3d_20260715/table_2048x1152.png'
```

其余三档同样执行。

- [ ] **Step 3：逐张目检**

检查顶部 HUD、左右暗牌、四家副露、弃牌、本家手牌、中心指示、工具栏和安全区，无任何视觉相交或裁切。

- [ ] **Step 4：运行完整 UI 回归**

```bash
'/Applications/Godot.NET.app/Contents/MacOS/Godot' --headless --path '/Volumes/AI/Codex/四川麻将工程_20260701_v2' --script 'res://tests/current/SichuanDenseTableLayoutRunner.gd'
'/Applications/Godot.NET.app/Contents/MacOS/Godot' --headless --path '/Volumes/AI/Codex/四川麻将工程_20260701_v2' --script 'res://tests/current/SichuanTableLayoutContractRunner.gd'
'/Applications/Godot.NET.app/Contents/MacOS/Godot' --headless --path '/Volumes/AI/Codex/四川麻将工程_20260701_v2' --script 'res://tests/current/SichuanTableTouchTargetRunner.gd'
'/Applications/Godot.NET.app/Contents/MacOS/Godot' --headless --path '/Volumes/AI/Codex/四川麻将工程_20260701_v2' --script 'res://tests/current/SichuanUiSixIssuesRunner.gd'
'/Applications/Godot.NET.app/Contents/MacOS/Godot' --headless --path '/Volumes/AI/Codex/四川麻将工程_20260701_v2' --script 'res://tests/current/SichuanIosFontRegressionRunner.gd'
'/Applications/Godot.NET.app/Contents/MacOS/Godot' --headless --path '/Volumes/AI/Codex/四川麻将工程_20260701_v2' --script 'res://tests/current/SichuanTileAssetRegressionRunner.gd'
'/Applications/Godot.NET.app/Contents/MacOS/Godot' --headless --path '/Volumes/AI/Codex/四川麻将工程_20260701_v2' --script 'res://tests/current/SichuanTableMotionContractRunner.gd'
```

Expected: 所有 runner 打印成功标记，且无 `SCRIPT ERROR`、`Parse Error`、`FAILED`。

- [ ] **Step 5：确认没有打包产物**

本轮只交付源码、测试与截图证据，不运行 Android/iOS 导出脚本。

## 自检结果

- 需求覆盖：顶部信息移位、扩大有效区、左右 AI 零重叠、四家弃牌零重叠、四方向立体牌、减少空白、四档截图和压力测试均有对应任务。
- 占位符扫描：无 `TBD`、`TODO` 或未定义步骤。
- 接口一致性：`PlayerUI.get_visual_tile_rects()` 和 `TableDiscardLayer.get_tile_rects(seat)` 由布局测试统一消费。
- 任务边界：本轮不改规则、AI、结算算法和移动端导出。
