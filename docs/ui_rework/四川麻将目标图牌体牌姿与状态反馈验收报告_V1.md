# 四川麻将目标图牌体、牌姿与状态反馈验收报告 V1

验收日期：2026-07-22
对应标准：`docs/ui_rework/四川麻将目标图牌体牌姿与状态反馈验收标准_V1.md`

## 结论

本轮 A1–A7 全部通过，用户指定的六项修正均已进入实时 Godot 3D 表现通路。实际 Metal Forward+ 图像证明暗牌、明牌、摸牌标记和四组副露压力状态均可正常渲染。全量当前 Godot Runner `26/26` 通过，Blender 5.2 LTS 可重复生成 GLB，项目无脚本解析错误。

本报告所谓“通过”是指定功能、空间合同和当前美术门槛通过，不声称与商业参考图像素级复制。商业参考仍有角色头像、外围场景、定制特效和更多手工材质细节，不在本次六项修正范围内。

## 六项修正对齐

| 用户要求 | 实施结果 | 验收证据 |
| --- | --- | --- |
| 上家、下家站立角度 | 侧家牌架倾角改为 `76°`，暗牌显示翡翠绿大背面+象牙窄顶边，两侧镜像对称 | A3；摄像机四比例 `4/4`；暗牌 Metal 图 |
| Blender 重建麻将牌 | Blender 生成暖象牙主体+独立翡翠绿背层，正面浅凹、安全边、四段倒角；导入 Godot 使用 | A1/A2；`mahjong_tile_body.glb` 20,448 bytes；两项牌体回归通过 |
| 摸牌小黄锥反馈 | 取消摸牌整张变色，改为牌上方单一金色立体钻锥，`120°/s` 旋转；减少动态时静止但不消失 | A5；明牌/摸牌 Metal 图；状态和动效合同通过 |
| 恢复旧中央余牌区 | 恢复深青黑切角牌匾、暗金双线，只保留“余牌 / 大数字 / X家出牌中”，去掉东南西北和彩色方向角 | A6；HUD 状态完整性和商业成熟度合同通过 |
| 上/下家碰杠方向及动态缩进 | 侧家副露字面与手牌阅读方向一致；各组沿桌边并行列布置，四组压力状态不与手牌、弃牌区相交 | A4；3D 牌桌与空间布局合同通过；四组副露 Metal 图 |
| 本家缺门牌优先在右 | 仅调整显示顺序：缺门牌连续放最右，其余牌稳定排序；tile id、选牌和点击映射保持原值 | A7；缺条/缺筒/缺万与点击映射回归通过 |

## 量化验收结果

| 门槛 | 实测/合同结果 | 状态 |
| --- | --- | --- |
| A1 外框 | Godot X/Y/Z = `0.42/0.18/0.58`，独立 WarmIvory/JadeBack 几何 | 通过 |
| A2 材质 | 暖象牙、中粗糙度、非金属；面/倒角/侧壁/接触影在 Metal 图可辨 | 通过 |
| A3 侧家牌姿 | `76°`，要求 `74°–78°`；左右透视比均 `1.2749` | 通过 |
| A4 副露压力 | 1–4 组并行缩进；手牌/副露/弃牌边界无交叉 | 通过 |
| A5 摸牌标记 | 仅 1 个立体金色钻锥；`120°/s`；reduced-motion 冻结 | 通过 |
| A6 中央面板 | 方向字 `0`，边框 `2`层，文字水平居中误差 `≤2 px`，图形层忽略鼠标/触控 | 通过 |
| A7 缺门右置 | 三种缺门+13/14 张+重复牌+id 逆映射 | 通过 |

## 验证证据

### 自动化

- `tests/current/*Runner.gd`：`26/26` 通过，`0` 失败。
- 摄像机横屏矩阵：`1365×768` / `2048×1152` / `2400×1080` / `2556×1179`，`4/4` 通过。
- 16:9 结果：本家牌高 `15.57%`，本家总宽 `76.66%`，远端/本家牌高比 `39.15%`，侧家透视比 `1.2749/1.2749`。
- `python3 -m py_compile tools/3d/generate_mahjong_assets.py`：通过。
- Blender 5.2.0 LTS 无界面重建：exit `0`；Tripo3D 调用 `0`。
- GLB SHA-256：`mahjong_tile_body.glb = 363b17b4f50c9c1349cf59c8973bc4aed4b1e2d9869fefdb7f8b3e6cd08f1e00`。
- Godot 项目解析：exit `0`，无 `Parse Error` / `SCRIPT ERROR`。
- 本轮源码密钥扫描：无 `sk-` / `tsk_` / `API_KEY` 命中。

### 实际渲染

三张终验图均由 Godot Metal 4.0 Forward+ 在 Apple M1 Pro 上以 `2048×1152` 捕获：

- `evidence/ui_tile_pose_feedback_20260722/camera_hidden_2048x1152_final.png`：暗牌模式、侧家牌姿、中央旧牌匾、缺门右置。
- `evidence/ui_tile_pose_feedback_20260722/reveal_draw_2048x1152_final.png`：明牌方向、摸牌金色钻锥、四家弃牌与副露。
- `evidence/ui_tile_pose_feedback_20260722/max_meld_2048x1152_final.png`：本家四组副露、手牌右移、侧家副露与空间压力。

## 客观视觉复核

与目标图相比，本轮已消除最明显的结构性差异：两侧不再是斜放的平牌，暗牌不再是灰色薄卡，摸牌不再整块变色，中央不再是复杂方向仪表，副露也不再穿入弃牌区。麻将牌的曲面倒角、象牙主体、绿背层和接触影已可稳定读取。

仍然存在但不影响本轮通过的 P2 差异：

1. 参考图是完整商业美术包，含头像、大厅环境、独立动效和手工材质；当前工程仍保留原有 HUD 身份框。
2. 目标牌面是专用商业符号集；当前使用本项目已经验证的条/筒/万字面资源，不直接复制竞品美术。
3. 当前牌桌是清爽夏日蓝桌方向，已保持与用户前一轮的配色决策一致，不按参考图逐像素复制。

## 边界

- 本轮未修改麻将规则、AI、计分或胡牌判定。
- 本轮没有新的打包或 iOS 覆盖安装请求，因此最强证据到“本机 Metal 真实渲染+全量自动回归”，不把这一层证据写成 iPhone 真机完整牌局。
