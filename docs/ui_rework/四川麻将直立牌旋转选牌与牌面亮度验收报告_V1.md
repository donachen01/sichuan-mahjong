# 四川麻将直立牌、旋转选牌与牌面亮度验收报告 V1

日期：2026-07-22
版本：`2.6.3`

## 当前结论

- 三家暗手、选牌反馈与本家牌面亮度已通过源码合同、真实 Metal 截图和全量回归。
- iOS 2.6.3 Xcode 工程和 NativeAOT 已导出成功；签名构建因本机 Xcode 账号列表与 provisioning profile 缓存为空而暂停，手机当前 2.6.2 未被覆盖。
- 规则、计分、AI、tile id、定缺排序和点击映射未改动。

## UI 验收结果

| 验收项 | 结果 | 证据 |
| --- | --- | --- |
| 三家暗手与桌面 90° | 通过 | `opponent_rack_tilt_degrees=90`、`far_rack_tilt_degrees=90`；三家牌高轴水平分量不超过 `0.001` |
| 选牌无整牌底色 | 通过 | `StateMarker` 在 selected 状态强制隐藏 |
| 独立旋转图形 | 通过 | 天青色 `SelectedThreeBladeRotorMarker`，`96°/s`；金色摸牌钻锥为 `120°/s` |
| 双标记共存 | 通过 | 同牌状态左右各偏移 `0.115 m`，自动合同确认不重叠 |
| 本家/弃牌白度一致 | 通过 | 2048×1152：本家 `241.1`、弃牌 `242.0`，绝对差 `0.9/255` |
| 横屏视觉检查 | 通过 | `evidence/ui_upright_selection_brightness_20260722/selection_1334x750.png`、`selection_calibrated_2048x1152.png`、`selection_2556x1179.png` |

## 回归证据

- Godot 当前 Runner：`27/27` 通过。
- 摄像机构图：1365×768、2048×1152、2400×1080、2556×1179，`4/4` 通过。
- `SichuanMahjong.Godot.csproj` Release：`0 warning / 0 error`。
- `git diff --check`：通过。
- Godot 退出时仍有项目既存 RID/ObjectDB/resource leak 告警；所有 runner 退出码均为 0，本轮未新增崩溃或脚本错误。

## iOS 2.6.3 发布证据

- Xcode 工程：`build/ios/SichuanMahjong-2.6.3-ios-xcode/SichuanMahjongIOS.xcodeproj`，Godot 导出返回 `ios_export_result=0`。
- Bundle ID：`com.chendong.sichuanmahjong.iosdev`；Team ID：`FCB4ZVWWD8`。
- 本机仍有一个有效 `Apple Development: donachen2027@outlook.com (MN54CC5STV)` 证书。
- 第一轮自动签名构建在 `GatherProvisioningInputs` 阶段失败：`No Accounts`、`No profiles for 'com.chendong.sichuanmahjong.iosdev' were found`；尚未产生可安装 App，不把这次失败记作构建通过。
- 手机通过 CoreDevice 仍在线配对，设备库确认当前安装版本为 `2.6.2/2.6.2`。下一步是手机 USB 连接并解锁后，只读导出其仍有效的个人 provisioning profile，再继续手动开发签名构建；也可由用户在 Xcode Accounts 重新登录 Apple ID 触发自动 profile。
