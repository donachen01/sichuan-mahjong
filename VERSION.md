# 版本

当前应用版本：`2.6.11`

`2.6.11` 是牌局指向标记、胡牌布局与 Android 体积收口版：碰/杠来源统一为贴牌的天蓝色短杆小箭头；胡牌来源仅保留更小的简洁箭头，不再显示点炮座位文字；上家、下家胡牌结果避开本家手牌区；胡牌和暗杠使用与正常暗手一致的墨绿牌背。Android 发布包排除了测试、证据、工具、文档与设计源资产，不改变规则、计分、AI和触控合同。

## 版本维护规则

- 每次正式发布同步更新 `project.godot`、`export_presets.cfg`、`VERSION.md` 与 `README.md`。
- Android 当前配置使用 `version/name=2.6.11`、`version/code=271`，产物文件名包含版本号。
- iOS 当前使用短版本号和构建版本 `2.6.11`，产物目录名包含版本号。
- 构建产物保留在 `build/android/` 与 `build/ios/`，验证证据保留在 `evidence/`。
- 版本完成标准区分源码测试、构建、签名、安装、启动和真机完整流程，不以其中一层替代另一层。
