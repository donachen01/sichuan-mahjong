# 版本

当前应用版本：`2.6.6`

`2.6.6` 是对手明暗牌、自摸展示、退出交互与副露来源标记修正版：三家暗手统一翡翠牌背材质；明牌模式同时翻开上家、对家、下家；AI 自摸继续整手平扣为牌背，本家自摸改为整手正面平铺，并用旋转金色标记指出自摸张；左上工具栏提供带确认层的“退出游戏”，iOS 确认后调用平台可用的进程退出通路；碰、杠来源改为第二张牌正上方的小号蓝色卡通箭头。

## 版本维护规则

- 每次正式发布同步更新 `project.godot`、`export_presets.cfg`、`VERSION.md` 与 `README.md`。
- Android 当前配置使用 `version/name=2.6.6`、`version/code=266`，产物文件名包含版本号。
- iOS 当前使用短版本号和构建版本 `2.6.6`，产物目录名包含版本号。
- 构建产物保留在 `build/android/` 与 `build/ios/`，验证证据保留在 `evidence/`。
- 版本完成标准区分源码测试、构建、签名、安装、启动和真机完整流程，不以其中一层替代另一层。
