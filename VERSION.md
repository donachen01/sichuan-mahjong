# 版本

当前应用版本：`2.6.18`

`2.6.18` 上移定缺圆印中的条/筒/万文字，立牌与胡牌倒牌统一同一实体模型和运行时缩放，弃牌底层明确补回绿色背层；重新按内容高度约束结算页三区域，使用响应式列宽和裁剪，避免文字、边框和下一局按钮越界。Android 继续使用已验证的 `gl_compatibility`，iOS 保留 Forward+。

`2.6.17` 平滑移动端倒牌阴影并提升阴影贴图精度；修正条/筒/万在圆印中的 CJK 光学居中；牌墙流局时排除已胡玩家的重复查叫收分；结算页支持通过鼠标和 iOS 原生触摸点击任一玩家查看其手牌与逐项得分，并以四档响应式布局保持标题、内容和下一局按钮完整处于屏幕内。Android 继续使用已验证的 `gl_compatibility` 发布后端，iOS 保留 Forward+。

## 版本维护规则

- 每次正式发布同步更新 `project.godot`、`export_presets.cfg`、`VERSION.md` 与 `README.md`。
- Android 当前配置使用 `version/name=2.6.18`、`version/code=278`，产物文件名包含版本号。
- iOS 当前配置使用短版本号和构建版本 `2.6.18`；设备安装、启动与真机牌局验证必须和构建证据分层记录。
- 构建产物保留在 `build/android/` 与 `build/ios/`，验证证据保留在 `evidence/`。
- 版本完成标准区分源码测试、构建、签名、安装、启动和真机完整流程，不以其中一层替代另一层。
