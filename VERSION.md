# 版本

当前应用版本：`2.6.8`

`2.6.8` 是杠上花验证、暗杠辨识与翡翠织纹桌布修正版：补齐“暗杠后摸最后一张牌并合法自摸”的完整回归，确认胡牌入口和杠上花计分链；暗杠四张牌改为四张清晰分离的翡翠牌背。3D 主桌与 2D 回退桌面统一改为参考图的翡翠绿色、细密交叉织纹与边缘低浮雕云纹，不显示任何品牌文字。

## 版本维护规则

- 每次正式发布同步更新 `project.godot`、`export_presets.cfg`、`VERSION.md` 与 `README.md`。
- Android 当前配置使用 `version/name=2.6.8`、`version/code=268`，产物文件名包含版本号。
- iOS 当前使用短版本号和构建版本 `2.6.8`，产物目录名包含版本号。
- 构建产物保留在 `build/android/` 与 `build/ios/`，验证证据保留在 `evidence/`。
- 版本完成标准区分源码测试、构建、签名、安装、启动和真机完整流程，不以其中一层替代另一层。
