# 版本

当前应用版本：`2.6.61`

`2.6.61` 修复结算页的桌布皮肤联动，选中皮肤的真实纹理与配色会进入结算背景；点炮胡牌张独立显示，并在牌张上方用悬浮立体箭头标识点炮来源，极端手牌数量下也会自动等比适配。分数明细文字与得分数字放大，增加适合手机的粗滚动条，并将 iOS 触摸拖动交回原生 ScrollContainer 处理。

`2.6.60` 补齐本家 AI 操作提示：他家出牌后的响应窗口会调用同一套 C# AI 响应决策，明确显示建议胡、杠、碰或过、对应牌张、可选动作和核心原因；自己回合的自摸、暗杠、补杠也会与继续出牌统一比较并显示建议。提示框不代替玩家操作，AI 核心未返回时明确显示不可用，不由界面猜测动作。

`2.6.59` 深化移动端性能治理：iOS 从桌面取向的 Forward+ 切换到 Godot Mobile 渲染器；触摸与动画短时保持 60 FPS，静止牌桌自动降到 30 FPS并启用低处理器模式，静止且没有 AI 后台请求时主场景帧回调完全休眠。移动端默认关闭方向光动态阴影、绒布各向异性和牌体清漆/边缘光/次表面等高成本材质分支；相同 3D 麻将状态不再重复递归配置材质，最新弃牌金色标记停止永久旋转。AI 结果队列只在存在后台请求时轮询，看门狗只在过渡恢复时复制完整快照，牌局状态刷新不再递归重刷整棵 UI 字体树；点炮/抢杠胡的公开胡牌张和来源箭头保持始终显示。

`2.6.58` 针对 iPhone 连续多局后发热与耗电：移动端限制为 60 FPS 并关闭 SSAO，静态圆形按钮改为事件驱动重绘，演出调度器在空闲时停止处理，AI 后台结果轮询固定为 30 Hz，不改变规则、计分、触摸区和动画时长。

`2.6.57` 补齐结算页标题栏右侧背景，并将按钮和方位框的圆环角饰替换为边角如意云纹；点炮胡牌张恢复立体金色来源箭头。下一庄按首胡、一炮多响点炮者、流局留庄执行；点杠改为点杠者付 2 分且其余未胡玩家各付 1 分，暗杠各付 2 分，补杠各付 1 分，呼叫转移按实际杠钱总额转移。根番继续统一统计手牌与副露杠。

`2.6.56` 按目标图重做本局结算页：接入原创蜀风云纹、山水、竹叶和多层描金装饰底板，重建深色标题栏、四家金边米白积分卡、分数明细题匾和玉色下一局按钮；中部严格保持标签一行、麻将一行，并放大手牌、碰牌与胡牌张。桌布换肤只改变结算底板纹理与主色，不改变结构。

`2.6.55` 修正本家四川话男声：将四川话男声替换为温暖阿虎 2.0，并改用筒、条、万及动作短句连续生成后按静音切分，降低逐条生成造成的音色漂移；运行时测试新增本家实际加载 `tts_sichuan/male`、上家加载 `tts_sichuan/female` 的资源路径断言。

`2.6.54` 新增普通话/四川话四套男女语音：本家和对家使用男声，上家和下家使用女声；普通话采用天才同桌 2.0、小何 2.0，四川话采用云舟 2.0、Vivi 2.0。筒、条、万出牌严格只报对应牌名，碰、杠及点炮胡、自摸、抢杠胡、杠上花、杠上炮使用各角色的短句，并在左上工具栏切换语言。

`2.6.53` 根据 iPhone 实画面再次收敛牌桌信息层：缩小左上工具抽屉并在展开时动态让上家铭牌下移避让；桌布选择器与文字整体放大，并计算工具栏占用区后在右侧独立居中；AI 提示在后台分析返回前立即提供有效建议，提示框恢复居中、触摸拖动并扩大正文。本家铭牌继续下移，对家铭牌向左调整。

`2.6.52` 将上家铭牌下移至放大后的左上工具按钮下方，并保留安全间距，消除收起状态下按钮与舒燕铭牌的遮挡；其他三家铭牌位置不变。

`2.6.51` 按真机反馈将左上工具抽屉的按钮和文字提升至 2.6.50 的约两倍，并改为双列向下展开以兼顾结算入口和屏幕高度；上家、本家铭牌进一步靠左，下家铭牌进一步靠右。四家铭牌改为半透明烟熏翡翠玻璃底，保留铜边、高光和文字描边，在减轻桌面遮挡的同时维持信息可读性。

`2.6.50` 重绘横屏牌桌信息层：四家铭牌扩大并提升姓名、分数字号；左上工具改为大图标、向下展开的覆盖式抽屉，结算与下一局入口统一收纳其中，并在定缺阶段保持随时可展开和缩进；条、筒、万定缺圆印及文字扩大一倍。修正明牌展示优先级：开启明牌以及牌局结算时，四家剩余暗手全部翻开；默认胡牌演出仍保留 AI 盖牌效果，副露展示不受影响。

`2.6.49` 将四川麻将课程与理论书的行牌方法接入正式 AI：普通进攻先比较向听和实际活张，再以四类经典牌型、成搭保留和听口质量破平；同时修正明杠根数及金钩钓组合番、自摸加底的课程核对。书中“机会数”保持为结构启发式，不冒充精确胜率，防守、保分和追大番仍使用独立的桌面目标。

`2.6.48` 修正 `2.6.47` 动作按钮未真正跟随皮肤的问题：六套皮肤分别使用本桌布真实 albedo 布料贴图渲染圆形碰、杠、胡、取消按钮面，保留单层铜边与大字，不再使用方形平涂样式。

`2.6.47` 重新设计碰、杠、胡、取消动作徽章：保留单圈识别但移除圈中圈复杂装饰，文字放大并增加暖象牙描边/阴影；按钮色板随当前桌布皮肤即时切换，不改变玩法逻辑。

`2.6.46` 将碰、杠、胡按钮尺寸与触摸区域扩大一倍；修正补杠候选和结算，使碰牌后的第四张牌即使留在手牌、不是当前最新摸牌，也能在后续自己回合显示补杠并按补杠分结算。

`2.6.45` 修正碰、杠、胡来源箭头的挂点与几何：箭头现在位于麻将牌面中心，采用带厚度的实体箭头，并按来源座位方向旋转；仅调整界面表现。

`2.6.44` 将碰、杠、胡来源箭头统一为中心弃牌黄色菱形的金黄色材质，放大箭头轮廓并保留来源方向标注；仅调整界面表现，不改变四川麻将规则、AI 或牌局流程。

`2.6.43` 将四川牌桌中心区域替换为内江最近版本的单环中心仪表：连续深玉底座、四块齐平活动色嵌片、珠白分隔线、单个金色圆环和数字牌盘；取消旧玻璃菱形、红色活动区和东南西北文字。四川规则、AI、牌局流程和其他桌面区域不变。

`2.6.42` 针对青黛格绒单独缩小格纹：将 UV 密度提高到 6.0，并降低法线强度，避免大格子抢占桌面视觉。其他五套皮肤不变。

`2.6.41` 优化桌布皮肤交互与材质观感：缩进按钮扩大触摸目标，换肤面板先显示后完成选中态整理，减少首次打开等待；六套桌布统一纹理密度、法线强度和翡翠/铜色桌面配色。玩法、AI、牌体和桌面灯光预算不变。

`2.6.40` 修复桌布皮肤入口的交互反馈：打开换肤面板时不再强制收回桌面工具栏，工具栏保持展开，换肤面板在其上层显示；保留 2.6.39 的触摸卡片明确命中分发。玩法、AI、牌体和桌面灯光预算不变。

`2.6.39` 修复 iOS 换肤面板点击无效：模态层现在对触摸卡片做明确命中分发，并只吞掉同一次触摸产生的重复鼠标事件，皮肤卡片可以正常应用并保存选择；入口打开弹窗时保留工具栏展开状态，避免误认为点击被收回；玩法、AI、牌体和桌面灯光预算不变。

`2.6.38` 新增与内江麻将一致的桌布换肤入口，提供深翡翠绉绒、翡翠精纺、墨绿暖绒、黑金暗纹、香槟缎面、青黛格绒六套 Poly Haven CC0 实体布料 PBR 材质。换肤只替换 `TableFelt` 的 BaseColor、Normal 和 Roughness 贴图，不改变牌体、桌体几何、相机、触控、规则、计分或 AI，也不增加移动端实时光源；选择结果写入本地偏好并在后续牌局恢复。桌面面板按安全区布局并阻断其下方麻将输入。

`2.6.37` 建立可复验的离线 AI 进化闭环：弃牌与碰/过/杠候选共享同一批按后验权重聚合的暗手和牌墙粒子，由不读取线上评分的交叉验证裁判输出动作净值、置信区间和后悔值；候选策略以单座对三个冻结基线、四座轮换和相同牌墙进行整局对照。线上弃牌增加尾盘安全降向听与精确牌形近分裁决，同向听同活张时不再让策略残差压过互斥分解，同时不允许近分回退增加拆对子。500 状态盲测按 `0.002` 非劣界晋级：严重错误率为 0、同状态一致率 100%，反应动作平均后悔值从冻结基线 `0.7950` 降至 `0.2275`；160 局混合擂台平均净分 `+0.14375`，但区间跨 0，不声称统计显著胜出。512 状态线性影子模型未达到改善与 Top-1 门槛，保持离线、未进入生产。

`2.6.36` 针对真机尾盘未成叫与过度碰牌问题，将“未见牌”拆分为对手暗手与真实牌墙后验，并严格守恒到当前墙余张数；墙空时不再把他家暗手当成活张，直接以精确听口、4 番封顶和查叫付款裁决。修正过、碰、杠后距离下次自摸的座次偏移，各动作共用同一批确定性后验牌墙；旧的“对子减少就奖励碰”仅保留诊断，不再参与统一净值选择。动态前瞻仅限墙余 12 张内且净值近似打平时启用，避免用一份具体牌序代替早中盘概率分布。真机 Top-K 诊断增加统一动作净值和策略残差字段。

`2.6.35` 修正碰、过、直杠、暗杠和补杠反事实的真实动作时序：过牌保持 13 张等待摸牌，碰牌完成副露后再枚举弃牌，杠牌先结算并补张，再判断杠上花或后续弃牌。所有候选使用同一批确定性补张样本和同尺度 ActionValue 比较，不再由随机短搜索或预先锁定的旧启发式决定最终动作。手牌结构改为互斥分解的动态规划，保留近优牌形、五搭、对子、刻子和七对路线，降低无收益拆对子/拆搭子。新增离线独立反事实裁判、冻结基线配对擂台和受限影子日志；影子日志仅记录公开信息与决策座位手牌，单会话最多 1600 事件、最多保留 4 个会话，不启用线上自动调权。

`2.6.34` 将默认老手基线切换为只读公开信息的 `bone_ash`，并把 `hell` 明确隔离为透视围剿挑战；桌面与 iOS 不再因移动平台自动进入轻量牌理路径。四川计分统一为 4 番（16 倍）封顶，碰、直杠、暗杠、补杠、过牌和胡牌共享同一规则与净收益合同。副露反事实会枚举后续弃牌、完整听口、路线损失、首打风险、杠后补牌和抢杠风险；公开读牌新增手切/摸切、生熟张、筋线断张、根潜力和定缺竞争特征。同状态模拟、逐动作后悔值与 Brier/ECE 作为发布证据，不新增固定回归清单。

`2.6.33` 在不改变桌布 BaseColor 的前提下，用 Blender 生成可穿越移动端 mip 的双尺度干净短绒 Normal/ORM，增强启动页式绒感同时保持低频无水渍。中心余牌圆面改为独立不透明哑光烟熏玉，数字采用暖象牙细描边，消除遮挡视线的高光。新摸牌不再显示蓝色旋转菱形，而是作为同一张可点击实体牌暂放手牌最右侧；弃出它时直接飞向牌河，弃出其他牌时再以 `180ms` 归入正确排序位置。

`2.6.31` 修复 2.6.30 桌布 BaseColor 的多尺度明度调制在 iOS mipmap 过滤后形成大片云状水渍的问题。Blender 5.2 生成器现在输出均匀、自然暖绿的 BaseColor，细密短绒只由高频 Normal 和 `0.86–0.90` 粗糙度表达；真实桌面顶面 UV 的低频能量与亮度跨度加入专项门禁，Metal 成品仍保持 `RGB(53, 131, 64)` 的启动界面同类色调。固定 27 项回归清单已按用户指示删除，单项 Runner 保留并仅按本次修改风险选择执行。几何、UV、灯光、木框、皮革、中心方向器、布局、触控、规则、计分和 AI 均未改变。

`2.6.30` 将牌局桌布重建为与启动图质感一致的自然暖绿短绒 PBR：BaseColor 使用克制的多尺度明度变化，Normal 改为四组近等权、多方向高频绒毛场，移除旧版偏青底色和纵向织带；roughness 收敛到 `0.86–0.90`，metallic 保持 `0`。固定 Metal/Filmic 灯光仅做限定暖色校准，四档成品桌布中位色均为 `RGB(53, 131, 64)`，接近启动图基准约 `RGB(54, 130, 64)`。木框、皮革、凹槽、中心方向器、镜头、布局、触控、规则、计分、AI 和牌面专用补光保持不变。

`2.6.29` 修正中心四向器按相同 `90°` 生成状态片造成的东/西缺红：真实石墨分隔线对应东/西 `124.48292°`、南/北 `55.51708°`，四个状态片现与分隔线共用角度参数，东/西原缺口楔形完整填充，同时继续使用半径 `0.460` 的圆弧切口避让 `0.455` 古铜计数环。结算页改为单层深翡翠主壳，玩家列、手牌和明细区域取消完整套框，以焦点左侧铜色强调和交替底色建立层级；四家列表与明细行按可用高度扩展，减少空白，并放大主标题、玩家信息、统计、焦点分数和表格正文。展示字段、权威账本、规则、AI、相机、牌体和触控合同保持不变。

`2.6.28` 修正中心四向器的状态片内缘不对称：旧东/西状态片使用 `y=±0.20`，会伸入半径 `0.365` 的透明余牌镜片下方；北/南则停在 `x=±0.38`。现在四个真实 3D 红色状态片统一使用半径 `0.460` 的分段圆弧切口，完整避让半径 `0.455` 的中央古铜计数环，余牌数字圆圈内不再透出红色。状态片仍覆盖外侧直边和切角，最终 Metal 红色保持精确 `#A13D2D`，四边黄色描边保持移除；模型为 `1,044` 三角面、`9` 个静态对象和 `4` 个材质。规则、AI、相机、触控、牌体与桌面布局不变。

`2.6.27` 根据最终牌桌近景把 2.6.26 过度装饰、凸起的中心件重做为齐平亮面玻璃嵌件。Blender 资产由连续烟熏翡翠玻璃、石墨方向发丝线、干净且无黄色/旧铜描边的外缘、独立低高光古铜计数环、玻璃镜片和用户指定的 `#A13D2D`（RGB `161, 61, 45`）状态片构成；状态片仍是真实 3D 顶面网格，但使用经过 Filmic 显示补偿的无光照材质，避免暖色灯光与清漆把最终画面抬成粉红色。每个状态方向拆成一个主梯形和两个切角三角形，确保东/南/西/北激活时整块区域连同直边与两侧切角都被红色填满，且无黑侧壁。方向字使用不受玻璃深度遮挡的亮白/暖白字和细描边。中心最高面只保留 `0.006` 世界单位防闪烁间隙，视觉上与绒布齐平；模型为 `988` 三角面、`9` 个静态对象和 `4` 个材质。桌布底色去除大块低频明暗斑，只用细密短绒法线和微粗糙度表达织物；分界线沉入绒布并收窄为近色暗纹。三家立牌中心高度为 `桌面接触面 + 当前座位等比牌高的一半 + 0.012`，牌底不再插入桌布并保留接触阴影。规则、AI、相机、触控和本家 `0.83` 紧凑牌距保持不变。

`2.6.25` 修复 2.6.24 本家手牌中心距小于实体牌宽、导致白色牌身重叠成一整条且失去逐牌边界的问题：常规中心距改为 `0.83`，在 `0.42 × 1.94 = 0.8148` 实体牌宽之外保留约 `0.0152` 的真实正间隙；比 2.6.23 的约 `0.873` 更紧凑。中心四向盘升级为静态多层实体模型，增加暗色底座、蓝钢外壳、金属倒角、香槟色内嵌边、四条分区接缝和三级中央表圈；所有组件关闭投影且无逐帧动画，继续保留 Android `2048/质量 0` 阴影预算与 `gl_compatibility`。

`2.6.24` 按用户提供的原版牌桌参考图，将本家手牌从 2.6.23 的明显正间隙恢复为原始固定 `0.80` 中心距，使 14 张牌紧密排齐、只留下牌体边缘细缝；牌体大小、相机和触控映射保持不变。Android 继续使用 2.6.23 已恢复的 Godot 4.6 移动阴影默认预算 `2048/质量 0` 与 `gl_compatibility`，不重新引入 2.6.17 的 4096/质量 2 性能退化。

`2.6.23` 将中心区按参考图重做为低矮四向实体仪表盘：蓝灰切角外壳、深色四分区、中央圆形余牌数字、当前方位红色梯形与亮色方位字；静态网格只随回合和余牌数更新，不增加逐帧 UI。修复本家手牌步距小于实体牌宽造成的白面相互重叠，所有合法缩放下保留明确正间隙。Android 移动阴影从 4096/中等软滤镜恢复 Godot 4.6 的 2048/质量 0 移动预算，针对 2.6.16 之后的卡顿退化；除明确要求的中心盘和本家手牌布局外，不改变规则、AI、相机或触控。

`2.6.22` 在 `2.6.21` 大小/厚度校准上继续修复其余三家平扣与碰杠的白绿分层：共享 `mahjong_tile_body.glb` 的翡翠背层由 18mm 总内缩收紧为 3mm，平扣实体覆盖层复用 `0.414×0.574` 近满幅尺寸，保留窄象牙唇边并保持真实圆角、厚度和 PBR 材质。上/下家平扣 `1.34`、对家平扣 `1.56`、本家碰/杠 `0.84×` 保持不变；未改变规则、AI、相机、触控或桌面布局合同。

`2.6.20` 修正三家 AI 胡牌平扣牌列的视觉倍率：恢复上家/下家 `1.44`、对家 `1.28` 的原有三维牌体大小，不再误用本家胡牌倍率；翻扣抬高量按实际等比倍率同步计算，确保同一 `0.42×0.24×0.58` 实体 GLB 的完整侧壁落在桌面上，不被桌面遮住。暗杠中间两张同样按副露倍率校正落桌高度。未改变规则、AI、相机、触控或桌面布局合同。

`2.6.19` 修正上家、对家、下家平扣胡牌牌背和四座位暗杠中间牌受顶灯直射后偏亮/发白的问题，使用深翡翠 PBR 源色补偿；三家 AI 胡牌平扣手和暗杠整组统一三轴模型与同步牌距，恢复与立牌一致的颜色和实体厚度观感。未改变规则、AI、相机、触控或桌面布局合同。

`2.6.18` 上移定缺圆印中的条/筒/万文字，立牌与胡牌倒牌统一同一实体模型和运行时缩放，弃牌底层明确补回绿色背层；重新按内容高度约束结算页三区域，使用响应式列宽和裁剪，避免文字、边框和下一局按钮越界。Android 继续使用已验证的 `gl_compatibility`，iOS 保留 Forward+。

`2.6.17` 平滑移动端倒牌阴影并提升阴影贴图精度；修正条/筒/万在圆印中的 CJK 光学居中；牌墙流局时排除已胡玩家的重复查叫收分；结算页支持通过鼠标和 iOS 原生触摸点击任一玩家查看其手牌与逐项得分，并以四档响应式布局保持标题、内容和下一局按钮完整处于屏幕内。Android 继续使用已验证的 `gl_compatibility` 发布后端，iOS 保留 Forward+。

## 版本维护规则

- 每次正式发布同步更新 `project.godot`、`export_presets.cfg`、`VERSION.md` 与 `README.md`。
- Android 当前配置使用 `version/name=2.6.61`、`version/code=321`，产物文件名包含版本号。
- iOS 当前配置使用短版本号和构建版本 `2.6.61`；设备安装、启动与真机牌局验证必须和构建证据分层记录。
- 构建产物保留在 `build/android/` 与 `build/ios/`，验证证据保留在 `evidence/`。
- 版本完成标准区分源码测试、构建、签名、安装、启动和真机完整流程，不以其中一层替代另一层。
