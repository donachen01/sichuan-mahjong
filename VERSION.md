# 版本

当前应用版本：`2.6.7`

`2.6.7` 是结算明细与全面毛绒桌布修正版：牌墙流局时，已胡玩家收到的查大叫不再成为隐藏差额，而是在明细中独立列出；任何未被专门展示的新结算事件都会作为账本调整明示，保证可见明细相加严格等于最终收分。3D 主桌与 2D 回退桌面移除菱格、云纹、回纹等几何暗纹，改用覆盖全桌、交错无规则的程序化短绒纤维和顺逆毛明暗。

## 版本维护规则

- 每次正式发布同步更新 `project.godot`、`export_presets.cfg`、`VERSION.md` 与 `README.md`。
- Android 当前配置使用 `version/name=2.6.7`、`version/code=267`，产物文件名包含版本号。
- iOS 当前使用短版本号和构建版本 `2.6.7`，产物目录名包含版本号。
- 构建产物保留在 `build/android/` 与 `build/ios/`，验证证据保留在 `evidence/`。
- 版本完成标准区分源码测试、构建、签名、安装、启动和真机完整流程，不以其中一层替代另一层。
