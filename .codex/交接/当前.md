# 当前任务：深翡翠精品牌桌与牌局演出最终改造（2026-07-26）

## 2026-08-09 2.6.35 老手 ActionValue、离线进化链与 iOS 发布（当前）

- `bone_ash` 保持为公平老手基线，`hell` 只作为透视围剿挑战。碰、过、直杠、暗杠和补杠已改为合法动作时序：过牌保持等待摸牌，碰后弃牌，杠先处理抢杠/杠分/补张，再判断杠上花或弃牌。
- 弃牌、碰、杠和过牌使用同尺度 ActionValue 与确定性公共样本；手牌结构改为互斥动态规划分解，增加对子、刻子、复合搭和五对以上七对路线保护。新增独立离线反事实裁判、冻结基线配对擂台和受限影子日志，正式包继续禁止按少量输赢线上自动调权。
- 两组 500 状态配对擂台均通过晋级门槛。盲测弃牌后悔值 `0.199027 -> 0.170147`，碰/过/杠后悔值 `1.587463 -> 0.196338`，同状态一致率 `100%`。C# Release 0 警告/0 错误、19 组 PDF 黄金听口、131,841 牌形穷举均通过；Godot 真实 C# runtime 5 局全部自然结束、强制终止 0。
- 影子日志 schema 2 一局为 94 事件/268KB，隐藏状态非空事件 0；每会话最多 1600 事件、只保留最近 4 个会话。首次验证暴露的约 1.7GB 递归临时日志已删除。
- 版本统一为 `2.6.35`，Android `295`。iOS NativeAOT/Xcode 26.6 Release 构建成功；IPA 为 `build/ios/SichuanMahjong-2.6.35-development-20260809.ipa`，SHA-256 `abeb4f2e077b57498e6def3847c9b3a504aa42163cd384bb62acfc844d328a7c`。
- 已通过局域网覆盖安装到已配对的 iPhone 15，设备数据库读回 `2.6.35/2.6.35`；远程启动成功，延时进程检查确认应用仍在运行。原始设备 JSON 只留本机、不提交 GitHub。描述文件有效至 `2026-08-14 20:36:34 CST`，到期后需重新签名安装。
- 最强证据到算法验证、Godot 实际 runtime、多局压力、签名构建、真机安装/版本读回/启动/进程存活；长期真人平均净分与老手主观复核需继续用真实影子样本评估。完整报告：`evidence/2.6.35_ios_release_20260809/release_report.md`。

## 2026-08-08 2.6.33 短绒桌布、哑光余牌与自然摸牌（当前）

- 桌布 BaseColor 保持哈希 `8e7b868...`，Blender 生成双尺度 Normal/ORM：中尺度能量 `11.03%`、低频 `0.016%`、方向比 `1.213`、粗糙度中位数 `0.851`。三档真实 Metal 截图的色彩/亮度指标通过，人工复核无水渍、条带或闪烁斑。
- 中心 `CounterGlassLens` 保留几何但改用独立 `CenterMatteSmokedJadeCounter`：`#163B32`、metallic `0.01`、roughness `0.72`、不透明、无传输/发光；数字为 `#E8DFC8`、5px 深色描边。外层四向玻璃、9 对象、1044 三角形与环半径保持。
- 新摸牌在人类可弃牌阶段移到手牌最右侧，间隙为 `0.10 * scale`；弃出其他牌后原 tile id 以 `180ms` 归位，直接弃出时同一实体节点飞向牌河。蓝色菱形节点、网格、材质、导出样式和 `_process` 旋转均已移除。
- 版本已统一为 `2.6.33`，Android `293`。风险相关 Runner、C# Release（0 警告/0 错误）、Metal 指标和 `git diff --check` 通过。
- iOS 全新 NativeAOT/Xcode Release 构建、开发签名和 IPA 封装成功。IPA SHA-256 `8b97bdbc00ef37b4aea7f70232dc09b4da46529a0071d1f63a32e7eb1eecfe6b`，profile 到期 `2026-08-14 20:36:34 CST`。已安装到 paired iPhone 15，设备侧版本 `2.6.33/2.6.33`，启动成功且后续进程查询仍在运行。真机牌局画面和触控的最终肉眼验收仍需用户在手机上确认。

---

## 2026-08-08 2.6.32 主界面物理一体化改造

- 用户确认按推荐 A 路线实施。桌布既有颜色、BaseColor、Normal、ORM 和干净短绒生成逻辑保持不变；三张核心桌布贴图哈希与 2.6.31 一致。
- 光照已从重阴影改为移动端短软接触关系：环境光 `0.22`、主光 `0.79`/更靠近顶光、shadow opacity `0.64`、SSAO `0.84`；牌专用补光改为暖中性 `DED8CC / 1.30`。白牌与三家绿背统一为非发光 PBR，移除本家/对家局部 emission。
- Blender 牌桌删除完整中心矩形，替换为八段短转角压痕；外边界进一步变细、变浅、下沉；胶边收窄并提亮，胡桃木粗糙度提升。中心器保持 `9` 对象/`4` 材质/`1044` 三角面和既有方向/红区几何，只降低玻璃、透射和黑贴片反差。
- SeatHUD 收敛为统一墨玉外壳、默认单层青铜边和活动态 2px 古金边；移除五套外观、双重光晕和持续呼吸闪烁，180ms 入场完成后关闭逐帧处理。最新弃牌改为 `0.22` 宽低矮静态古铜折角，无实时投影和持续旋转。
- 版本统一为 `2.6.32`，Android `versionCode=292`；`VERSION`、`VERSION.md`、`README.md`、`CHANGELOG.md` 及三平台导出预设已由新增一致性 Runner 锁定。中心材质与桌体几何门禁也已强化，可区分旧版玻璃参数并锁定十二段凹槽、胶边内外尺寸。本轮相关 11 个 Godot Runner 全部退出码 `0`；C# Release `0 warning / 0 error`；`git diff --check` 通过。Blender 连续复建牌桌/中心器 SHA-256 分别为 `98d7428...` / `e4ef734...`。
- Apple M1 Pro / Metal 4.0 / Forward+ 已完成 1365x768、2048x1152、2556x1179 和密集弃牌截图人工检查；没有发现 HUD/牌墙/文字重叠，黑栅栏阴影、白牌过曝、完整中心矩形和悬浮金宝石均已消除。证据：`evidence/ui_physical_integration_20260808/verification_report.md`。
- 结论边界：2.6.32 尚未执行 iOS 导出、NativeAOT、签名、真机安装或启动，不能声称 iPhone 已更新。下一件事：若用户确认画面方向，重新打包并安装 2.6.32 到 iPhone，再以用户真机截图复核阴影和牌体曝光。

## 2026-08-08 2.6.30 iOS 全新打包与局域网安装（当前）

- 已从提交 `7b20f09f27f2fede875c33724a0942012a829b45` 全新执行 C# Release、Godot iOS/NativeAOT 导出、Xcode 26.6 Release 真机签名构建和 IPA 归档；固定发布清单 `27/27 PASS`，没有复用 2.6.29 App。
- 最终 App 为 `com.chendong.sichuanmahjong.iosdev`、arm64、`2.6.30/2.6.30`；深度 codesign、证书/描述文件/entitlements/目标 UDID、IPA ZIP、PCK 同源和包内桌布五项资产逐哈希检查全部通过。
- 已通过局域网覆盖安装到 `dona‘s iPhone`（iPhone 15）。设备端读回“四川麻将新版”`2.6.30/2.6.30`，新容器为 `/private/var/containers/Bundle/Application/EA658B39-0340-4960-BE33-BCC94C54D952/SichuanMahjongIOS.app`。
- 远程前台启动成功；启动约 3 分钟后进程仍在，PID `3480`。此前 2.6.29 的新描述文件信任阻塞已关闭。
- IPA：`build/ios/SichuanMahjong-2.6.30-development-20260808.ipa`，`96,600,726` bytes，SHA-256 `627bb28ad2f3e96124c3c20dd3582790eeae8a30216e779e6fa042b301c9c1b5`。描述文件到期时间仍为 `2026-08-14 20:36:34 CST`，到期后需重新签名安装。
- 最强证据到完整源码回归、签名 App/IPA、包内桌布同源、真机安装、设备版本读回、远程启动和约 3 分钟驻留；手机最终画面、真实触控、完整一局和长时性能仍需用户现场体验。完整报告：`evidence/2.6.30_ios_install_20260808/install_report.md`。

## 2026-08-07 2.6.30 启动图同质感桌布（当前）

- 已按用户确认的第三路线直接在当前分支升级到 2.6.30，没有创建新分支。桌布 BaseColor、Normal、ORM 和桌体 GLB 由 Blender 5.2 固定种子重建：自然暖绿底色、四组近等权多方向短绒法线、roughness `0.86–0.90`、metallic `0`，移除旧版偏青综合色和纵向织带。
- 限定灯光校准仅调整环境光到 `A9B79C / 0.16`、主光到 `FFF1E1 / 0.88`；方向、阴影、牌面专用补光、木框、皮革、凹槽、中心方向器、相机、布局、触控、规则、计分和 AI 保持不变。
- 最终源码提交为 `407f0e298589f656c37b1562c689465bdc7fb5f6`。源资产门禁全部通过，法线方向能量比 `1.2182`、BaseColor 明度变异系数 `0.00687`、roughness 中位 `0.8784`/跨度 `0.0314`、metallic 最大值 `0`；连续两次 Blender 完整重建的 BaseColor/Normal/ORM/GLB 哈希一致。
- Apple M1 Pro / Metal 4.0 / Forward+ 四档真实窗口成品均为 `RGB(53, 131, 64)`，接近启动图基准约 `RGB(54, 130, 64)`；R/G `0.4046`、B/G `0.4885`、明度 `0.4297`，1365×768、2048×1152、2400×1080、2556×1179 全部通过。三联对比：`evidence/ui_splash_matched_felt_20260807/splash_before_after_contact_sheet.png`。
- 回归：八项 Godot 专项合同 `8/8`、四档空间布局 `4/4`；C# Release `0 warning / 0 error`。布局与触控 Runner 在 PASS 后仍各有一条既有 `1 resources still in use at exit` 清理诊断，退出码均为 `0`，已在报告中明确记录。完整报告：`evidence/ui_splash_matched_felt_20260807/verification_report.md`。
- 证据边界：本轮没有构建 Android APK 或 iOS IPA，没有签名、安装或启动 2.6.30 手机包，不能声称手机端画面、真实触控、完整一局或长时性能已验证。此前安装在 iPhone 的仍是 2.6.29，且新开发描述文件信任项仍按上节处理。

## 2026-08-07 2.6.29 iOS 重新签名与局域网安装（当前）

- 原个人开发描述文件已于 `2026-08-06 20:25:19 CST` 到期。保持提交 `1bda1c48c6d617faaba764e8bc2c42b5a932be2f` 和版本 2.6.29 不变，已重新完成 C# Release、Godot iOS/NativeAOT 导出和 Xcode 26.6 Release 真机构建。
- 新描述文件 UUID `db1baa0e-3dbc-4c83-9049-a7c3d944a9ed`，Apple Development 签名，到期时间 `2026-08-14 20:36:34 CST`；证书指纹、Bundle ID、Team ID、entitlements 和 iPhone 15 UDID 均匹配，深度 codesign 通过。
- 已通过局域网覆盖安装到 `dona‘s iPhone`，设备端读回“四川麻将新版”`2.6.29/2.6.29`。新安装路径为 `/private/var/containers/Bundle/Application/C1D9748E-3D7A-4449-AE1B-5EF8A661EA8E/SichuanMahjongIOS.app`。
- 当前唯一未关闭项是手机端尚未显式信任新开发描述文件。三次远程启动均被 SpringBoard 以 `Security` 拒绝，没有应用进程；用户需在“设置 → 通用 → VPN 与设备管理”中信任 `donachen2027@outlook.com`，随后重试远程前台启动和延时 PID 检查。
- 新 IPA：`build/ios/SichuanMahjong-2.6.29-development-20260807.ipa`，`94,524,750` bytes，SHA-256 `06e0fa66a165532aa5907c1946cdf70848d6b5dda758667470a4ca9d822857f8`。报告：`evidence/2.6.29_ios_reinstall_20260807/reinstall_report.md`。

## 2026-08-04 2.6.29 Android Release APK（当前）

- 已从 GitHub 同源提交 `1bda1c48c6d617faaba764e8bc2c42b5a932be2f` 重新导出 Android Release，未改动 UI、规则、AI、相机或触控。成品：`build/android/SichuanMahjong-2.6.29-release.apk`，`151,460,614` bytes，SHA-256 `ef7aa9d35c96270adddd09008f6e87af7ebd0e7e117fd68830eac1cbb869013e`。
- 包名 `com.chendong.sichuanmahjong`，设备清单版本 `versionCode=289 / versionName=2.6.29`，minSdk/targetSdk `24/35`，仅 `arm64-v8a`；ZIP、zipalign、APK v2/v3 签名和包体大小门禁通过。
- 最终 APK 内中心导入场景与当前本地导入场景 SHA-256 同为 `4bea60b2b113f447a598df7e12eaefd62569cb435e075203b872dec566e284e4`。解压签名 APK 后直接加载包内资源，中心 `9` 对象/`1,044` 三角面/`4` 个分隔线对齐扇区、切口半径 `0.4600`，结算页单层外壳和内部无套框均通过：`PACKAGED_ANDROID_2_6_29_PASS CENTER + SETTLEMENT`。
- 当前 `adb devices -l` 为空，因此没有 Android 真机/模拟器安装、启动、触控或画面证据。完整报告：`evidence/2.6.29_android_release_20260804/release_report.md`。

## 2026-08-04 2.6.29 东西红区完整覆盖、结算页重设计与 iOS 发布（当前）

- 用户新截图确认东、西激活红色仍未填满其整个区域，同时要求结算页减少空白、增大文字、减少框线，并保持展示内容不变。
- 中心根因不是状态片尺寸，而是旧生成器把四区错误地按相同 `90°` 处理。真实石墨分隔线内端点为 `±0.38, ±0.20`，因此东/西跨度应为 `124.48292°`，南/北为 `55.51708°`。激活片现与分隔线共用角度参数；计数器切口仍为 `0.460`，大于 `0.455` 表圈半径。
- 中心 GLB 仍为 `1,044` 三角面、`9` 对象、`4` 材质；连续两次 Blender SHA-256 均为 `ab4bb4f0ec1f5f4d5544a486b36c7eea7267cc5841c8b8dd68152a6fb38e8e44`。四方向 Metal 成品红色均为精确 `#A13D2D`，计数器内部红色侵入和中心外缘铜黄像素均为 `0`。
- 结算页改为单层深翡翠主壳：仅一像素外轮廓，玩家列/详情/手牌/明细取消完整套框，焦点区仅保留左侧铜色强调；玩家四行和明细行按可用高度扩展，消除底部大空白；主标题、玩家、统计、焦点分数和表格文字整体放大；展示字段、麻将牌、账本权威来源、玩家切换触控和按钮全部保持。
- 四方向中心与四档结算均已用 Apple M1 Pro / Metal 4.0 / Forward+ 实拍并人工检查；四档逻辑视口为 1365×768、2048×1152、2400×1080、2556×1179，macOS 对两个奇数边长 drawable 分别取整为 1366×768、2556×1180。固定清单从头执行 `27/27 PASS`，结算 `3/3` 标准、`5/5` 权威账本通过，C# Release `0 warning / 0 error`。
- 完整报告与 8 张成品图：`evidence/ui_center_settlement_redesign_20260804/verification_report.md`。
- 版本已统一升级至 `2.6.29`。Godot iOS 导出、NativeAOT、Xcode Release、Apple Development 签名、深度 codesign、IPA ZIP、PCK 同源与包内中心/结算专项检查全部通过。IPA 为 `build/ios/SichuanMahjong-2.6.29-development.ipa`，`97,363,720` bytes，SHA-256 `4573f52ad379e70f5b3af644f9d58242de9d678a0da08b62cad36cede6c8586d`。
- iPhone 15 已覆盖安装，设备端读回应用“四川麻将新版”版本 `2.6.29/2.6.29`。自动启动被 iOS 以设备锁屏 `Locked` 明确拒绝，不是应用崩溃；解锁后需人工点击应用完成最终画面与真实触控验收。
- 个人开发描述文件到期时间为 `2026-08-06 20:25:19 CST`，到期后必须重新签名安装。完整发布报告：`evidence/2.6.29_ios_release_20260804/release_report.md`。

## 2026-08-04 2.6.28 东西方向红色侵入余牌圆圈修复与 iOS 发布（当前）

- 用户截图确认东、西激活红色状态片会透过透明镜片侵入余牌数字圆圈，而北、南不会。根因是旧网格的内缘距离不对称：东/西为 `0.20`，北/南为 `0.38`。
- 四个真实 3D 状态片已统一为半径 `0.460` 的分段圆弧切口，完整避让半径 `0.455` 的古铜计数环；没有把玻璃改成不透明，也没有改变规则、AI、相机、触控或牌桌布局。
- 中心 GLB 当前为 `1,044` 三角面、`9` 对象、`4` 材质；确定性 SHA-256 `5584990ba79587f7fef28ecd1c57ebefce433de47ae0ce221ded00e88b8fe717`。包内模型实测四状态片最小半径 `0.4600`。
- Apple M1 Pro / Metal 4.0 四方向像素门禁：余牌圆圈红色侵入像素均为 `0`、四边黄色像素均为 `0`、激活红色相对 `#A13D2D` 最大通道误差 `0`；固定清单 `27/27 PASS`。
- 版本已更新至 `2.6.28`。Godot iOS 导出、NativeAOT、Xcode Release、Apple Development 签名、codesign、IPA ZIP、PCK 同源与包内中心模型检查全部通过。IPA 为 `build/ios/SichuanMahjong-2.6.28-development.ipa`，SHA-256 `fbeff09087f96487a2d4245c1afc42a1b329d3376331ad40cb11a0bad1b6339b`。
- iPhone 15 已覆盖安装并从设备读回 `2.6.28/2.6.28`。两次远程启动因设备锁屏被 iOS 明确拒绝，等待解锁后重试前台启动及延时 PID 检查；这不是应用崩溃。
- 个人开发描述文件到期时间为 `2026-08-06 20:25:19 CST`，到期后需要重新签名并安装。完整报告：`evidence/2.6.28_ios_release_20260804/release_report.md`。

## 2026-08-04 2.6.27 中心区域四边黄线清理（当前）

- 用户以新截图指出中心四向器左、右、下边仍有黄色细线；根因定位为 Blender 资产中独立的 `CenterBronzeKeyline` 外圈网格，并非中央数字周围的 `CounterBronzeBezel` 古铜计数环。
- 已从生成源和 GLB 删除 `CenterBronzeKeyline` 及其专用 `CenterAgedBronze` 材质；中央古铜计数环、玻璃、石墨方向线、四方向状态片、尺寸和位置保持不变。2D 降级路径的 3px 铜黄色外边框也同步归零，避免降级时复发。
- 中心资产现为 `988` 三角面、`9` 对象、`4` 材质；SHA-256 `13992d52f5a0d346da5940d6c30426b7f5f2f78042359a47b6b3e452934640de`，连续两次 Blender 生成一致。`Sichuan3DTableStageRunner` 新增负向门禁，重新出现 `CenterBronzeKeyline` 即失败。
- Apple M1 Pro / Metal 4.0 / Forward+ 四方向 2048×1152 实拍确认四边无黄线；新像素门禁四座位外圈铜黄像素均为 `0`，修改前同一区域分别为 `218/366/466/366`。激活红色仍为精确 `RGB 161/61/45`，相对 `#A13D2D` 最大通道误差 0。
- 四方向中心近景：`evidence/ui_minimal_center_2_6_27/metal_clean_edges_four_seats_center_crop_20260804.png`；完整报告：`evidence/ui_minimal_center_2_6_27/clean_outer_edges_verification_report.md`。
- 最终固定清单从头执行 `27/27 PASS`，Blender/Python/Godot 导入、中心专项 Runner、边缘与颜色像素门禁、确定性哈希和 `git diff --check` 全部通过。截图退出阶段仍有既有 CanvasItem/RID/ObjectDB 清理警告，不影响退出码或产物。
- 结论边界：本次未重新构建 Android APK 或 iOS 包，也未在目标手机安装复核；旧 2.6.27 中间包不包含四边黄线清理。若要发布，必须从当前源码重新构建并验证包内 GLB 哈希。

## 2026-08-04 2.6.27 激活状态精确红色修复（颜色基线）

- 用户纠正了上一会话“`#A13D2D` 已锁定”的结论。重新拍摄真实 Metal 基线后，激活区实际像素为约 `RGB 196–204 / 105–126 / 88–110`，最大通道误差 64，确实偏粉、偏亮；旧回归只检查材质存在和无运行时覆盖，没有检查最终颜色。
- 根因是把目标 HEX 直接交给受桌面暖光、清漆和 Godot Filmic 显示变换影响的 PBR 状态片。四块状态片仍保留 Blender 生成的真实 3D 顶面网格，但状态材质改为 `KHR_materials_unlit`，并在生成源中保留语义目标 `ACTIVE_RED_DISPLAY_HEX = "A13D2D"`、使用 Filmic 校准作者色 `7F3226`；玻璃和中央计数环继续使用原 PBR 材质，颜色基线阶段仍在的旧铜外圈已由上方后续任务移除。2D 降级路径直接使用 `A13D2D`。
- 新增资产门禁：`Sichuan3DTableStageRunner` 检查四个状态片均为无光照、无发光、无清漆、导入色为校准值；`SichuanHudStateIntegrityRunner` 检查 2D 降级合同的 `active_color_hex=A13D2D`；新增 `tools/verify_center_active_color.py` 检查四座位 2048×1152 Metal 成品像素。
- 四座位 Metal 实拍全部通过：座位 `0/1/2/3` 对应西/北/东/南，所有避开文字的状态面采样点均为精确 `RGB 161/61/45`，相对目标 `161/61/45` 最大误差 0；四方向的主面、直边和两个切角均完整填满。联系图：`evidence/ui_minimal_center_2_6_27/metal_exact_red_four_seats_contact_sheet_20260804.png`。
- 最终零误差版本的固定清单从头执行 `27/27 PASS`，Python 编译、Blender 资产连续三次 SHA-256 `063bb7f241243971000ae3b879e71b8056c52c05a21c8eeb31386d212ae81785`、Godot 重新导入、聚焦 3D/HUD/商业成熟度/动效 Runner 和 `git diff --check` 全部通过。既有 Godot 退出阶段 RID/ObjectDB/resource 清理警告仍存在，不属于 Runner 失败。
- 结论边界：当前最强证据到 Apple M1 Pro / Metal 4.0 / Forward+ 的四方向真实渲染、最终像素量测和全量源码回归；本次颜色纠正后尚未重新构建 Android APK 或 iOS 包，也未在目标手机上安装，因此旧 2.6.27 中间包不能作为这一修复的交付物。
- 下一件事：若继续发布，必须从当前 SHA 对应源码重新生成 Android/iOS 2.6.27 产物，再检查包内 GLB 哈希、安装、启动和目标设备画面；不得复用本次修复前的中间包。

## 2026-08-03 2.6.25 中心模型、手牌分界与 Android 同机 A/B（当前）

- 用户指出 2.6.24 中心四向模型仍过于简陋，且本家手牌连成一片白色、没有逐牌边界。根因确认：`0.80` 中心距小于正常 `0.42 × 1.94 = 0.8148` 世界牌宽，相邻实体牌重叠约 `0.0148`。
- 2.6.25 将本家手牌中心距校准为 `0.83`，保留约 `0.0152` 的真实正间隙；正常 14 张投影宽度为 `82.22%–83.08%`，仍明显紧于 2.6.23 的约 `0.873` 稀疏步距，四档安全区和触控拾取通过。
- 中心盘升级为暗色底座、蓝钢外壳、金属倒角、香槟色内嵌边、四条分区接缝和三级中央表圈；当前方位继续用红色梯形与亮色方位字双重编码。全部中心网格关闭投影，使用静态无逐像素灯光材质、20 段圆环且无逐帧动画。
- 最终源码固定清单重新从头执行 `27/27 PASS`，C# Release `0 warning / 0 error`；Apple M1 Pro / Metal 4.0 与最终 Android APK 实拍均确认中心层级和逐牌缝线生效。
- 同一官方 Android 15 ARM64 AVD、同一正常牌局、同一 8 秒 SurfaceFlinger 图层帧计数 A/B：2.6.16 为 `4.50 / 4.25 / 4.25`（均值 `4.33`），2.6.24 为 `4.38 / 4.50 / 4.38`（均值 `4.42`），2.6.25 为 `4.25 / 4.50 / 4.38`（均值 `4.38`）。2.6.25 比 2.6.16 约高 `1.0%`，与 2.6.24 差约 `-0.9%`，均属单帧离散波动；直接版本 A/B 没有复现 2.6.16 之后明显变卡。该绝对值与旧报告采样口径不同，只用于本轮同机 A/B。
- Android Release：`build/android/SichuanMahjong-2.6.25-release.apk`，150174470 bytes，SHA-256 `cd81e31f9c7f16ff8f51b89f155353a905e5d1c31c7883b4bc08600de5721e54`；v2/v3、ZIP、zipalign、`285/2.6.25`、冻结 Mono 库与包内 `gl_compatibility` 通过。完成 2.6.16/2.6.24 A/B 后已恢复安装最终 2.6.25；AVD 冷启动、C# runtime ready、真实点击定缺进入牌局，稳定 PID `2841`，未见 FATAL/ANR/native signal/QueuePresentKHR。
- 完整证据：`evidence/ui_center_hand_seams_2_6_25/verification_report.md`。最强证据到官方 Android 模拟器的安装、启动、真实定缺、正常牌局实拍与严格同机版本 A/B；仍未证明 Android 真机长时性能、完整一局或 iOS 本轮构建/安装。

## 2026-08-03 2.6.24 恢复本家紧密牌距并保留 Android 性能修复（当前）

- 用户用原版牌桌截图纠正 2.6.23：本家手牌应紧密排齐、只留牌边细缝，不应使用明显正间隙。代码对比确认 2.6.23 将原始固定中心距 `0.80` 增大到约 `0.873`，是稀疏回归的直接原因。
- 2.6.24 已恢复本家固定 `0.80` 中心距；常规 14 张投影宽度回到 `80.28%`，原有 `74%–81%` 紧密构图门禁、左右/底部安全区和触控映射全部通过。本家牌体倍率 `1.94`、相机和已校准的本家副露间距未改。
- Android 卡顿修复保持不变：继续使用 Godot 4.6 移动默认阴影 `2048/质量 0` 和 `gl_compatibility`。同一官方 Android 15 ARM64 AVD / SwiftShader 正常牌局三轮 SurfaceFlinger 采样为 `6.17 / 6.06 / 6.20 FPS`，均值约 `6.14`；2.6.23 为约 `5.88`，退化的 2.6.22 为 `4.90`，本轮没有重新引入卡顿。
- 固定清单重新从头执行 `27/27 PASS`，C# Release `0 warning / 0 error`；真实 Apple M1 Pro / Metal 4.0 实拍和 Android 定缺/正常牌局实拍均已检查。
- Android Release：`build/android/SichuanMahjong-2.6.24-release.apk`，150174470 bytes，SHA-256 `cd44a08f2a00fec810d34eaf50d55fb0938a9dccd2ab0e8a1705fa1a94998771`；v2/v3、ZIP、zipalign、`284/2.6.24`、冻结 Mono 原生库与包内 `gl_compatibility` 通过。AVD 覆盖安装、冷启动、C# runtime ready、真实点击定缺进入牌局，稳定 PID `1932`，未见 FATAL/ANR/native signal/QueuePresentKHR。
- 证据与完整结论：`evidence/ui_hand_spacing_perf_2_6_24/verification_report.md`。最强证据到 Android 官方 ARM64 模拟器的安装、启动、真实定缺、正常牌局实拍和相对性能采样；仍未证明 Android 实体手机绝对 FPS、长时发热/功耗或完整一局，iOS 本轮未重新构建/安装。

## 2026-08-03 2.6.23 中心四向盘、手牌分界与 Android 性能回归（当前）

- 用户补充三项 P0 已合并完成：中心区按参考图改为蓝灰切角外壳、深色东/南/西/北四分区、中央圆形蓝色余牌数字；当前方位同时使用红色梯形和亮色方位字。组件为低矮不透明静态实体网格，只在快照的回合/余牌数变化时更新，无逐帧中心 UI。
- 本家手牌旧步距 `0.80` 小于 `0.42 × 1.94 = 0.8148` 的实体牌宽，根因是相邻象牙牌身实际轻微重叠。本轮改为 `0.42 + 0.030` 的每倍率物理步距；普通 14 张与 18 张极限组合行都保持正间隙，Android 定缺/牌局和 Metal 普通/最大副露实拍均可逐牌辨识。
- Android 性能退化已做同机 A/B：官方 Android 15 ARM64 AVD、`gl_compatibility`、正常牌局静态画面下，2.6.16 约 `5.82 FPS`，修复前 2.6.22 约 `4.90 FPS`。根因是 2.6.17 将 Godot 4.6 移动默认的方向阴影 `2048/质量 0` 覆盖为 `4096/质量 2`。恢复移动预算后，2.6.23 三轮为 `5.94 / 5.86 / 5.85 FPS`（均值约 `5.88`），回到并略高于 2.6.16 基线；该数值用于版本相对 A/B，不等同于实体手机绝对帧率。
- 版本已推进至 `2.6.23`，Android `versionCode=283`。固定清单重新从头执行 `27/27 PASS`；C# Release `0 warning / 0 error`；3D/HUD/相机/灯光/动效/规则/四档布局门禁全部通过。
- 中心方位映射已补齐双路径四座位矩阵：座位 `0/1/2/3` 分别激活 `西/北/东/南`；3D 主路径和 2D 降级路径均验证，无有效当前座位时四区保持中性，不再误亮“西”。
- Android Release：`build/android/SichuanMahjong-2.6.23-release.apk`，150174470 bytes，SHA-256 `661b0d6a29de80fb8eabce6ab112337ffd740086b194c30b102da81b07bf02f0`；APK v2/v3、ZIP、`283/2.6.23`、冻结 Mono 原生库 `9da745...64b6a` 与包内 `gl_compatibility` 通过。最终源码重建后再次在 AVD 覆盖安装、显式冷启动、C# runtime ready、真实定缺触控进入牌局、中心盘与手牌实拍，稳定 PID `2993`；无 FATAL/ANR/native signal/QueuePresentKHR。
- 证据目录：`evidence/ui_center_hand_perf_2_6_23/`。最强证据到 Android 官方 ARM64 模拟器的安装、启动、真实触控、视觉实拍与 SurfaceFlinger 版本 A/B；尚未证明 Android 实体手机的绝对 FPS、长时发热/掉帧、完整一局及所有连续触控路径。iOS 本轮未重新构建或安装。

## 2026-08-03 2.6.22 中心盘、胡牌/副露收尾与双端成品（当前）

- 在 2.6.21 的大小/厚度校准上完成中心区域收尾：旧凸起八边形中心盘隐藏，余牌计数改为与真实 `TableFelt` 顶面 `Y=0.155` 同平的透明玻璃菱形与窄玻璃边；Metal 实拍已确认轮廓和数字可见，没有重新引入凸起遮挡。
- 本家胡牌张、三家 AI 胡牌平扣、本家碰/杠仍只使用同一 `0.42×0.24×0.58` GLB 和三轴等比倍率；平扣/暗杠翡翠背层与运行时覆盖统一为 `0.414×0.574`，保留约 3mm 象牙唇边和真实厚度。
- 2.6.22 版本已写入 `project.godot`、`export_presets.cfg`、`VERSION`、`VERSION.md`、`README.md`、`CHANGELOG.md`，Android `versionCode=282`。
- 固定清单从头执行 `27/27 PASS`；聚焦 3D/HUD/动效/商业成熟度/牌体/结算 Runner 全部通过；C# Release `0 warning / 0 error`；导出脚本语法、`git diff --check`、敏感模式扫描通过。既有 Godot 退出阶段 RID/ObjectDB/resource leak 警告仍在，但不影响 Runner 退出码。
- Metal 证据与本轮报告：`evidence/ui_settlement_2_6_22/verification_report.md`；中心/胡牌/副露实拍来自 `evidence/ui_settlement_2_6_22/`。
- Android Release 已重建：`build/android/SichuanMahjong-2.6.22-release.apk`，150170374 bytes，SHA-256 `b79d519de953a6606d8bfc560cd350444eeffdeae790e9455eac79bc52566d0c`；v2/v3、ZIP、arm64-only 和 `gl_compatibility` 运行时覆盖通过。
- 官方 Android 15 ARM64 AVD `Codex_Sichuan_API35` 已覆盖安装、版本读回 `282/2.6.22`、显式启动、C# runtime ready 和真实输入推进；PID `1971` 短时稳定，无 FATAL/ANR/native signal/QueuePresentKHR。该证据不等于 Android 完整一局或长时体验。
- iOS Xcode/NativeAOT Release 已成功并完成 Apple Development 签名；App/IPA 版本 `2.6.22/2.6.22`，IPA SHA-256 `0ed6638acb4adee84e0bb74378480fe93ae1877712d75d2d10f94f0981671c8a`。当前 iPhone 15 CoreDevice 为 `unavailable`，本轮未取得 iOS 安装/启动/PID 证据。
- 下一件事：若设备重新变为 `available`，覆盖安装 2.6.22 IPA，记录版本读回、前台启动和延时 PID；设备未恢复前不要把 iOS 构建当真机交付。完成设备证据后再决定是否提交/推送本轮源码、报告和交接。

## 2026-08-02 2.6.21 三家胡牌与本家副露视觉大小二次校准（当前未提交）

- 用户最新复核：上家/下家胡牌平扣仍偏大、对家胡牌平扣偏小，并补充本家碰/杠牌偏大。本轮按真实透视投影面积拆开校准：上家/下家平扣倍率 `1.34`，对家平扣倍率 `1.56`，本家碰/杠倍率为本家手牌倍率 `0.84×`（正常本家 `1.6296`）。
- 三类倍率都继续作用于唯一 `mahjong_tile_body.glb` 的三轴，平扣牌距按倍率同比缩放，实体翻扣抬高按实际倍率同步；没有单轴拉伸、二维贴片、换模型或相机/布局改动。
- `Sichuan3DTableStageRunner` 已更新为验证座位专用胡牌倍率、世界空间牌体底边/厚度，以及本家副露缩小后的 18 张压力布局；Metal 实拍目录为 `evidence/ui_settlement_2_6_18/followup_20260802_v2/`。
- 版本推进到 `2.6.21`（Android `versionCode=281`）。`2.6.20` APK 只包含上一次修正，不代表本轮最新倍率；下一步重新跑固定清单并重建双端产物。

## 2026-08-02 2.6.20 三家胡牌牌体大小与实体落桌厚度校正（当前未提交）

- 用户复核指出上一版三家胡牌平扣牌列仍沿用了本家胡牌视觉倍率，且牌体底部被桌面遮住后只剩薄边。本轮已修正：三家 AI 胡牌保留原有座位倍率（上家/下家 `1.44`、对家 `1.28`），不再乘 `1.20`；同一 `0.42×0.24×0.58` GLB 保持三轴统一缩放。
- 翻扣抬高量改为 `0.24 × 当前实际等比倍率`，因此牌底在世界空间回到桌面接触高度，完整实体牌身/翡翠背层不再嵌进桌面。暗杠中间两张同理按 `MELD_SCALE` 或本家副露倍率抬高；没有新增平面贴片、单轴压缩或专用薄模型。
- 回归门禁已新增：逐个检查平扣/暗杠物理 GLB 的世界空间底边不低于桌面接触线，以及合并牌身+牌背的世界厚度达到 `0.24 × 实际倍率`；三家自摸胡、点炮胡和四座位暗杠矩阵继续覆盖。
- 版本已推进到 `2.6.20`（Android `versionCode=280`），旧 `2.6.19` 双端产物不代表本轮修改。下一步是完成聚焦/全量清单、Metal 三家实拍和双端重新构建/安装验证。

## 2026-08-01 用户截图复核后的牌体材质、平扣厚度补修（当前未提交）

- 用户进一步明确：不能只修对家；上家、对家、下家三家 AI 胡牌平扣，以及本家/三家暗杠中间背面都必须保持真实 3D 一致。
- 当前已修正：三家 AI 自摸胡、点炮胡统一使用同一 `mahjong_tile_body.glb`、实体尺寸 `0.42×0.24×0.58`、三轴倍率 `1.20`、同步牌距和 `0.24×1.20` 翻扣抬高；平扣/暗杠使用同一树脂 PBR 参数和深翡翠补偿源色 `FLAT_RESULT_JADE_BACK=0C5729`。禁止单轴压厚、二维贴片和仅对家专用分支。
- 暗杠回归已扩展为四座位矩阵：逐组检查外侧正面、中间两张真实物理翡翠背层、GLB、倒角、材质、等比缩放和离桌高度；三家自摸胡/点炮胡也在 Runner 中逐席验证。
- 当前验证：`Sichuan3DTableStageRunner` PASS；`SichuanTileVisualQualityRunner` PASS；1365×768、2048×1152、2400×1080、2556×1179 四档 `SichuanSpatialLayoutRunner` 全 PASS；固定清单 `27/27` PASS；真实 Apple M1 Pro / Metal 4.0 实拍目录为 `evidence/ui_settlement_2_6_18/followup_20260801/`，含三家自摸胡和三家点炮胡图。
- 结论边界：当前可证明的是源码、四座位暗杠矩阵、三家胡牌路径、空间合同、Metal 实拍和 2.6.19 双端构建/安装/启动均已覆盖；不同座位仍保留 `self=1.94 / side=1.44 / far=1.28` 的站立牌透视倍率，未改变相机、规则、AI 或触控。
- 交付复核已完成：Android `build/android/SichuanMahjong-2.6.19-release.apk`，SHA-256 `4f0bc42ded550922212ade58f63071770df6b52a763a93941fcfe7e202932b07`，`2.6.19/279`，官方 ARM64 模拟器安装/启动/稳定 PID `1979`；iOS `build/ios/SichuanMahjong-2.6.19-development.ipa`，SHA-256 `667cf150d3a53145f44d59cd872e4fed99ae6b40159a050639b2b6bcf420de19`，iPhone 15 安装/版本读回/启动/稳定 PID `95209`。
- 当前唯一未证实边界是用户设备上的完整一局、真实手指连续触控和长时性能/发热；本轮没有提交、推送或清理历史脏证据文件。

## 2026-08-01 2.6.18 定缺、实体牌、弃牌底层与结算页重排（最新）

- 针对用户最新四项反馈完成源码修复：定缺 `条/筒/万` 圆印文字按 CJK 字面重心向上补偿 7px；立牌与胡牌倒牌复用同一 `0.42×0.24×0.58` GLB 和统一运行时倍率（本家平扣倍率恢复为 `1.00`）；弃牌河面朝上的实体牌显式保留绿色翡翠底层；结算页按安全区可用高度重排四家列表、胡牌摘要、手牌和分数明细，比例列宽及裁剪保证文字、边框和下一局按钮不越界。
- 新增弃牌底层回归门禁；同步更新定缺光学居中门禁和本家平扣统一倍率门禁。固定清单重新从头执行 `27/27 PASS`，Metal 实拍在 `evidence/ui_settlement_2_6_18/`。
- 版本升级至 `2.6.18`；Android `versionCode=278`。正式 APK 为 `build/android/SichuanMahjong-2.6.18-release.apk`，SHA-256 `fe131a437fe04fbd6358f7692530d4e0c1bbbc2697b9d2fef37f4b43adf608f8`，v2/v3 签名通过。免费官方 ARM64 模拟器覆盖安装、版本读回、`gl_compatibility` 启动、C# runtime ready、PID `2155` 约 62 秒稳定且无崩溃/ANR。
- iOS `Release-iphoneos` Xcode/NativeAOT `BUILD SUCCEEDED`；签名 App 位于 `build/ios/DerivedData-2.6.18/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`，Development IPA 为 `build/ios/SichuanMahjong-2.6.18-development.ipa`，SHA-256 `c07b7690b0a25e5997d2585cb3f1a8a2d2f466ed0249fba00f6a6261a7c39e79`。iPhone 15 覆盖安装成功并读回 `2.6.18/2.6.18`；首次启动因锁屏被拒后，解锁重试成功，约 20 秒进程仍为 `SichuanMahjongIOS` PID `92768`。自动化证据已到构建、安装、版本读回、启动和短时存活；完整一局、真实手指触控和长时性能仍需用户人工验收。
- 详细证据：`evidence/ui_settlement_2_6_18/verification_report.md`。GitHub 推送待本轮提交完成；未触碰既有历史翻译和证据脏文件。

## 2026-08-01 2.6.17 五项牌桌/结算修复、双端发布与 iOS 真机安装（最新）

- 修复平扣麻将牌阴影锯齿：移动端定向阴影图提高到 4096，软阴影过滤提高到中等质量；保留牌桌紧凑灯光距离合同。Metal 4.0 实拍的平扣阴影边缘连续平滑。
- 定缺条/筒/万印章移除装饰点并按 CJK 实际字面光学下移居中；三枚印章仍只有当前选择项显示外圈高亮。
- 查叫结算不再把已胡玩家加入未听目标；规则回归新增“已胡排除”案例。结算玩家行支持 iOS `InputEventScreenTouch` 选择并展示该家的手牌/分数明细；移除“已胡查大叫收益”重复条目。结算外层改为 0.94×0.92 安全区、三列对称标题栏和响应式行高，四档屏幕尺寸均在根节点内。
- 固定清单从头执行 `27/27 PASS`，规则回归 `34/34 PASS`；完成 Metal 定缺、结算、自摸阴影实拍和 4 档响应式边界检查。证据在 `evidence/ui_settlement_shadow_fixes_20260801/`。
- Android `2.6.17/277` 正式 APK：`build/android/SichuanMahjong-2.6.17-release.apk`，SHA-256 `ec0ed19119a3f7685588e27945db6b1476734a0e64fb3a25cd1122c6549bd8a7`；免费官方 Android 15 ARM64 模拟器覆盖安装、版本读回、显式启动、C# runtime ready、定缺画面和稳定 PID `2012`（约 20 秒）通过，无 FATAL/ANR/native signal/QueuePresentKHR。
- iOS Godot/NativeAOT 与 Xcode `Release-iphoneos` 构建成功；个人 Apple Development 签名 App 位于 `build/ios/DerivedData-2.6.17/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`，Development IPA 位于 `build/ios/SichuanMahjong-2.6.17-development.ipa`，SHA-256 `695b720645923e319d90bd572c05adf6d930bb224e6bca2ca9aaf8984168914c`。App 版本/构建 `2.6.17/2.6.17`、Bundle ID `com.chendong.sichuanmahjong.iosdev`、arm64 与 `codesign --deep --strict` 通过；App Store Distribution IPA 因账号没有 Distribution 证书未生成。
- iPhone 15 CoreDevice `516E99D2-18B6-5DD8-94E1-6993510A036D` 局域网隧道覆盖安装成功，设备数据库读回 `2.6.17/2.6.17`，前台启动命令成功并在后续复启命令成功。自动化仍不能替代用户真机完整一局、真实手指结算关闭→下一局和长时帧率/发热验收。

## 2026-08-01 2.6.16 六项牌桌修复、双端发布与 iOS 局域网安装（最新）

- 修复投骰/定缺交接：结束条件从依赖 69 次 Timer 回调改为 3.05 秒单调时钟截止，低帧率只影响中间换点、不再拖延骰子消失；进入定缺后骰子和中央计数隐藏。条/筒/万印章去除烘焙装饰点、统一文字上下边距，初始释放全部焦点，不再只亮“条”。
- 本家和牌倒牌继续复用唯一 `0.42×0.24×0.58` 实体，并使用三轴一致的视觉补偿。碰、明杠、暗杠、加杠与剩余手牌改为最多 18 张的共享底部轨道；本家副露和手牌使用相同模型、相同等比缩放，按总张数自动收紧。四组副露的极小投影相交未通过放宽测试处理，而是增加真实组间距后四档空间门禁通过。
- iOS 结算层增加 `InputEventScreenTouch` 显式路由；关闭积分后自动展开工具栏并露出“下一局”。回归测试实际推进 `round_index` 并进入下一局 TABLE_SETUP，不以桌面鼠标路径代替 iOS 触控路径。
- 固定清单最终从头执行 `27/27 PASS`。真实 Apple M1 Pro / Metal 4.0 / Forward+ 的定缺、自摸和 4 杠+2 手牌 18 张极限布局已逐图复核，证据与完整报告在 `evidence/ui_six_fixes_20260801/`。
- Android 版本 `2.6.16/276`；正式 APK `build/android/SichuanMahjong-2.6.16-release.apk`，大小 `150166278` bytes，SHA-256 `8749f6e913a6988fcb4ac44248a2942b0d025650708226da1d944f5ac89f715c`，zipalign、v2/v3 签名通过。免费官方 Android 15 ARM64 模拟器覆盖安装、版本读回、gl_compatibility/C# runtime、牌桌、手牌触摸与稳定 PID 通过，无应用崩溃信号。
- iOS Xcode/NativeAOT Release `BUILD SUCCEEDED`；签名 App/IPA 分别位于 `build/ios/DerivedData-2.6.16/Build/Products/Release-iphoneos/SichuanMahjongIOS.app` 和 `build/ios/SichuanMahjong-2.6.16-development.ipa`。iPhone 15 通过局域网覆盖安装成功，设备数据库读回 `2.6.16/2.6.16`，前台启动后延时复核仍为 PID `90894`。
- 自动化尚不能替代用户真机完整一局、真实手指执行结算关闭→下一局、以及长时帧率/发热体验；这三项仍作为明确人工验收边界。

## 2026-08-01 2.6.15 Android 黑屏/闪退根因修复与官方模拟器发布验证（最新）

- 用户指出 `2.6.14` Android 包仍闪退，并要求本机模拟器真实跑通后再发布。MuMu 需要付费，因此未使用、未修改其数据；已通过现有 Android SDK 安装免费的官方 Emulator `37.1.11` 和 Android 15 `arm64-v8a` 系统镜像，新建隔离 AVD `Codex_Sichuan_API35`。
- 根因 A/B：`2.6.14` 正式包以 `forward_plus` / Vulkan 启动，在模拟器持续报 `QueuePresentKHR failed with error: 5` 并长时间黑屏；未替换 Mono 库的基础包虽约 2 秒进主循环，却无法加载 `SichuanCSharpRuntime.cs`。因此保留已验证的 Godot .NET/Mono 原生库和 C# runtime，同时让 Android APK 通过 `assets/_cl_` 固定携带 `--rendering-method gl_compatibility`。发布脚本增加缺少该运行时覆盖即失败的防回归断言；共享移动设置仍保留 Forward+，不改变现有 iOS 渲染管线。
- 版本已升级为 `2.6.15`，Android `versionCode=275`。最终 APK：`build/android/SichuanMahjong-2.6.15-release.apk`；大小 `150166278` bytes；SHA-256 `7c0c84ed1bb504a1eeb869967f5316689ffaf21791946bfaad153a8c0aad77a5`；arm64-only、ZIP、zipalign、v2/v3 签名和证书读回通过。
- 最终 APK 在官方 Android 15 ARM64 模拟器中覆盖安装成功，设备读回 `2.6.15/275`。日志明确为 `usesVulkan(): false`、`renderer: gl_compatibility (CommandLine)`，C# runtime 约 2 秒 ready。完成定缺 touchscreen 输入并实际打出一张缺门手牌；同一 PID `2001` 存活 `01:56`，无 FATAL、native signal、ANR、进程死亡、`QueuePresentKHR` 或 Godot force quit。
- 模拟器刚启动、资源 overlay 尚未稳定时的首次立即启动曾触发 Android Activity 资源配置重建并 force quit；等待系统启动收尾后，同一已安装 APK 正常冷启动并通过上述验证。自动化以后不能只等待 `sys.boot_completed=1`，还要等待设备配置稳定。
- 固定清单 `docs/ui_rework/emerald_final_runner_manifest_V1.txt` 本轮从头执行为 `27/27 PASS`，覆盖触控、规则、计分、C#、3D 牌桌、牌体、碰/明杠/暗杠/加杠模型一致性、四档空间布局、相机与灯光。完整报告：`evidence/2.6.15_android_emulator_fix_20260801/verification_report.md`。
- 本版本只重新发布 Android；iOS 未重新构建、签名或安装。

## 2026-07-31 2.6.14 碰杠模型一致性、双端发布、iOS 局域网安装与 GitHub 收尾（上一版）

- 用户要求在统一加厚牌体后继续检查碰、明杠、暗杠、加杠的大小与厚度。源码审计确认四种副露都由同一 `_append_meld_entries` 入口创建，并复用唯一的 `mahjong_tile_body.glb`；落定缩放统一为 `Vector3.ONE × 1.12`。现新增逐类型/逐座位硬门禁：根节点三轴必须一致，牌身与绿背 Mesh 资源必须相同，实体子节点必须保持 `Vector3.ONE`，禁止类型专属薄模型和子节点压缩。
- 唯一基础牌体仍为 `0.42×0.24×0.58`；副露落定世界尺寸为 `0.4704×0.2688×0.6496`。碰、明杠、加杠、暗杠动画起始与落定均三轴同步；暗杠中间两张只翻转同一实体。手牌、弃牌和副露保留布局需要的不同整体倍率，但都等比例作用于宽、厚、长，不是不同模型。
- 真实 Apple M1 Pro / Metal 4.0 / Forward+、`2556×1179` 已复核四座位碰杠矩阵、最大四组副露、右家混合碰杠，以及碰/明杠/加杠/暗杠四种动作落定帧；未发现压薄、牌组穿插或区域重叠。证据在 `evidence/ui_meld_model_consistency_20260731/metal/`；固定清单从头执行为 `27/27 PASS`。
- 版本已升级为 `2.6.14`，Android `versionCode=274`。正式 APK 为 `build/android/SichuanMahjong-2.6.14-release.apk`，大小 `150,166,278` bytes，SHA-256 `0009a9caef16b20c40e06066c6378c051e035bd72dc577e5819cfcca27e5cc0f`；zipalign、v2/v3 签名、ZIP 完整性、arm64-only 与包内牌体一致性通过。当前没有 Android 真机。
- iOS Xcode/NativeAOT `Release-iphoneos` 为 `BUILD SUCCEEDED`；签名 App 位于 `build/ios/DerivedData-2.6.14/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`，IPA 为 `build/ios/SichuanMahjong-2.6.14-development.ipa`，SHA-256 `47ee658e6a5ee8fcb5a98eb9e4030d487b4a917105aea68db496dc44d84c9a25`。签名、arm64、版本 `2.6.14/2.6.14`、PCK 一致性和 IPA ZIP 完整性通过；挂载成品 PCK 实测牌体仍为 `(0.42,0.24,0.58)`。
- iPhone 15 通过局域网 CoreDevice 隧道覆盖安装成功；设备数据库读回 `2.6.14/2.6.14`，前台启动后的 5/50/95 秒均为同一 PID `88361`。完整一局、真实触控、手机最终主观大小/厚度观感与长时性能仍由用户人工验收。完整报告为 `evidence/ui_meld_model_consistency_20260731/verification_report.md`、`evidence/2.6.14_android_release_20260731/release_report.md`、`evidence/2.6.14_ios_release_20260731/release_report.md`。

## 2026-07-31 2.6.13 实体牌统一加厚、双端发布与 iOS 真机安装（最新）

- 用户真机截图指出胡牌后倒下的暗手明显比立牌薄、像纸片。复查确认所有姿态原本已复用同一个 GLB 且没有姿态专属非等比缩放；根因是唯一实体牌只有 `0.42×0.18×0.58`，远端平扣后的 `0.18` 厚度在手机透视里只剩几像素。
- 唯一牌模型已统一改为 `0.42×0.24×0.58`（厚度 `+33.3%`），翡翠背层从 `0.055` 同比例改为 `0.074`。立牌、平扣牌、弃牌、副露和暗杠仍只旋转同一模型；翻扣抬高量、牌面和标记高度已同步，未改相机、牌距、规则、计分、AI、布局或触控。
- 真实 Apple M1 Pro / Metal 4.0 / Forward+、`2556×1179` 已复核上家/对家/下家三种平扣、三家普通立牌与暗杠扣牌。证据在 `evidence/ui_tile_physical_thickness_20260731/after/`。实体厚度、姿态统一缩放和离桌高度回归已补齐；最终固定清单从头执行为 `27/27 PASS`。
- Android 正式包：`build/android/SichuanMahjong-2.6.13-release.apk`，`versionCode=273`、`versionName=2.6.13`，大小 `150,166,278` bytes，SHA-256 `ed5241658bda800dad5c8b490ebd06d0384ffdb63c8b8ac640f697cd941f153f`；zipalign、v2/v3 签名、ZIP 完整性通过。APK 内牌体导入场景与本轮本地导入场景逐字节一致。当前无 Android 真机，证据止于可安装签名 APK。
- iOS Xcode/NativeAOT `Release-iphoneos` 为 `BUILD SUCCEEDED`；签名 App 位于 `build/ios/DerivedData-2.6.13/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`，Development IPA 为 `build/ios/SichuanMahjong-2.6.13-development.ipa`，SHA-256 `037e3cb1935aad8489bd165415ef905a80eee5b9ccfbd3d769dd459819de59c6`。签名、arm64、版本 `2.6.13/2.6.13`、PCK 一致性和 IPA ZIP 完整性通过；挂载 App 内 PCK 实测牌体 AABB 为 `(0.42,0.24,0.58)`。
- iPhone 15 已覆盖安装；设备数据库读回 `2.6.13/2.6.13`。第一次启动在 5/20/60 秒均为 PID `88064`，较晚复核时进程已不在列表中且原因无法从进程表判断；因此重新前台启动，新的 5/50/95 秒检查点均为同一 PID `88110`。自动化最强证据到签名构建、包内新模型、安装、版本读回、启动和独立复启后约 95 秒稳定 PID；完整一局、真实触控、手机最终主观观感与长时性能仍由用户人工验收。完整报告分别为 `evidence/ui_tile_physical_thickness_20260731/verification_report.md`、`evidence/2.6.13_android_release_20260731/release_report.md`、`evidence/2.6.13_ios_release_20260731/release_report.md`。

## 2026-07-31 2.6.12 Android 最新源码 Release（最新）

- 用户确认继续 Android 发布后，先核对发现原 `build/android/SichuanMahjong-2.6.12-release.apk` 构建于 2026-07-30 21:51，早于 2026-07-31 19:37 的最新牌背源码，不能作为当前成品。旧签名包已备份为 `build/android/archive/SichuanMahjong-2.6.12-release-pre-tile-back-20260730.apk`，SHA-256 `071047762b5d684f2ac27fab657b2b248ff4cbe670fb400c956dff90f16e909c`。
- 使用当前源码重新执行 `tools/export_android_release.sh`，Godot/.NET 导出返回 `export_result=0`、`message_count=0`，随后完成瘦身、Mono Android 原生库冻结校验、zipalign 和正式签名。最终包：`build/android/SichuanMahjong-2.6.12-release.apk`，修改时间 2026-07-31 21:13:19，大小 `150,166,278` bytes，SHA-256 `d451d0c12cd01199686083a7830d1eae5e1f0d8b73ccd037d65655e20fc65e59`。
- 最终 APK 元数据：包名 `com.chendong.sichuanmahjong`、`versionCode=272`、`versionName=2.6.12`、minSdk 24、targetSdk 35、仅 `arm64-v8a`。包内含 `SichuanMahjong.AI.Core.dll`、`SichuanMahjong.Godot.dll` 及最新两个 3D GDScript 编译文件。
- 入包证明：`SichuanTile3D.gdc` 新/旧哈希不同，新包哈希 `7a9da5b354166346f1bfc02ee5c4322d1407d29bc5fb846880e80b59f053bf36`；`SichuanTableStage3D.gdc` 新包哈希 `b8ac59f351425d4556d36cb9b0e5a7f218615dc814fd674776e4bb143aee260a`。二者都与本轮 fresh base APK 完全一致，证明三家普通牌背同色修正已进入最终签名包。
- 成品验证：zipalign PASS；APK v2/v3 签名 PASS；证书 SHA-256 `9ca9ba45de9a950e19f140e2d706d0fe187e90070601d162becf8848b6a75600`；ZIP 完整性 PASS；`libgodot_android.so` 冻结 SHA-256 `9da745177c364666278250a8912d43869166998a277ab8eebfb9f7322a064b6a`；禁止目录和 `.pdb` 条目为 0；没有声明额外 Android 应用权限。
- 源码发布门禁沿用本轮当前源码刚完成的固定清单 `27/27 PASS`，包含 C# Release 编译；Android 导出脚本语法、`git diff --check` 和敏感信息模式扫描均通过。完整报告：`evidence/2.6.12_android_release_20260731/release_report.md`。
- `adb devices -l` 当前为空，因此 Android 最强证据到可安装正式签名 APK；尚未验证安卓真机安装、设备版本读回、启动、稳定 PID、真实触控和完整牌局。

## 2026-07-31 2.6.12 iOS Release 与真机安装（最新）

- 工作区已有但未交付的 `2.6.12` 版本元数据被沿用，没有跳号到 `2.6.13`；本次将 Android 2.6.11 启动修复、平扣姿态牌背和最新三家普通暗手牌背同色修正一起纳入 2.6.12。设备安装前版本为 `2.6.11`。
- `tools/export_ios_xcode_project.sh` 的清理边界从整个 `build/ios` 收窄为仅当前 `EXPORT_DIR`，因此旧 `2.6.11` Xcode 工程和签名 App 均保留，可作为回退边界。
- 发布前固定清单重新执行为 `27/27 PASS`，并包含 C# Release 编译；`zsh -n tools/export_ios_xcode_project.sh` 与 `git diff --check` 通过。日志在 `evidence/2.6.12_ios_release_20260731/preflight_logs/`。
- Godot 4.6.2 .NET iOS Xcode/NativeAOT 导出成功；Xcode `Release-iphoneos` 为 `BUILD SUCCEEDED`。签名 App 位于 `build/ios/DerivedData-2.6.12/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`，Bundle ID `com.chendong.sichuanmahjong.iosdev`，版本/构建 `2.6.12/2.6.12`，主程序与 C# AOT framework 均为 arm64，`codesign --deep --strict` 通过。PCK SHA-256 为 `a2e64537f3c6702e3d9a99980f1e7feec8694fb0e357ee6803ebd8d4750cb34b`，工程 PCK 与 App 内 PCK 完全一致。
- Development IPA 为 `build/ios/SichuanMahjong-2.6.12-development.ipa`，大小 `96,067,167` bytes，SHA-256 `7532b663164e85378fb03ba17f889643f788b95b020a15dd16d606e0641d32d7`，`unzip -t` 通过。第一次直接归档被安全策略阻止，第二次生成的 POSIX tar 被完整性检查拦截并移入废纸篓，未用于安装；最终 IPA 使用 Apple `ditto` 标准 Payload 结构生成。
- iPhone 15（CoreDevice `516E99D2-18B6-5DD8-94E1-6993510A036D`）覆盖安装成功；设备安装数据库确认 `2.6.12/2.6.12`。锁屏阶段的四次自动启动曾被 iOS 明确以 `Locked` 拒绝；用户输入密码解锁后，`devicectl device process launch --terminate-existing --activate` 前台启动成功。
- 启动后 5 秒、20 秒和约 95 秒三个设备进程检查点均发现 `SichuanMahjongIOS`，PID 持续为 `87517`，自动化证据现已闭环到签名构建、覆盖安装、设备版本读回、前台启动和约 95 秒 PID 存活。完整一局、真实触控、手机屏幕上的三家牌背最终观感、发热和持续帧率仍由用户真机人工验收。完整报告：`evidence/2.6.12_ios_release_20260731/release_report.md`。

## 2026-07-31 三家普通暗手牌背同色修正（最新，覆盖上一条对家专用亮绿结论）

- 用户用定缺阶段同屏截图指出：对家整排牌背为亮绿，而上、下家大面为深翡翠，明显不是同一副牌。复查确认不是单纯灯光误差：上一轮给对家大面绑定了独立 `UNSHADED #29822F`，实体绿层还绑定了另一套 `#3DC94A` PBR；上、下家则使用共享 `#178B32` PBR。
- `scripts/ui/3d/SichuanTile3D.gd` 已删除对家专用绿色大面和专用绿色实体层。现在左家、对家、右家的普通站立暗手大面及真实 `MahjongTileBack` 全部引用同一个缓存翡翠 PBR；对家只保留白色牌身的局部朝向亮度补偿。相机、灯光、布局和牌局规则均未改变。
- 合同已改为 `all_three_shared_pbr_emerald_back`；`Sichuan3DTableStageRunner` 逐座位验证三家引用完全相同的缓存材质，且各自大面与实体绿层也是同一实例。`SichuanTileVisualQualityRunner` 明确禁止对家退回专用 unshaded 亮绿。
- Apple M1 Pro / Metal 4.0 / Forward+、`2048×1152` 定缺同屏实拍：修正前左/对/右的无高光大面采样为 `RGB(2,43,10)` / `RGB(43,144,51)` / `RGB(7,48,15)`；修正后为 `RGB(2,43,10)` / `RGB(2,43,10)` / `RGB(4,45,12)`，三家最大通道差仅 `2/255`。证据及坐标见 `evidence/ui_opponent_back_consistency_20260731/measurement.md`。
- 两项聚焦 Runner 通过；固定清单重新完整执行并取得 `27/27 PASS`，汇总为 `evidence/ui_opponent_back_consistency_20260731/full_manifest_logs_final/summary.txt`；`git diff --check` 通过。
- 本轮没有提高版本、打 Android/iOS 包或安装手机。最强证据到源码合同、固定回归与本机真实 Metal 渲染；手机现有 `2.6.11` 不包含本轮修正。

## 2026-07-31 图 1 / 图 2 牌背分姿态恢复（对家专用亮绿结论已被上条覆盖）

- 当时曾把目标拆成两类：自摸、点炮胡后的保留手牌、暗杠中间两张等“平扣背面”按图 1 的明亮正绿色；对家行牌中的立牌背面按图 2 单独校准。用户随后用三家同屏截图确认“对家专用亮绿”不成立，因此该结论已被上条覆盖。
- 当时实现过两套显示合同：平扣显示面走稳定明亮正绿；对家立牌使用独立稳定绿色大面、专用 PBR 亮绿 `MahjongTileBack` 窄层和白色牌身。当前仅保留平扣姿态合同与对家白色牌身补偿；对家两套专用绿色材质均已删除，普通站立暗手三家共享 PBR。
- 当时的 Display P3 目标和旧 Metal 实测仍保存在 `evidence/ui_tile_back_target_restore_20260731/`，只作为历史过程证据，不能再作为当前对家行牌验收值；当前验收值以 `evidence/ui_opponent_back_consistency_20260731/measurement.md` 为准。
- 最终复核发现旧合同仍要求 AI 点炮胡后保留站立牌，虽然首轮固定清单通过但与用户最新要求冲突。现已改为三家 AI 点炮胡后整手物理翻扣、使用图 1 亮绿背面；外来胡牌仍独立明牌并保留来源箭头。下家胡牌附加牌移到其牌列远离本家的安全端，避免被安全线压回并与平扣手牌重叠。
- 真实 Apple M1 Pro / Metal Forward+ 证据：对家行牌 `ding-que_final_v3_2048x1152.png`；三家自摸 `ai-self-draw-{1,2,3}_final_2048x1152.png`；上/下家点炮胡 `ai-discard-win-{1,3}_final_2048x1152.png`；暗杠中间两张 `motion-an-gang/frame_041.png`。
- `SichuanTileVisualQualityRunner` 与 `Sichuan3DTableStageRunner` 聚焦通过；修正点炮胡旧合同后，固定清单重新从头执行并取得 `27/27 PASS`，日志在 `evidence/ui_tile_back_target_restore_20260731/full_manifest_logs_final/summary.txt`；`git diff --check` 通过。
- 本轮没有修改摄像机、常规牌桌布局、规则、计分、AI、触控或牌面；没有提高版本号、打 Android/iOS 包或安装手机。最强证据到源码合同、固定全量回归和本机真实 Metal 视觉闭环；手机现有 `2.6.11` 不会自动包含本轮效果。

## 2026-07-31 参考图牌背样式恢复

- 用户复核后指出两个剩余实体方向错误：对家站立牌真正的 `MahjongTileBack` 翡翠层位于桌外侧；对家暗杠中间两张只覆盖了深绿顶面，真实背层仍朝下并露出白色牌身。
- 根因不是颜色参数，而是只交换 `face_mesh/concealed_cap_mesh` 材质、没有翻转 GLB 实体。现已把对家立牌倾角符号改为与上/下家一致的 `+90°`，三家站立暗手统一为“本地 -Y 实体背层朝桌心、+Y 象牙面朝牌主”，不再使用 `concealed_surface_flip` 伪交换。
- 暗杠中间两张和 AI 自摸暗手改为整块牌绕本地 X 轴物理翻面，并按牌体高度 `0.18` 同步抬升，避免翻面后沉入桌布。深绿覆盖顶面继续使用朝向补偿材质；真正有厚度的 `MahjongTileBack` 永远复用与上/下家完全相同的原版 `#178B32` PBR，因此绿色实体层、象牙牌身和端面均可见。
- `Sichuan3DTableStageRunner` 已增加世界空间门禁：三家真实翡翠层必须处于牌身的桌心侧且共享同一个缓存 PBR；暗杠/AI 自摸真实翡翠层必须朝上、材质不得为白色，翻面后根节点高度不得低于 `0.26`。真实 Metal 证据在 `evidence/ui_tile_back_orientation_fix_20260731/`：`far-hand-closeup.png`、`far-concealed-kong-final-closeup.png`、`far-hand-and-self-draw-final-closeup.png`。
- 实体翻转使对家透视高度比例短暂降到 `0.3786`、低于既有 `0.3800` 门禁；未降低阈值，而是把 `FAR_HAND_SCALE` 从 `1.27` 微调为 `1.28`，最终比例 `0.3815`。固定清单第 1–8、单独第 9、单独第 10、第 11–27 均重新通过，合计 `27/27 PASS`；`git diff --check` 通过。
- 用户进一步明确图 2 的目标不是单一深绿色，而是原版牌背的光感、树脂质感与分层：深绿主体、方向性亮边、独立绿色背层、象牙牌身、灰色端面和接触阴影；普通暗手、暗杠、自摸/胡牌倒牌都必须遵守该视觉语言。
- `scripts/ui/3d/SichuanTile3D.gd` 已恢复原版普通牌背 PBR：翡翠基色 `#178B32`、roughness `0.27`、metallic `0.02`、clearcoat `0.34`、clearcoat roughness `0.20`，不再使用 unshaded 平涂。隐藏牌背增加 `0.012` 深的真实下沉倒角以承接方向光；亮牌正面继续使用原平整网格，牌面符号和白面未改变。
- 平扣牌正对顶灯，使用 `#0A3B13` 朝向补偿色、specular `0.18`、clearcoat `0.12/0.38` 并关闭环境泛白，但仍保留方向灯、PBR 粗糙度、清漆和实体倒角。结果不再是荧光亮绿或灰绿平面，而是深绿树脂中心、窄亮边、象牙牌身和实体阴影。
- 普通三家暗手、暗杠中间两张、AI 三家自摸倒牌与点炮胡牌保留暗手均已统一；`SichuanTileVisualQualityRunner` 和 `Sichuan3DTableStageRunner` 同步门禁 PBR 参数与 `>=0.01` 的牌背实体倒角。
- 真实 Apple M1 Pro / Metal Forward+ 证据：`evidence/ui_tile_back_layered_restore_20260731/ding-que_final_2048x1152.png`、`ai-self-draw-1_beveled-flat-pbr-v5_2048x1152.png`、`motion-an-gang/frame_041.png`。固定清单按 1–9、10–27 两段复跑，合计 `27/27 PASS`；牌体质量和 3D Stage 聚焦 Runner 单独复跑通过；`git diff --check` 通过。退出阶段仍有既有 CanvasItem/RID/ObjectDB 清理警告，不属于 Runner 失败。
- 本轮没有提高版本、打包或安装手机；最强证据到源码、全量自动回归和本机真实 Metal 视觉闭环。若要发到手机，下一步是版本升级、重新打 Android/iOS 包并按安装/启动/真机牌局分层验证。

## 2026-07-30 2.6.11 五项牌桌修正与 Android 瘦身发布

- 用户提出的五项问题均已实现并由独立裁判判定 `RELEASE JUDGE PASS`：碰/杠来源箭头统一为小号、扁平、无渐变的天蓝色方向箭头；胡牌来源箭头同步缩小简化并去除“某家点炮”文字；上/下家胡牌陈列增加本家手牌安全边界；胡牌与暗杠牌背统一为正常墨绿色 `#073B32`；Android Release 包执行符号剥离和非运行时目录裁剪。
- 版本已升级为 `2.6.11`，Android `versionCode=271`。固定清单 `27/27 PASS`；五个聚焦 Runner 全部通过，其中 `SichuanUiSixIssuesRunner` 为 `7/7`；Godot/C# Release 构建 `0 warning / 0 error`，脚本语法、Python 编译和 `git diff --check` 均通过。
- 最终 Android 包：`build/android/SichuanMahjong-2.6.11-release.apk`，大小 `150,149,894` bytes（约 143.2 MiB），SHA-256 `ed8daacf847e57ab7d3f56767bd44087a546639b5776d49e93c8487693deae3f`；包名 `com.chendong.sichuanmahjong`；zipalign、ZIP 完整性以及 APK v2/v3 签名均通过，签名证书 SHA-256 `9ca9ba45de9a950e19f140e2d706d0fe187e90070601d162becf8848b6a75600`。
- 相比上一正式包 `2.6.9` 的 `766,713,674` bytes，本包缩小约 `80.4%`；对比旧 base 包 `858,787,072` bytes，缩小约 `82.5%`。包内没有证据目录、测试目录、文档目录或 `.pdb`。
- 手机安装当前唯一阻塞：`adb devices -l` 未发现安卓设备，因此尚未取得“安装成功、版本查询、启动成功、稳定 PID”真机证据。连接手机、开启 USB 调试并确认授权后，下一步直接执行覆盖安装与启动验证。GitHub `main` 已同步 2.6.11 发布提交 `f028869`。

- 2026-07-29 续作（用户牌局反馈）：新摸牌固定为与“最新弃牌”同一实体菱锥语言、但本地宽度 `0.16` 的小号亮蓝 `#42A5FF` 3D 菱形；本家手牌的 `48°` 站立牌倾角已反向抵消，因此其菱面保持水平可辨。选中牌完全去除勾号、光环及贴片，仅以 `+0.11` 实体抬升表达。暗杠明确固定为第 1/4 张明牌、第 2/3 张翡翠背面；碰/杠来源箭头统一为参考图金黄 `#F6C52D` 的小号短杆三角箭头，固定在第二张副露牌并以朝向辨识来源，不显示“某家出”文字。
- 2026-07-29 纠正：用户指出蓝色摸牌菱形之前仍沿本家 `48°` 牌架局部轴旋转。现改为 `NewDrawWorldYawPivot` 独立抵消倾角，蓝色网格作为子节点仅绕桌面世界 Y 轴自转，且与平放黄色最新弃牌同为 `126°/s`；两者只保留尺寸与颜色差别。焦点 Runner 均 PASS，最终 Metal 复拍为 `evidence/ui_user_tile_feedback_20260729/selection_world_yaw_2048x1152.png`。
- 2026-07-29 续作：碰、杠的来源提示保留黄色方向箭头，但来源副露牌旁不再显示“上家出 / 对家出 / 下家出”等座位文字；胡牌来源文字不在本次移除范围内。
- 2026-07-29 续作：用户指定碰、杠箭头须完全采用参考牌局的颜色与形状。来源标记现为参考金黄 `#F6C52D` 的小号平面“细杆 + 小三角”箭头，贴在每组第二张副露牌面上并按来源方向旋转；不再使用大三角、发光渐变或悬空箭头，也不显示任何来源文字。新摸牌蓝色菱形改为纯色、无受光渐变，并收紧至紧贴新摸牌。最终 Metal 复拍为 `evidence/ui_user_tile_feedback_20260729/meld_arrow_flat_blue_tight_2048x1152.png`。
- 实现/合同：`scripts/ui/3d/SichuanTile3D.gd`、`scripts/ui/3d/SichuanTableStage3D.gd`，并同步 `SichuanTileVisualQualityRunner.gd` 与 `Sichuan3DTableStageRunner.gd`；最终 Metal 实拍为 `evidence/ui_user_tile_feedback_20260729/meld_arrow_flat_blue_tight_2048x1152.png`（Apple M1 Pro / Metal Forward+）。
- 验证：牌体视觉与 3D 桌面合同均 PASS；固定清单重新跑完 `27/27`（含四个空间分辨率），每项命令退出码均为 0，且无显式 FAIL/脚本错误；`dotnet/AI.Core` Release 构建 `0 warning / 0 error`；`git diff --check` 通过。Godot 部分 runner 在退出阶段仍打印既有 CanvasItem/RID/ObjectDB/resource 清理警告，不属于 runner 失败。本轮没有打包或真机安装，真实手指触控和整局体验仍需手机人工验收。

- 2026-07-29 续作：中心余牌已合并为实体八边形统一计数牌（黄铜边、翡翠内嵌面、静态白色数字），不再让数字单独漂浮；左右两家 3D 弃牌河改为按玩家自身朝向的“左上起点→右向推进→向下换行”局部坐标。2D 回退弃牌层同步声明同一 `upper_left_from_each_player_perspective` 起点合同。
- 真实 Metal / Forward+ 截图：`evidence/ui_unified_center_perspective_river_20260729/table_2048x1152_v2.png`；中心可见 `40` 与八边形底盘融为一个实体控件，左右弃牌按各自玩家视角的起点排列。
- 聚焦回归：`Sichuan3DTableStageRunner`、`SichuanDenseTableLayoutRunner`、`SichuanSpatialLayoutRunner`、`SichuanCommercialMaturityVisualRunner` 与其余 9 个 UI/触控/字体/动效 Runner 全部 PASS；`git diff --check` PASS。Godot 退出仍有既有 RID/ObjectDB/resource leak 警告，但无 Runner 失败。

- 用户已确认此前提出的 Blender 混合资产管线、空间重排与牌局演出方案，并要求把颜色、字体、材质、布局、动画、触控、结算和双端交付全部固化为最终验收标准。
- 新标准：`docs/ui_rework/四川麻将深翡翠精品牌桌与牌局演出最终验收标准_V1.md`。
- 逐项矩阵：`docs/ui_rework/四川麻将深翡翠精品牌桌逐项验收矩阵_V1.json`；每条只能在独立裁判给出 PASS 后标为“已验证”，任何 FAIL 不允许被总分抵消。
- 不可变边界：不做大厅/商业化/开局仪式；玩家面板不得缩小；本家手牌 `132×198` 等基线不得缩小；规则、计分和 AI 不得由表现层改写；只写本项目。
- 当前准确状态：逐项矩阵现为 `已验证 35/43、未处理 8/43`。Stage 1 至 Stage 5 均已由独立裁判逐项 PASS。Stage 5 判词为 `evidence/ui_emerald_final_20260726/judges/Stage5_结算视觉账本与转场_独立验收判词_20260726.md`，总分 `15/15`、无阻塞项；深翡翠/墨玉/旧铜结算视觉、Blender 九宫格、五类四家净零账本以及普通/减弱动态结算转场均有独立实跑与真实截图/录像证据。当前继续 Stage 6 `AC-ENG-01..04`：触控硬门禁、四分辨率七状态安全区、指定参考机性能合同和固定清单全量回归；2.6.9 发布包已完成构建，正在同步 GitHub。

## 2026-07-29 最新续作：Blender 连续胡桃木托盘与深森林绿绒面（已完成本轮 UI 材质优化）

- 针对用户重新确认的图1→图2目标，继续在 Blender 生成管线 `tools/3d/generate_sichuan_table_v2.py` 中做了精细化桌体改造：桌布保持深森林绿短绒 PBR，重新校准到目标图的森林绿中间调；桌体改成一体连续圆角、偏红棕的温暖胡桃木托盘边框，并把原本压住木框的厚墨绿皮革边轨收窄为连续内嵌 gasket；中心/外圈分区槽保持低对比凹槽，不改牌局几何、摄像机、规则和触控。
- Blender 5.2 重新生成 `res/art/3d/sichuan_table_v2.glb` 与 `res/art/materials/table_v2/`；连续圆角框导出前三角面实测 `18,232`，控制在 `8k–20k` 桌体预算内；木框增加纵向木纹浮雕曲线，绒面显示校准进一步向图2的青绿中间调靠拢，Godot 主路径继续保留 GLB 的 PBR，不使用运行时平涂覆盖。
- 真实 Metal 截图：`evidence/ui_warm_walnut_forest_felt_20260729/final_table_forest_walnut_v5_2556x1179.png`，可见连续红棕胡桃木外框、内嵌深绿皮革、无亮铜/缝线装饰、低对比分区槽和更平顺的深森林绿绒面。
- 验证：Blender 生成与 Godot 导入通过；`SichuanTableMaterialQualityRunner`、`Sichuan3DTableStageRunner`、`SichuanLightingQualityRunner` 均通过；最终固定清单 `docs/ui_rework/emerald_final_runner_manifest_V1.txt` 全部 `27/27 PASS`，日志为 `evidence/ui_warm_walnut_forest_felt_20260729/full_manifest_logs_final_v2/summary.txt`；`python3 -m py_compile` 与 `git diff --check` 通过。
- 结论边界：本轮已完成源码/GLB/纹理、本机 Metal 视觉闭环以及 2.6.9 Android/iOS Release 构建；没有改摄像机/桌面布局。Android 无真机连接；iOS 安装命令已对配对 iPhone 15 返回成功，但 CoreDevice 隧道随后进入 `connecting`，尚未取得稳定的设备版本/PID 查询证据。完整一局、真实触控和长时性能仍需用户在手机上人工验收。

## 2026-07-29 2.6.9 发布记录

- Android：`build/android/SichuanMahjong-2.6.9-release.apk`，`versionCode=269`、`versionName=2.6.9`，文件大小 `766,713,674` bytes，SHA-256 `f26539e862853685a04cad413209e94b452bcb3a688494de304655a5b4cb74ac`；`apksigner` v2/v3 通过。
- iOS：`build/ios/DerivedData-2.6.9/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`，Bundle ID `com.chendong.sichuanmahjong.iosdev`，短版本/构建版本 `2.6.9`，arm64，`xcodebuild BUILD SUCCEEDED`，`codesign --deep --strict` 通过；PCK SHA-256 `a03d95f8d23a96ae0369eb47cbb62f584349a3b9a0d6720b0e246c1c5612df82`。
- 真机：已配对设备 `dona‘s iPhone`（iPhone 15，CoreDevice `516E99D2-18B6-5DD8-94E1-6993510A036D`）；安装命令返回成功，启动重试返回成功，但随后 CoreDevice 隧道变为 `connecting`，本轮不把它误报为已完成稳定启动/PID 验证。
- GitHub：`main` 已同步至 `1786f1a`（包含上一提交 `4865e9f` 的 2.6.9 发布与材质升级）。

## 2026-07-28 最新续作：弃牌金菱、中心余牌与绒面质感（已实现并交付多端包）

- 用户本轮三项要求已经完成：① 最后弃牌改为直接悬于牌面上方的实体金黄倒菱锥，使用金面、橙金侧面、阴影和 `126°/s` 自转；② 中心旧文字/暗框/环绕余牌彻底移除，改为中心罗盘上方的无框“余N”金色立体层叠字，原地慢速自转一周（`12°/s`、约 30 秒一周）并轻微悬浮，非绕行；③ Blender 桌布 PBR 重生成，加入顺逆短绒、微纤维起伏、高粗糙度和增强法线，不改变主题色或桌面比例。
- 兼容边界：`CenterTurnIndicator.gd` 仅保留非 3D 回退文本；正常 3D 桌面隐藏它，开启定缺掷骰时才按状态隐藏/显示真正的 3D 余牌，避免重影。减弱动态会把 3D 数字复位为正向静止姿态。
- 实现主文件：`scripts/ui/3d/SichuanTile3D.gd`、`scripts/ui/3d/SichuanTableStage3D.gd`、`scripts/ui/table/CenterTurnIndicator.gd`、`scripts/game/MainSceneV2.gd`、`tools/3d/generate_sichuan_table_v2.py`；相应 3D、动效、材质、牌体视觉 Runner 已同步更新。
- 最终真实 Metal 截图：`evidence/ui_latest_discard_velvet_20260728/rotation_selection_slow_final_2556x1179.png`（可见金菱、余牌悬浮字与高密度绒面）；静态桌面截图可用同目录 `final_table_2556x1179.png`。渲染环境为 Apple M1 Pro / Metal Forward+。
- 验证：最新固定清单 `docs/ui_rework/emerald_final_runner_manifest_V1.txt` 全部 `27/27 PASS`，日志为 `evidence/ui_latest_discard_velvet_20260728/full_manifest_logs_final/summary.txt`；`Sichuan3DTableStageRunner` 再跑通过；`python3 -m py_compile tools/3d/generate_sichuan_table_v2.py` 与本轮 `git diff --check` 通过。
- 重新生成的交付物：macOS `build/macos/SichuanMahjong-2.6.8-ui-v4.app`（`codesign --deep --strict` 通过）；Android `build/android/SichuanMahjong-2.6.8-release.apk`（zipalign、v2/v3 签名验证通过）；iOS `build/ios/DerivedData-2.6.8-ui-v4/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`（arm64、签名验证、`BUILD SUCCEEDED`）。生成项目的 Release 预置同时要求开发自动签名与 Distribution 身份，首次 `xcodebuild` 因冲突失败；以命令行 `CODE_SIGN_IDENTITY='Apple Development'` 覆盖后成功，未改规则/表现源码。
- iOS App 已覆盖安装到已连接 iPhone，Bundle ID `com.chendong.sichuanmahjong.iosdev`，并由 `devicectl` 启动成功。自动化最强证据到安装和启动；完整一局、实际手指触控和长时发热/耗电仍需用户在真机人工验收。

---

# 历史交接：四川麻将 2.6.7 结算明细对齐与全面毛绒桌布

## 2026-07-24 当前状态（实现、回归、双端打包、iOS 安装与启动均已闭环）

- 结算截图中的额外 `+1` 已定位为“牌墙流局后，未下叫玩家向已胡玩家支付的查大叫”，不是自摸加底重复计算；原错误是总账计入、明细漏显。
- 结算页现独立显示“查大叫收益（已胡）”或“查大叫赔付（对方已胡）”；并有账本对齐兜底，保证右侧明细的有符号合计严格等于顶部及左侧最终收分。
- 3D 主桌和 2D 回退桌布移除菱格、云纹、回纹等几何暗纹，改为覆盖完整桌面的交错无规则短绒纤维、顺逆毛明暗和高粗糙度毛毡。
- 真实 Metal 证据：`evidence/ui_settlement_plush_20260724/table_2048x1152_v2.png`、`settlement_2048x1152.png`。
- 四川计分专项、四川规则 `32/32`、结算 UI、恢复 UI、桌体材质、高端桌面视觉与 3D 牌桌聚焦 Runner 均通过；C# Release 构建为 `0 warning / 0 error`。
- Android Release APK：`build/android/SichuanMahjong-2.6.7-release.apk`，文件大小 `361,678,400` bytes，SHA-256 `785be42d19502863fc461d2a33baad3be436994679d0e86eda86a9bba7acb56f`；版本 `267/2.6.7`，`zipalign`、v2/v3 签名、压缩结构及 ARM64 Mono 库校验均通过。
- iOS Xcode/NativeAOT Release 工程：`build/ios/SichuanMahjong-2.6.7-ios-xcode/SichuanMahjongIOS.xcodeproj`；签名 App：`build/ios/DerivedData-2.6.7/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`。
- iOS 成品 Bundle ID `com.chendong.sichuanmahjong.iosdev`，版本 `2.6.7/2.6.7`；主程序和 C# framework 均为 `arm64`，`codesign --deep --strict` 通过，PCK SHA-256 为 `1231b54870b4741a0b4a0448215649d7d134d5281c61f17f54ec8a304b523852`。
- `2.6.7/2.6.7` 已覆盖安装到 `dona‘s iPhone`（iPhone 15，CoreDevice `516E99D2-18B6-5DD8-94E1-6993510A036D`）；设备安装数据库确认版本正确，应用启动成功，延时检查时进程 PID `41393` 仍然存活。
- 当前没有 Android 设备连接，因此 Android 最强证据到签名安装包完整性。iOS 自动化证据已到真机安装、设备版本查询、启动和进程存活；完整一局、实际计分显示、真实触控手感和长时性能仍由用户在手机上人工验收。

## 2026-07-24 Android 2.6.6 发布补齐

- Android Release APK：`build/android/SichuanMahjong-2.6.6-release.apk`，文件大小 `358,790,331` bytes，SHA-256 `3a5a611fe230a797fb820c6873a57de69cb79eafa1c0fd93278a70d2c85e84cf`。
- APK 元数据已核验：包名 `com.chendong.sichuanmahjong`、`versionCode=266`、`versionName=2.6.6`、`minSdk=24`、`targetSdk=35`、ABI `arm64-v8a`。
- `zipalign -c -p 4`、`apksigner verify --verbose --print-certs`（v2/v3）和 `unzip -t` 均通过；APK 内 Mono Godot Android 原生库 SHA-256 为 `9da745177c364666278250a8912d43869166998a277ab8eebfb9f7322a064b6a`，与脚本冻结值一致。
- 当前 `adb devices -l` 没有列出 Android 设备，因此本轮完成的是可安装签名 APK 的构建与静态完整性验证，尚未做 Android 真机安装、启动和完整牌局验收。

## 2026-07-23 当前状态（代码、渲染、回归、签名、安装与启动均已闭环）

- 用户本轮五项已完成源码实现：三家暗手共用翡翠牌背；明牌同时展示上家、对家、下家；AI 自摸仍整手平扣牌背，本家自摸整手正面平铺并标出自摸张；左上改为带二次确认的“退出游戏”；碰、杠在第二张副露牌上方显示小号亮蓝箭头。
- iOS 退出不再依赖会被平台忽略的普通 `SceneTree.quit()`；确认后在 iOS 调用 `OS.kill(OS.get_process_id())`，其他平台继续使用正常场景树退出。
- 聚焦合同已验证：`Sichuan3DTableStageRunner.gd`、`SichuanTableTouchTargetRunner.gd`、`SichuanRestoredUiRegressionRunner.gd` 均退出码 0。
- Metal 对比证据：`evidence/ui_five_corrections_20260723/final/table_1365x768.png`、`3d-reveal_1365x768.png`、`self-draw_1365x768.png`、`utility-expanded_1365x768.png`、`right-meld_1365x768.png`。
- 当前版本已升至 `2.6.6`。Xcode/NativeAOT Release 工程：`build/ios/SichuanMahjong-2.6.6-ios-xcode/SichuanMahjongIOS.xcodeproj`；签名 App：`build/ios/DerivedData-2.6.6/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`。
- 签名成品主程序和 C# framework 均为 `arm64`，Bundle ID `com.chendong.sichuanmahjong.iosdev`，版本 `2.6.6/2.6.6`，`codesign --deep --strict` 通过；PCK SHA-256 为 `f7adb184bfaf0423d92cc35cb8b3cd90e95c9364eeef14c63cf4e79ca838a4b6`。
- `2.6.6/2.6.6` 已覆盖安装到 `dona‘s iPhone`（iPhone 15，CoreDevice `516E99D2-18B6-5DD8-94E1-6993510A036D`），设备安装数据库确认版本正确；应用启动成功且设备进程 PID `37060` 存活。
- 自动化证据最强到真机安装和启动。退出确认后的系统关闭、完整一局、真实触控手感与长时性能仍由用户在手机上人工验收。

---

# 历史交接：四川麻将 2.6.5 暗纹、自摸扣牌、碰杠来源与计分重构

## 2026-07-23 当前状态（代码、规则、渲染、回归、签名、安装与启动均已闭环）

- 用户本轮四项要求均已完成：桌面加入低对比蜀锦暗纹；自摸后整手平扣且全部显示统一翡翠牌背；碰、明杠、补杠用按座位着色的大箭头和中文标签标明来源；胡牌、杠钱、杠上炮转移与流局计分按用户给出的四川规则重构。
- 计分主合同：`基础得分=底分×2^番数`，全局 4 番/16 倍封顶；自摸不加番、每家在基础得分外固定加 1 底；点炮仅放炮者支付；无海底加番；直杠/补杠/暗杠当场独立结算且流局不退；杠上炮将该次全部杠钱从开杠者转给胡牌者。
- 番型与流局合同：平胡 0、对对胡 1、清一色/小七对/金钩钓 2、清对/将对/龙七对 3、清七对/清金钩/十八罗汉 4；根、杠上花、抢杠胡、杠上炮各加 1 后统一封顶；花猪固定 16 倍赔已下叫者，未下叫者按对方最大可胡分赔下叫和已胡玩家。
- 最终 Metal 证据：`evidence/ui_scoring_texture_20260723/table_1365x768_v2.png`、`self-draw_1365x768_v2.png`、`right-meld_1365x768_v2.png`。
- Godot 项目导入/解析退出码 0；计分专项、四川规则 `32/32`、3D 桌面、桌体材质、结算 UI、恢复 UI、触控目标和高端视觉合同共 8 个 Runner 全部通过。Runner 退出时仍有既有 RID/ObjectDB/resource leak 告警，但无测试失败。
- C# AI Core Smoke Release 构建 `0 warning / 0 error`；完整冒烟通过，精确牌形穷举 `131,841` 状态无失败。
- 版本已升至 `2.6.5`。Xcode/NativeAOT Release 工程：`build/ios/SichuanMahjong-2.6.5-ios-xcode/SichuanMahjongIOS.xcodeproj`；签名 App：`build/ios/DerivedData-2.6.5/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`。
- 签名成品为 `arm64`，Bundle ID `com.chendong.sichuanmahjong.iosdev`，版本 `2.6.5/2.6.5`，`codesign --deep --strict` 通过；`xcodebuild` 为 `BUILD SUCCEEDED`。
- `2.6.5/2.6.5` 已覆盖安装到 `dona‘s iPhone`（iPhone 15，CoreDevice `516E99D2-18B6-5DD8-94E1-6993510A036D`），设备安装数据库确认版本正确，`devicectl --terminate-existing` 启动成功。
- 剩余验收边界仅为用户在真机上人工体验本轮三项画面、完整一局计分、真实触控手感及长时性能/发热/耗电；自动安装和启动不替代人工玩法验收。

---

# 历史交接：四川麻将 2.6.4 五项 UI 精修与 iOS 真机交付

## 2026-07-23 当前状态（代码、渲染、回归、签名、安装与启动均已闭环）

- 用户本轮五项要求均已实现：定缺只展示条/筒/万三枚 230×230 质感圆印且无提示卡；桌面增加浅微纤维纹理、桌框增加分辨率无关织纹；最后弃牌正上方改为大号旋转翡翠菱形；当前手牌选中改为暖象牙金边手型；结算页统一为主桌深蓝/钢蓝/金色体系。
- 最终本机 Metal 证据在 `evidence/ui_five_polish_20260723/`：`ding_que_2048x1152.png`、`clean_2048x1152.png`、`selection_vector_hand_2048x1152.png`、`settlement_2048x1152.png`。
- 聚焦回归 `7/7` 通过：牌体视觉、桌体材质、3D 桌面合同、HUD 状态完整性、触控目标、恢复 UI 回归、六项历史 UI 回归；Godot 个别 headless Runner 仍打印既有 RID/resource 泄漏告警，但没有测试失败。
- 版本已升至 `2.6.4`；Xcode/NativeAOT Release 工程为 `build/ios/SichuanMahjong-2.6.4-ios-xcode/SichuanMahjongIOS.xcodeproj`，`xcodebuild` 为 `BUILD SUCCEEDED`。
- 成品主程序与 NativeAOT framework 均为 `arm64`，`codesign --deep --strict` 通过；成品 PCK SHA-256 为 `3fcd2f780b63f86219e354fbfc193a4b70cc9e7365a134a65558b5d2bcf0109e`，且实际包含手型纹理、桌框纹理 shader、桌布 shader。
- `2.6.4/2.6.4` 已覆盖安装到 `dona‘s iPhone`（iPhone 15，CoreDevice `516E99D2-18B6-5DD8-94E1-6993510A036D`）；`devicectl` 启动成功，设备进程 PID `36629` 存活。
- 当前开发签名 profile 到期时间为 `2026-07-29T14:17:33Z`，到期后需重新签名安装。剩余验收边界仅为用户在真机上人工体验五项视觉、完整一局、真实触控手感及长时性能/发热/耗电；自动安装和进程存活不替代这些体验证据。

---

# 历史交接：四川麻将 2.6.3 直立牌、旋转选牌、白面与 iOS 安装

## 2026-07-22 当前 P0（等待手机 USB 连接或 Xcode 重新登录）

- 用户最新要求：三家暗手与桌面严格 90°；选牌使用非菱形旋转图形且去掉大块底色；本家手牌白度对齐弃牌；完成后打包并安装 iOS。
- UI 已完成：`SIDE_RACK_TILT_DEGREES=90`、`FAR_RACK_TILT_DEGREES=-90`；选牌为天青色三叶旋轮 `96°/s`，摸牌仍为金色钻锥 `120°/s`；本家/弃牌 Metal 中位亮度为 `241.1/242.0`，差 `0.9/255`。
- 验证已完成：Godot Runner `27/27`、摄像机 `4/4`、C# Release `0 warning / 0 error`；最终截图在 `evidence/ui_upright_selection_brightness_20260722/`。
- 版本已升至 `2.6.3`；Godot iOS Xcode/NativeAOT 导出成功，工程为 `build/ios/SichuanMahjong-2.6.3-ios-xcode/SichuanMahjongIOS.xcodeproj`。
- 当前阻塞：有效 Apple Development 证书仍在，但 Xcode Accounts 和本地 provisioning profile 缓存为空；第一次 xcodebuild 在签名前失败为 `No Accounts` / `No profiles for com.chendong.sichuanmahjong.iosdev`，没有产生或安装不完整 App。
- 手机通过 Wi-Fi CoreDevice 在线，UDID `00008120-000915803A90A01E`，当前安全保留 `2.6.2/2.6.2`。临时 `/tmp/sichuan_device_profile_20260722` 已装 `pymobiledevice3`，但 Wi-Fi tunnel 需要管理员权限；最小下一步是用户用数据线连接并解锁手机，随后运行 `pymobiledevice3 provision dump` 只读恢复手机现有 profile，或用户在 Xcode Settings > Accounts 重新登录 Apple ID。
- profile 恢复后：手动 Apple Development Release 构建 -> arm64/codesign/PCK/版本检查 -> devicectl 覆盖安装 -> 设备版本查询 -> 启动 -> 延时进程存活 -> 补齐报告/README/本交接。

---

# 历史交接：四川麻将手牌亮度、对手正面与侧家内倾

## 2026-07-22 当前状态（三项修正已进入终验）

- 恢复来源：会话 `019f6a3f-c450-7c23-bd89-905d949609c1` 最后未完成请求；范围为本家手牌提亮、三家玩家侧正面恢复象牙白、上/下家牌顶向桌心内倾。
- 本家手牌使用局部 `local_ivory_floor_boost`，只抬高自身暖象牙材质的暗部下限，不修改全局曝光、桌布或其他牌区。
- 暗手改成实体双面合同：玩家侧 `warm_ivory_front`，桌心侧独立翡翠背；按座位姿态翻转表面，对家仍从桌心方向显示绿色背面。
- 上家/下家使用正向 `76°` 立牌倾角，牌顶世界空间方向指向桌心；对家保留原负向 `76°`，摄像机构图远端/本家牌高比恢复为 `0.4324` 并通过门禁。
- 标准/报告：`docs/ui_rework/四川麻将手牌亮度对手正面与侧家内倾验收标准_V1.md` / `四川麻将手牌亮度对手正面与侧家内倾验收报告_V1.md`。
- 最终 Metal 2048×1152：`evidence/ui_hand_surface_tilt_20260722/final_2048x1152.png`，SHA-256 `39765c73a0300beed7f15aab75a12ecc21b202e3aa8a1f6bddf01d692240a460`。
- 验证：当前 Godot Runner `27/27`；最后无行为清理后聚焦 3D Runner 再次通过；Godot 项目扫描 exit 0 且无 Parse/SCRIPT Error；Python 工具编译、差异检查与本轮秘密扫描通过。
- 结论边界：本轮没有提高版本、打包或安装 iOS；最强证据到本机真实 Metal 渲染与自动回归，真机完整一局/触控/长时性能仍需单独验收。

---

# 历史交接：四川麻将自摸、弃牌三行、玉石牌体与下家副露

## 2026-07-22 当前状态（四项修正已验收）

- 自摸/点炮已经分流：自摸牌保留在完整手牌中并随全手倒下，不生成独立 `winning_*` 牌或来源箭头；点炮仍保留独立胡牌张与来源箭头，AI 原站立手牌不消失。
- 四家弃牌行距由 `0.50` 增至 `0.78`；18 张三行压力状态的第 1/2、2/3 行逐列投影均不相交。
- Blender 牌体重建为 `0.032` 八段圆角、`0.055` 翡翠背层；亮牌去掉不透明白色内框，字面直接落在圆润象牙玉体，材质为 roughness `0.25`、clearcoat `0.40`、SSS `0.06`。GLB SHA-256 为 `b3d73b47c5a2a6d73f9ea591e267136e0bdcffaa643658c8442c9f2fcd54270e`。
- 下家碰/杠固定在 `X=4.85` 的同一竖向副露导轨，和下家手牌同方向；多组继续沿 Z 排列并留组间缝，不再横向侵入对家区域。对家副露与下家副露、暗手、弃牌均不相交。
- 本家牌最终为 `1.94×`、步距 `0.80`；14 张构图宽 `79.67%`、单牌高 `14.10%`，保持手机可读并避免整排合成白色长板。
- 标准/报告：`docs/ui_rework/四川麻将自摸弃牌玉石牌体与下家副露验收标准_V1.md` / `四川麻将自摸弃牌玉石牌体与下家副露验收报告_V1.md`。
- 验证：Godot 当前 Runner `26/26`；Blender 5.2 生成 exit 0；Godot 导入/解析通过；Python 编译、差异检查和本轮秘密扫描通过。
- 最终 Metal 2048×1152 证据：`evidence/ui_selfdraw_jade_meld_20260722/self_draw_2048x1152.png`、`discard_pressure_2048x1152.png`、`right_meld_2048x1152.png`。
- 本轮未提高版本、未打包或安装 iOS；最强证据到本机真实 Metal 渲染与全量回归。

---

# 历史交接：四川麻将目标图牌体、牌姿与状态反馈

## 2026-07-22 当前状态（六项牌体/牌姿修正已验收）

- 侧家手牌改为 `76°` 站立角，暗牌显示翡翠绿大背面+象牙窄顶边；明牌符号方向朝向各自玩家。
- Blender 5.2 LTS 确定性生成分层牌体：暖象牙主体、正面浅凹安全边、四段倒角和独立翡翠背层；`mahjong_tile_body.glb` SHA-256 为 `363b17b4f50c9c1349cf59c8973bc4aed4b1e2d9869fefdb7f8b3e6cd08f1e00`，Tripo3D 调用为 0。
- 摸牌反馈改为牌上方单一金色钻锥，`120°/s` 旋转，不再整张变色；reduced-motion 保持可见但停止旋转。
- 中央恢复旧牌匾：深青黑切角+暗金双线，只保留“余牌/大数字/X家出牌中”；无东南西北和彩色方向角。
- 上/下家碰杠与各家手牌阅读方向一致，多组副露改为并行动态缩进；四组压力状态不与手牌/弃牌相交。
- 本家缺门牌只在显示层稳定排到最右，原 tile id、选牌和点击返回映射不变。
- 验收标准/报告：`docs/ui_rework/四川麻将目标图牌体牌姿与状态反馈验收标准_V1.md` / `四川麻将目标图牌体牌姿与状态反馈验收报告_V1.md`。
- 验证：当前 Godot Runner `26/26`；摄像机四横屏比例 `4/4`；Blender 重建 exit 0；Godot 项目解析 exit 0；三张最终 Metal Forward+ 2048×1152 图已人工复核。
- 证据目录：`evidence/ui_tile_pose_feedback_20260722/`，最终图后缀为 `_final.png`。
- 本轮未重新打包或安装 iOS；最强证据到本机真实 Metal 渲染与全量回归。

---

# 历史交接：四川麻将目标图布局二次校准与 iOS 重打包

## 2026-07-19 当前状态（六项校准与 2.6.2 真机交付已完成）

- 用户指定六项均已验证：中央余牌/四家弃牌上移、本家副露位于手牌左侧、三家 AI 字面旋转 180°、AI 点炮保留原手牌并邻接胡牌张、四家 HUD 重排、左上缩进态使用 76×76 汉堡图标。
- 验收标准/报告：`docs/ui_rework/四川麻将目标图布局二次校准验收标准_V1.md` / `四川麻将目标图布局二次校准验收报告_V1.md`。
- 最终真实 Metal 矩阵为 4 状态×4 分辨率共 16 张；独立设计复核 `24/25`，P0/P1/P2 均为 0。
- 非摄像机 Godot Runner `24/24`、摄像机 `4/4`、C# 主工程和 AI Core Smoke Release 均为 0 warning/0 error，Python 工具编译与 `git diff --check` 通过。
- 2.6.2 iOS Release `BUILD SUCCEEDED`，主程序/NativeAOT 均为 arm64，开发签名和深度验证通过，源/成品 PCK SHA-256 均为 `485972fc6202460cada445598bb89565a27052c8dd9e6dd4ef2f2ed054e5779c`。
- 2.6.2 已覆盖安装到 `dona‘s iPhone`，设备安装库确认 `2.6.2/2.6.2`；08:40:01 启动成功，PID `24210` 在 08:44:06 仍存活。
- 剩余边界仅为用户真机人工验收：完整一局、真实手指触控、10–20 分钟帧率/发热/耗电。构建、安装和进程存活不替代这些体验证据。

---

# 历史交接：四川麻将“锦夏蓝庭”对手牌姿、AI 点炮展示与 iOS 交付收口

## 2026-07-18 当前 P0（真机启动待解锁）

- 用户用商业目标图复核后指出：上家、下家、对家暗手的桌面摆放角度仍不自然；AI 点炮胡牌只应展示被点炮的胡牌张。必须客观对比、量化通过、独立设计师复核后才能打包安装。
- 验收标准/报告：`docs/ui_rework/四川麻将对手牌姿与点炮展示验收标准_V1.md` / `四川麻将对手牌姿与点炮展示验收报告_V1.md`；最终证据为 `evidence/ui_opponent_pose_20260718/final_v3/`。
- 左右家暗手已按目标图改为向远端收敛、向本家前景外扩，对家保持水平居中；AI 点炮胡牌仅展示 1 张胡牌张与 1 个来源箭头。
- 最终带符号量化：左右平均轴 `76.033°`，目标 `73.136°`，差 `2.897°`；左/右前景位移 `-2.847%/+2.848%`；对家水平偏差 `0.0031°`，全部通过。
- 独立设计首轮 `15/20` 因轴方向相反否决；重做后二轮 `18/20`，P0/P1 为 0，PASS。Godot 非摄像机 Runner `24/24`、摄像机 `4/4`、C# Release `0 warning / 0 error`、Metal 600 帧性能门禁全部通过。
- `2.6.1` iOS Release `BUILD SUCCEEDED`，主程序与 NativeAOT framework 均为 arm64，开发签名与 `codesign --deep --strict` 通过，源/成品 PCK SHA-256 均为 `d9fcc4f4b92f54ffdf465d230972cfc53c58e146a6e5b75f2740ab4671d2c6d1`。
- `2.6.1` 已覆盖安装到 `dona‘s iPhone`，设备安装库确认 `2.6.1/2.6.1`。当前唯一未闭环项是手机锁屏导致 iOS 以 `Locked` 拒绝自动启动；解锁后必须重试启动并记录 PID 与 60 秒存活。
- 交付报告：`docs/ui_rework/四川麻将动效触控性能与iOS交付验收报告_V1.md`。真机完整一局、真实触控手感、长时帧率/发热/耗电仍属用户人工验收，不由安装或自动启动替代。

---

# 历史交接：四川麻将“锦夏蓝庭”参考蓝桌 3D UI 与旧功能恢复（第六阶段通过）

## 2026-07-18 HUD、状态与旧功能完整性门禁（第六优先已通过）

- 标准/报告：`docs/ui_rework/四川麻将HUD状态与旧功能完整性验收标准_V1.md` / `四川麻将HUD状态与旧功能完整性验收报告_V1.md`。
- 四轮独立复核依次为 `18/25`、`20/25`、`21/25`、`23/25`；只有第四轮满足每项不低于 4、总分不低于 23、无 P0/P1，前三轮失败均保留。
- 最终修复定缺大圆印、HUD 身份与状态槽、胡/取消、自摸/取消、已胡倒牌与不可点击、点炮来源“上家 + 箭头”、明牌稳定刷新、工具栏事件去重、AI 拖动/展开/0%–100% 透明度和超宽屏手牌裁切。
- 真实 Metal `4×4=16` 图全部通过结构检查；定缺直径约为短边 `14.8%–15.0%`、等距误差 `0–0.5 px`、文字描边对比 `19.033:1`。
- Godot Runner `29/29`，AI.Core 构建 `0 warning / 0 error`；双设计师最终均 `23/25`，无 P0/P1。P2：AI 展开会覆盖少量左下公共牌区，商业头像与材质细节仍可提升。
- 当前进入第七关：动效、触控、Forward+ 真机性能、iOS 打包/签名/安装/启动。未完成真机长时帧率、发热和完整一局前，不得宣称移动端全部验收完成。

## 2026-07-18 光照、阴影、AO 与后处理质量门禁（第五优先已通过）

- 标准/报告：`docs/ui_rework/四川麻将光照阴影AO与后处理验收标准_V1.md` / `四川麻将光照阴影AO与后处理验收报告_V1.md`。
- 共做八轮真实渲染；前七轮分别因方向虽对但死黑、牌面退化、桌面越线、阴影覆盖指标诱导长硬影等原因未放行。
- 最终中央阴影 P5/中位比 `0.616`、深影覆盖 `5.35%`、P5 `64.36`；桌面三段与牌面/牌背/字面对比三套分析器全部通过。
- 灯光预算保持 `2/1`，SSAO 小半径启用；牌面印刷层色彩稳定、实体树脂层接受真实光照。
- Compatibility 实测桌面抬亮并失败，移动渲染器升级为 Forward+；真机性能留到第七阶段硬验收。
- 四档 Metal `4/4`，十项聚焦 Godot、C# Release、Python 编译和差异检查通过；双设计师均 `23/25`，无 P0/P1。P2：影边略硬、白面略亮。
- 当前进入第六关：HUD、定缺、胡/取消、已胡倒牌、点炮箭头、明牌、工具栏和旧 UI 功能完整恢复。

## 2026-07-18 夏日蓝桌材质与桌体质量门禁（第四优先已通过）

- 标准：`docs/ui_rework/四川麻将夏日蓝桌材质与桌体验收标准_V1.md`；报告：`docs/ui_rework/四川麻将夏日蓝桌材质与桌体验收报告_V1.md`。
- 经过四轮真实校准才通过：最终上/中/下 RGB 距离为 `3.32/4.12/3.61`，相对亮度差为 `0.047/0.028/0.075`，相邻 5% 高度带最大跳变 `9.0`。
- 桌布 roughness `0.92`、压纹贡献 `3.5%`、局部最大通道标准差 `1.31`；实体桌框、桌布和四条冷灰蓝金属嵌线尺寸/材质合同通过。
- 四档 Metal 图 `4/4`，九项聚焦 Godot 回归、C# Release `0 warning / 0 error`、Python 编译与差异检查通过。
- 主设计师 `24/25`，独立设计师 `23/25`，每项均不低于 4，无 P0/P1，明确通过。P2：织感略干净、桌框嵌线偏保守。
- 当前进入第五关：灯光、阴影、AO 与后处理。前四阶段参数继续冻结，未通过第五关前不进入 HUD/动效。

## 2026-07-18 牌体与字面可读性质量门禁（第三优先已通过）

- 新标准：`docs/ui_rework/四川麻将牌体与字面可读性验收标准_V1.md`；报告：`docs/ui_rework/四川麻将牌体与字面可读性验收报告_V1.md`。
- 最终完整牌局牌面/牌背色差为 `6.78 / 4.0`；绿字/红字对比度由基线 `3.89 / 3.31` 提升为 `7.66 / 6.72`，目标图为 `7.39 / 6.87`。
- 牌体保持真实 `0.42×0.18×0.58` 圆角实体；牌面、树脂侧边、牌背三层明确；27 张字面资源包围盒和 mipmap `27/27` 通过。
- 正常牌局四分辨率像素检查 `4/4`；全员明牌和已胡/点炮来源箭头状态证据通过。九项 Godot 回归通过；C# Release `0 warning / 0 error`；差异检查通过。
- 主设计师与独立设计师均为 `24/25`，无 P0/P1，明确通过第三阶段。P2：1365 右侧密集区略紧、个别远端明牌仍可更从容。
- 当前进入第四关：夏日蓝桌材质与桌体；摄像机、牌组排布、牌体颜色与字面资源作为已通过基线冻结。

## 2026-07-18 牌组排布与空间张力质量门禁（第二优先已通过）

- 前置摄像机 V2 参数保持冻结；第二关只调整四家暗手、副露和弃牌的空间排布。
- 新标准：`docs/ui_rework/四川麻将牌组排布与空间张力验收标准_V1.md`；报告：`docs/ui_rework/四川麻将牌组排布与空间张力验收报告_V1.md`。
- 收紧后的 `--profile layout` 最终 15/15；左右家平均宽度/顶部/底部从 7.41%/17.80%/74.41% 修正为 8.35%/14.49%/70.76%，目标为 8.41%/14.25%/70.82%。
- 新增四家各 18 张弃牌压力合同；中央余牌保留区、四家弃牌、副露、暗手与本家点击区不重叠。本家副露不再被前景手牌遮住。
- `camera/` 与 `gameplay/` 两套四分辨率 Metal 图均 4/4；聚焦 Godot 回归、C# Release、Python 编译和差异检查通过。
- 主设计师 24/25；独立设计师 23/25、无 P0/P1，明确通过本阶段。P2 保留：侧家略外扩/略长、底端略低、左下本家副露与手牌间距偏近。
- 当前进入第三关：麻将牌体、牌背、字面颜色与手机可读性；尚未开始桌面材质、灯光、HUD 或动画的后续门禁。

## 2026-07-18 摄像机重构 V2 质量门禁（第一优先已通过）

- 当前只完成“第一优先：摄像机与四家构图重构”，尚未进入材质、灯光、牌体、HUD 或动画的下一阶段。
- 用户将 V1 正交版与目标图直接对比后提出未达标；V1 验收标准和报告已明确标记作废/撤销，不得再作为通过证据。
- 当前门禁为 `docs/ui_rework/四川麻将摄像机重构验收标准_V2.md`；最终报告为 `docs/ui_rework/四川麻将摄像机重构验收报告_V2.md`。
- 最终方案是真实 3D 长焦透视：摄像机 `(0,13,13)` 看向 `(0,0,-0.50)`，俯角约 44°，横向 FOV 49.5°，使用 `KEEP_WIDTH` 同时保持目标宽屏构图和 16:9 安全性。
- 2556×1179 目标差异量测 15/15 通过：本家总宽 75.63%（目标 74.84%）、顶部 82.80%（目标 83.29%）、牌面高 16.27%（目标 16.71%）、距底 0.93%（目标 0%）；桌面上/下宽 61.50%/87.56%（目标 62.09%/86.23%）。
- Metal 四图 1365×768、2048×1152、2400×1080、2556×1179 的黑边、透明空洞和边缘检查 4/4 通过；16:9 下 HUD 与四家牌列不再互相拥挤。
- 主设计师复核 24/25；独立设计师重新对比 V2 目标图后给出 23/25（5/4/5/5/4），无 P0、无 P1，明确“通过摄像机阶段质量门禁”。
- 独立评审 P2 如实保留：左右家整列略细略低；中央留白比目标略大；桌面下缘开口略大。这些进入后续“牌组排布/空间张力”的量化约束，不伪装成已修复。
- 聚焦回归：四宽高比摄像机合同 4/4、3D 牌桌合同、3D 回退合同、商业成熟度视觉合同、Python 工具编译与 `git diff --check` 均通过。
- 最终证据位于 `evidence/ui_camera_reconstruction_20260718/target_final/`。本阶段没有重新打包或安装 iOS，也未把桌面渲染回归冒充真机完整牌局验收。

## 2026-07-18 当前 P0

- 用户最新用两张成熟蓝色牌桌截图校准了方向：不再沿用前一轮薄荷绿桌面，先提取参考色、角度和比例，再映射到四川麻将。提炼文档为 `docs/ui_rework/四川麻将参考蓝桌色彩与3D摆牌提炼_V1.md`。
- 参考核心色已映射：桌面 `#5B80AF/#4F70A3/#435685`，桌外 `#202A43`，牌背 `#2B9131`，牌面 `#E9E8EB`，定缺条/筒/万 `#24B87A/#F6B73B/#EA5146`。本家牌架保留 48° 后仰，但改为更高、更紧密的参考比例。
- 用户要求在不丢失旧 UI 功能的前提下，将当前深色 3D 桌改为明亮、清爽的夏日风格；定缺条/筒/万改成大型玻璃圆印；本家暗手立起，碰杠才平放且靠近本家边缘。
- 已确认规则快照仍保留 `has_won` / `winning_tile` / `winning_source_seat` 和人类自摸/响应胡权限；回归来自新 3D 表现层未消费这些字段，不修改规则或 AI。
- 已发现一个关键新回归：`SichuanTableStage3D._apply_entries()` 只在创建新节点时调用 `configure()`，导致同一张牌后续的明牌、胡牌、最新弃牌、选牌和 AI 标记状态不刷新。
- 问题矩阵：①定缺圆印 `已验证`；②夏日明亮主题 `已验证`；③本家暗手立起 `已验证`；④本家碰杠靠近玩家 `已验证`；⑤胡/取消按钮通路 `已验证`；⑥已胡手牌平放、独立胡牌与点炮箭头 `已验证`；⑦旧状态动态刷新回归 `已验证`。
- 自动验证：当前 19 个 `Sichuan*Runner.gd` 全部通过；C# Release 构建 0 warning / 0 error；AI.Core Smoke 通过；四分辨率 Metal 截图像素检查 4/4 通过；`git diff --check` 通过。
- 发布验证：2.5.0 Xcode Release `BUILD SUCCEEDED`，App 与 NativeAOT framework 均为 arm64，`codesign --deep --strict` 通过；Xcode 源 PCK 与最终 App PCK 的 SHA-256 均为 `92177ccf013a8d5367b3c57f4a4a78a9b7a5d8323bd3235f8a39ea86ee57cbd8`。
- 真机交付：2.5.0 已覆盖安装到 `dona‘s iPhone`，设备安装库确认版本/构建 `2.5.0/2.5.0`；自动启动成功，进程 PID `21229` 保持运行。
- 最强自动证据到“真机安装、启动、进程存活”；手机上一整局的手感、触控体验、发热和持续帧率仍需用户人工验收，不能由自动启动替代。

---

# 历史交接：四川麻将 3D 牌桌可读性收口（2.4.1 已完成）

## 2026-07-18 2.4.1 当前任务

- 用户以成熟移动麻将界面为参照，要求扩大牌桌取景和全部麻将牌、修正本家牌面朝向、移除实体未摸牌墙、让 AI 手牌立起、降低牌体泛白，并删除中央余牌区方向标签。
- 已完成源码调整：相机 FOV 由 38 收紧至 32；本家/对手手牌、副露、弃牌统一放大；本家手牌与弃牌旋转纠正；三家隐藏手牌改为竖立；实体牌墙不再渲染；暗杠保留扣牌表现；牌体改为暖玉象牙材质；中央仅保留居中的“余牌”、数字和状态行。
- 3D 牌桌、回退、商业视觉、触控、动效、布局、Premium、恢复功能、紧密牌局、六项问题与 iOS 字体共 11 组聚焦 Godot 回归通过；C# Release 构建 0 warning / 0 error；`git diff --check` 通过。
- `evidence/ui_3d_readability_20260718/final/` 的 1365×768、2048×1152、2400×1080、2556×1179 四档 Metal 截图全部通过像素检查，人工复核确认无实体牌墙、无对家桌沿、牌面朝向正确且中央弃牌不互相覆盖。
- 版本已升级为 2.4.1；iOS Release `BUILD SUCCEEDED`，App 与 NativeAOT framework 均为 arm64，`codesign --deep --strict` 通过，Xcode 源 PCK 与最终 App PCK 的 SHA-256 一致。
- 2.4.1 已覆盖安装到 `dona‘s iPhone`；设备安装库确认版本/构建 `2.4.1/2.4.1`，自动启动成功，启动后进程 PID 20493 保持运行。
- 自动证据已覆盖源码、渲染、回归、构建、签名、安装、启动和进程存活；真机完整打一局的手感、发热与持续帧率仍需用户人工验收，不用安装/启动冒充完整牌局验收。

---

# 历史交接：四川麻将 3D 牌桌重构（2.4.0 已完成）

## 2026-07-18 当前目标

- 用户已安装 Blender 5.2 LTS，并授权在免费额度内使用 Tripo3D；密钥仅存于 macOS 钥匙串，未写入仓库。
- 目标是保留现有四川规则、老手/透视地狱 AI、2D HUD 和触控修复，完整替换牌桌、牌体、四家手牌、牌墙、弃牌与副露为 3D 表现。
- 正式方案：`docs/ui_rework/四川麻将纯3D牌桌改造设计方案_V1.md`。
- 成本决策：核心 V1 使用 Blender 精确建模和 Godot 程序化材质，Tripo 调用预算为 0，保留免费额度给未来非关键装饰。
- 工程原有大量未提交 UI/AI/evidence 变更，均须保留；本轮只做增量文件，不清理、不重置。

## 当前进度

1. 已审计工程、现有实验 `MahjongTile3D` / `HandStage3D`、快照与 UI 数据通路。
2. Blender 5.2.0 LTS 可执行；Tripo Keychain 配置已验证，尚未发起生成任务。
3. 3D 设计方案与验收门槛已落盘。
4. Blender 确定性生成倒角牌体与会所桌体 GLB，程序化蜀锦材质、透视相机、左上投影主光与无影补光已完成。
5. `SichuanTableStage3D` 已接入 `MainSceneV2`；四家手牌、牌墙、副露、弃牌、明牌、选牌、AI 标记、最新弃牌与手机点击映射全部走 3D 表现。
6. 新增 3D 数据/输入合同和 2D 回退合同；共8组聚焦 UI 合同连同新增回退验证均通过，C# Release 构建 0 warning / 0 error。
7. 四档 Metal 完整牌局截图与像素检查 4/4 通过；证据位于 `evidence/ui_3d_rebuild_20260718/final/`。
8. 版本已升级为 2.4.0；iOS Release `BUILD SUCCEEDED`，App 与 NativeAOT framework 均为 arm64，`codesign --deep --strict` 通过，PCK 源/成品 SHA-256 一致且包含本轮 3D GLB/Shader/编译脚本。
9. 2.4.0 已覆盖安装到 `dona‘s iPhone`，设备安装库确认版本/构建 2.4.0/2.4.0，自动启动成功且进程保持运行。

## 完成门槛

- 牌局 3D 状态合同、交互、截图矩阵、Godot/C# 回归、iOS 构建、最新 PCK/arm64 framework 检查。
- 设备可用时安装并启动；真机完整一局必须按证据层级单独说明。

上述自动化、构建、安装与启动门槛已全部满足；仍需用户在手机上人工打完一局，用于评估真实手感和长时帧率，不将安装/启动证据冒充为完整牌局人工验收。

---

# 历史交接：四川麻将中央余牌区简化与商业成熟效果 V3

## 2026-07-17 中央余牌区低密度改造

- 用户反馈 V3 中央余牌区的四向风盘、放射分区、方向字和循环铜光同时出现，信息密度过高、看着头晕。
- 已将中央区收敛为单焦点旧铜切角牌匾：只保留“余牌”、大号剩余数字、一行状态和一个当前方向小标签；移除本/上/对/下四向文字、四个象限、放射线、方向箭头和循环脉冲。
- 中央预留尺寸仍为 210×210，不改桌面麻将牌、弃牌河、回合状态数据、AI、规则或触控链路；图形层继续使用 `MOUSE_FILTER_IGNORE`。
- `SichuanCommercialMaturityVisualRunner` 新增低密度合同：单方向标签、零放射分区、静态底板、无旧四向节点。
- 商业视觉、布局、动效、触控、Premium 和密集牌桌共 6 个聚焦 Godot 回归通过；C# Release 构建 0 warning / 0 error；`git diff --check` 通过。
- `evidence/ui_center_simplified_20260717/` 已生成 1365×768、2048×1152、2400×1080、2556×1179 四档 Metal 截图，像素检查 4/4 通过；人工检查 2048 和 1365 档确认中央数字清楚、层级明显变轻。
- 本次仅完成源码与桌面实机渲染验证，尚未重新打包或覆盖安装手机；手机上的 2.3.0 仍是上一版四向风盘。

## 2026-07-17 商业成熟效果 V3 当前任务

- 用户要求按照能力上限先形成市面成熟效果改造方案，再直接实施。
- 方案已写入 `docs/ui_rework/四川麻将商业成熟效果改造方案_V3.md`。
- 本轮保留蜀锦玉案品牌，不复制竞品蓝色卡通皮肤；实施重点为固定视角空间、玩家身份徽章、中央四向回合仪表、状态反馈、响应式与触控。
- 批次 A–D 已实施：浅透视桌体与厚边、四家玉印 HUD、中央四向风盘、浮空圆印动作按钮、四档响应式验证和 iOS 交付；2.2.2 左上工具栏双事件修复、AI 老手算法、侧边手牌居中和既有 UI 合同全部保留。
- 工作树原本已有大量未提交变更和 `evidence/`，均属于既有成果；只在本轮明确文件内增量编辑，不清理、不重置。

### V3 实施与验收结果

- 牌桌使用固定视角浅透视结构，增加深色桌沿、旧铜内沿与四家围合区；不使用竞品蓝色卡通皮肤。
- 四家 HUD 增加可替换的程序化玉印徽章和座位字印，当前行动同时使用字章、边缘光和形状，不只依赖颜色。
- 中央余牌区升级为本/上/对/下四向风盘；碰/杠/胡/过升级为大型浮空圆印按钮，真实触控父节点保持稳定。
- 9 个聚焦 Godot UI/布局/动效/触控回归全部 exit 0；C# Release 构建 0 warning / 0 error；`git diff --check` 通过。
- `evidence/ui_commercial_v3_20260717/` 的 1365×768、2048×1152、2400×1080、2556×1179 四档 Metal 截图像素检查 4/4 通过。
- 版本升级为 2.3.0；Xcode Release `BUILD SUCCEEDED`，App 和 C# framework 均为 arm64，`codesign --deep --strict` 通过，最终 PCK 源/成品 SHA-256 均为 `e6fd53be783c009a6d545d39c3fc6209372d91ec4de9d3a62e93cd172bfbe3fc`。
- 包含最后中央风盘间距收口的 2.3.0 已再次覆盖安装到 `dona‘s iPhone`，设备安装库确认版本/构建为 2.3.0/2.3.0。首次自动启动被 iOS 明确以 `Locked` 拒绝，最终安装后的启动重试又遇到 CoreDevice 网络隧道超时；解锁并保持设备在同一网络后需重试启动，再人工验收左上四按钮、选牌和一局关键流程。

## 目标

按用户给出的八项问题完成 Godot 原生 UI 修复，并把经过实机渲染验证的状态同步到 Figma 正式稿。

## 当前状态

八项修复已实现，Figma 正式稿已同步；第二批牌面阴影、选牌/最新弃牌反馈和动作按钮动画也已实现。用户随后用最终规格指出“大团花”和“大圆形亮斑”不应存在，最终会所质感已经校正。最新一轮继续完成 AI 提示窗、回合语义、弃牌河、视觉状态冲突和低透明度可读性的交互收口。定向与扩大回归全部通过。当前工作尚未提交 Git；大量既有未跟踪 `evidence/` 文件保持原样，未清理。

## 第三批牌局交互收口

1. AI 提示窗默认变成 560×76 的紧凑提示条，明确点击后才展开为 760×264 的详细面板；默认位置动态贴在玩家手牌上方，不再遮住弃牌河。
2. AI 窗透明度继续支持 0%–100%，但透明度只改变容器背景；文字与必要轮廓保持可读。背景低于 22% 时自动增加文字描边和局部暗底，避免 8% 透明度时信息消失。
3. AI 窗布局偏好升级到版本 2：旧版本保存的拖拽坐标只迁移一次，防止旧坐标把新版紧凑条重新放回弃牌区。
4. 回合语义统一：正常阶段显示“某家出牌中”，本家有碰/杠/胡/过响应时显示“等待本家响应”，自摸操作阶段显示“本家操作中”；对应玩家框同步显示“出牌 / 响应 / 操作”徽标。
5. 四家弃牌河改为固定起点、固定流向；不再因一行牌数变化而反复重新居中，弃牌归属和阅读方向更稳定。
6. 牌面状态拆分：新摸牌使用玉绿角标，选中牌使用旧铜描边与抬升，危险牌使用朱砂底线，AI 推荐使用金玉标记，最新弃牌继续使用铜色菱形和箭头，不再全部依赖红色高亮。
7. 对手牌背降低内高光和装饰亮度；桌面暖光、环境补光以及锦纹重复节奏进一步收敛，减少牌背抢眼和暗纹“壁纸化”。
8. 展开的 AI 详细面板属于用户主动打开的检查态，允许短时覆盖部分公共牌区；默认紧凑态不遮挡牌局。

## 第二批质感增强

1. 蜀锦暗纹：最终规格明确禁止大花纹，因此移除八角团花，只保留回纹、云雷纹、菱格锦纹、卷草纹和缠枝纹的小尺度重复层；总墨量为 6.5%，明暗双层形成织压纹。
2. 环境灯光：移除桌面内部的径向椭圆光源，改为左上方向性暖主光、中部克制补光、右下沉降和边缘暗角；画面不再出现圆形亮斑。
3. 牌面阴影：加强环境阴影与接触阴影；被选中的牌保留在牌架上的落地阴影，避免悬浮感失真。
4. 选牌反馈：选中牌抬升 18 px，同时使用朱砂/铜色双描边和底部铜色键线，不仅依赖颜色。
5. 最新弃牌反馈：加入铜色菱形和箭头标记，保留轻微脉冲；减少动态效果时停止脉冲但不隐藏静态标记。
6. 动作按钮动画：加入不超过 200 ms 的入场、悬停、焦点和按压反馈；动画仅作用于按钮内部非交互高光层，不改变父按钮触控区；减少动态效果时即时复位。
7. 麻将牌与视觉层级：牌面统一为暖象牙色程序化材质，牌背使用三段深玉绿；“已胡”状态不再把自己的整排手牌压成灰色，仍保持牌面第一视觉权重。
8. 面板与按钮：深青黑切角面板、旧铜细边继续保留；动作按钮黄铜边统一收敛为 2 px。

## 八项结果

1. 蜀锦暗纹：提高 Shader 和矢量锦纹强度，保留回纹、云雷纹、菱格、卷草、缠枝五类语汇。
2. 光照：最终将 Shader `light_strength` 收敛为 `0.185`；使用左上方向性暖光、中部补光、右下沉降和暗角，不再叠加径向矢量光斑。
3. 四家牌面：自己手牌按高度保持可读尺寸，横向压力改用牌面叠放；上/下家与对家牌面同步放大。
4. 明牌：统一到共享 `TableUtilityBar`，真实点击区会调用现有明牌状态逻辑并保存偏好。
5. 左上工具：默认缩进，仅保留“展开”；展开后显示 AI提示、难度、明牌和退出，所有按钮使用原生信号和 76 px 触控区。
6. 姓名框：上家和下家姓名框按对应左右牌列的实际中心动态定位。
7. AI 窗：拖动热区只保留标题，按钮和滑杆不再被拖动事件吞掉；拖动后可继续展开和调节。
8. 透明度：运行时、场景、偏好加载与测试统一为 `0.0..1.0`，界面显示 0%–100%。

## Figma

- 文件：`https://www.figma.com/design/qTtaVjiYrKK9eHfC0xKJaF`
- `01 正式牌桌`：2048×1152 Godot 实机截图、八项验收说明与移动端硬指标。
- `02 交互与尺寸规范`：工具栏展开/缩进、AI 窗拖动与透明度、牌面尺寸、姓名居中、蜀锦/光照参数。
- Figma 文件原为空白，未发现可复用组件、变量或样式；本轮以正式屏幕和可编辑交互规范为交付，不建立完整发布型组件库。

## 关键变更

- `scripts/ui/TableMaterialOverlay.gd`
- `shaders/table_club_felt.gdshader`
- `scripts/ui/HandCanvas2D.gd`
- `scripts/ui/PlayerUI.gd`
- `scripts/ui/table/TableUtilityBar.gd`
- `scripts/ui/table/TableActionBar.gd`
- `scripts/ui/table/TableDiscardLayer.gd`
- `scenes/ui/table/TableUtilityBar.tscn`
- `scripts/ui/AIAssistant.gd`
- `scenes/ui/AIAssistant.tscn`
- `scripts/game/MainSceneV2.gd`
- 五个 UI 合同/回归测试文件

## 验证证据

- 11 个 Godot UI/布局/资源/结算/动画回归脚本全部 exit 0。
- `dotnet build SichuanMahjong.Godot.sln --nologo`：0 warning，0 error。
- Metal 实机截图矩阵：1365×768、2048×1152、2400×1080、2556×1179 全部生成。
- `python3 tools/check_ui_capture.py evidence/ui_eight_fixes_20260716`：4/4 通过；无黑边、透明空洞或桌面覆盖缺失。
- `git diff --check`：通过。

第二批补充验证：

- 9 个 Godot UI/布局/触控/动画回归脚本全部 exit 0。
- `dotnet build SichuanMahjong.Godot.sln --nologo`：0 warning，0 error。
- Metal 截图矩阵再次覆盖 1365×768、2048×1152、2400×1080、2556×1179。
- `python3 tools/check_ui_capture.py evidence/ui_second_batch_20260716`：4/4 通过；无黑边或透明空洞。
- 该阶段曾验证大团花暗纹；用户最终规格明确否定大花纹后，已由后续最终校正移除。牌面接触阴影、选中牌与最新弃牌标记仍保留并通过回归。
- 回归曾发现按钮入场缩放会临时缩小真实触控区，已改为内部高光层动画；修复后触控合同测试通过。

最终规格校正验证：

- 9 个 Godot 视觉、触控、动画、布局和既有功能回归脚本全部 exit 0。
- `dotnet build SichuanMahjong.Godot.sln --nologo`：0 warning，0 error。
- `evidence/ui_final_spec_20260716/` 生成四档 Metal 截图；`check_ui_capture.py` 4/4 通过。
- 2048、1365、2400 档人工复核确认：无大团花、无圆形亮斑；左上空桌亮度高于中央，中央高于右下；暖象牙牌面和深玉绿牌背保持第一视觉层级。
- `table_2048x1152_clean.png` 是隐藏 AI 辅助浮窗的展示截图，仅用于清楚验收牌桌材质和视觉层级，不改变运行时 AI 窗功能。

第三批交互收口验证：

- 10 个 Godot UI、布局、触控、响应动作、状态语义和视觉合同脚本全部 exit 0。
- `dotnet build SichuanMahjong.Godot.sln --nologo`：0 warning，0 error。
- `python3 tools/check_ui_capture.py evidence/ui_completion_20260716`：1365×768、2048×1152、2400×1080、2556×1179 共 4/4 通过；无左右空洞或异常黑边。
- 人工复核默认紧凑态和展开态：紧凑条在手牌上方且不遮弃牌河；响应按钮、中央状态和本家徽标语义一致；选中、新摸、危险、推荐和最新弃牌标记可区分。
- `git diff --check`：通过。

## 证据路径

- `evidence/ui_eight_fixes_20260716/`
- `evidence/ui_eight_fixes_20260716/capture_check_report.json`
- `evidence/ui_second_batch_20260716/`
- `evidence/ui_second_batch_20260716/capture_check_report.json`
- `evidence/ui_final_spec_20260716/`
- `evidence/ui_final_spec_20260716/capture_check_report.json`
- `evidence/ui_completion_20260716/`
- `evidence/ui_completion_20260716/capture_check_report.json`

## 边界

- 未修改 AI 决策、计分、牌局规则或打包配置。
- 未调用 ImageGen/Grok：本轮的暗纹与光照适合程序化 Shader/Control，实现更清晰、可缩放且不产生额外 API 费用。
- Godot headless 测试退出时仍打印项目既有的 CanvasItem/ObjectDB 清理警告，但所有测试退出码为 0，构建无警告；这不是本轮功能失败。

## 下一步

当前牌桌已按用户最终视觉规格与牌局交互层级收口。下一阶段可以做结算页、设置页和大厅视觉统一；若要交付移动安装包，再单独执行 Android/iOS 导出与真机流程验证。

## 恢复提示

继续四川麻将 UI 时，先查看本文件和 `git status --short`。不要删除既有 `evidence/`；八项修复、第二批反馈增强、最终会所质感和第三批交互收口已经通过回归。不得恢复大团花或桌面内部径向光斑；不得让 AI 提示窗默认展开或重新遮住弃牌河；后续修改必须重跑触控、动画、视觉、布局、回合语义核心合同测试和截图矩阵。

---

# 2026-07-16 左右手牌居中与 iPhone 真机交付

## 本轮目标与实现

- 上家、下家的竖向手牌按“实际牌组内容”在各自完整侧边轨道中垂直居中，而不是仅移动整条布局容器。
- `PlayerUI.gd` 新增固定实际内容盒 `SideHandGrid`，根据手牌行数计算内容高度和居中 Y；副露数量 0–4 组时均保持内容中心与座位宿主中心一致。
- `SichuanTableMetrics.gd` 将左右手牌轨道放到对应玩家信息框朝向桌心的一侧，并保留 8–12 px 安全间距，避免居中后与玩家信息框重叠。
- `SichuanDenseTableLayoutRunner.gd` 增加左右座位、0–4 组副露的中心误差合同测试，允许误差不超过 1 px。

## UI 验证

- Dense layout、Premium visual、TableLayout、UiSixIssues（7/7）、Restored UI、TableTouchTarget 六组 Godot 回归全部 exit 0。
- `dotnet build SichuanMahjong.Godot.sln --nologo`：0 warning，0 error。
- 1365×768、2048×1152、2400×1080、2556×1179 四档截图全部生成；`evidence/ui_side_hands_centered_20260716/capture_check_report.json` 为 4/4 通过。
- 2048×1152 和 1365×768 人工复核：左右手牌内容垂直居中，且未侵入玩家信息框。

## iOS 导出、签名与安装

- 已在 `export_presets.cfg` 增加正式 iOS preset；Godot 4.6.2 C# iOS 的正式 CLI 导出会因实验性平台检查阻断，因此使用项目现有直接导出脚本生成 Xcode 工程。
- `tools/export_ios_xcode_project.sh` 会兼容本机导出模板位于嵌套 `templates/ios.zip` 的情况，并在 Godot 预期位置建立缺失的兼容符号链接。
- Xcode 工程：`build/ios/SichuanMahjong-2.2.0-ios-xcode/SichuanMahjongIOS.xcodeproj`
- 签名 App：`build/ios/DerivedData/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`
- Xcode Release 真机构建：`BUILD SUCCEEDED`。
- Bundle ID：`com.chendong.sichuanmahjong.iosdev`；版本/构建号：`2.2.0 / 2.2.0`。
- 签名：`Apple Development: donachen2027@outlook.com (MN54CC5STV)`；Team：`FCB4ZVWWD8`；`codesign --verify --deep --strict` 通过。
- App 主程序和 NativeAOT framework 均为 arm64；源码 PCK 与 App 内 PCK 的 SHA-256 一致：`af2abb6ad01ebec2eda26fc6ad8ee754bd2e317225fd654b998010bb789e381b`。
- 已安装到 `dona‘s iPhone`（device id `516E99D2-18B6-5DD8-94E1-6993510A036D`），`devicectl` 返回安装成功。
- 已在真机启动，安装数据库中版本为 2.2.0，进程 PID `16938`，运行路径位于真机 `SichuanMahjongIOS.app/SichuanMahjongIOS`。
- 当前最强验证证据覆盖：签名构建成功、真机安装成功、真机启动成功、进程存活。真机内完整牌局和触控体验仍需用户现场打一局确认。
- 当前个人开发描述文件到期时间为 2026-07-20 12:32:52 UTC（北京时间 2026-07-20 20:32:52）；到期后需要重新签名、构建和安装。

## 当前边界更新

- 上一节“未修改打包配置”只适用于此前 UI 阶段；本轮已明确修改 iOS 导出配置和导出脚本。
- 直接 iOS 导出生成的 PCK 约 241 MB，功能交付不受影响，但尚未完成包体裁剪；后续如需长期分发，应单独优化导出过滤和包体。

---

# 2026-07-17 透视地狱老手算法恢复与 2.2.1 iPhone 交付

## 用户目标

- 复核所有四川麻将历史需求，确认“透视地狱”是否仍按教程训练出的老手算法出牌。
- 修复用户观察到的 AI 明显降智，并重新打包、安装到 iPhone。

## 历史合同与根因

- 难度合同：`hell` 是最强透视模式，三家 AI 可共享其他 AI 手牌、本家手牌与精确牌墙；`bone_ash` 是最强公平模式。
- 老手决策顺序：合法/定缺强制 > 胡/成叫 > 净分期望 > 最大威胁攻防 > 路线连续性 > 活张 > 单次杠分。
- 根因不是透视 payload 丢失：`GameState` 已正确传入 `allHands`、`exactWall`、当前积分和人类座位。
- 真正问题是 `SichuanHellChallengeEngine` 使用独立简化评分，只覆盖向听、活张、形状与透视围剿；它绕开了正式 `SichuanDecisionEngine`、净分期望、清一色路线规划、连续牌脑和统一决策层。
- 透视反应引擎另以 `forceLightweight=true` 调用公平反应，并使用独立牌脑，导致出牌和碰/杠/胡/过两个通路可能互相矛盾。

## 修复

1. `SichuanHellChallengeEngine` 每次出牌先调用完整 `SichuanAiFacade.DecideDiscard`，合法候选严格取老手候选集合，再叠加精确透视防点炮、阻止本家胡牌和三家协同约束。
2. 将老手分数、候选排名、路线、净分期望与刻子拆分标记接入透视排序；返回解释统一带“老手主线”。
3. `SichuanHellChallengeReactionEngine` 改用完整反应路径，取消轻量化旁路。
4. `SichuanCSharpRuntime` 让普通、透视出牌与透视反应共用同一 `SichuanAiFacade`，连续牌脑可跨出牌/反应保存“主动拆刻”等资产记录。
5. C# runtime 补充路线、净分期望与刻子保护诊断字段，供 Godot 实战审计逐张核对。
6. 版本更新为 `2.2.1`；Android `versionCode=221`，iOS 短版本和构建号均为 `2.2.1`。

## 新增防降智回归

- 明显清一色牌例：透视地狱继承老手清一色路线并先清异门。
- 刻子保护牌例：老手不拆刻时，透视地狱不以简化评分强行拆刻。
- 定缺合法牌例：透视候选集合只包含缺门牌。
- 连续牌脑牌例：上一手记录“主动拆刻 4”后，反应阶段拒绝把同一张重新碰回。

## 验证

- `dotnet build SichuanMahjong.Godot.sln -c Release --nologo`：0 warning，0 error。
- C# 全量 Smoke：exit 0；新增四组回归、19 组 PDF 黄金牌例全部通过；精确牌形穷举 131,841 状态无失败。
- Godot C# 移动/AOT 合同：29/29；透视紧凑 AOT 返回正常。
- 四川规则回归：32/32。
- 独立裁判回归：通过。
- 100 局透视地狱实战审计：100 局全部自然结束，`forced_stop=0`，平均 65.12 步；5,403 次实际出牌全部与 C# 决策一致、全部带老手主线和路线诊断；平均老手裁判分 98.89，Top-1 84.82%，严重错误率 0.46%，点炮率 21.25%，路线切换率 1.42%。
- 审计证据：`evidence/ai_old_hand_restore_20260716/100_rounds_hell_final.json`、同目录 CSV 和逐张 JSONL/CSV。
- `git diff --check`：通过。

## iOS 2.2.1 产物

- Xcode 工程：`build/ios/SichuanMahjong-2.2.1-ios-xcode/SichuanMahjongIOS.xcodeproj`。
- 签名 App：`build/ios/DerivedData-2.2.1/Build/Products/Release-iphoneos/SichuanMahjongIOS.app`。
- Xcode Release：`BUILD SUCCEEDED`。
- Bundle ID `com.chendong.sichuanmahjong.iosdev`；版本/构建号 `2.2.1 / 2.2.1`。
- 主程序与 `SichuanMahjong.Godot.framework` 均为 arm64；`codesign --verify --deep --strict` 通过。
- 签名为 `Apple Development: donachen2027@outlook.com (MN54CC5STV)`，Team `FCB4ZVWWD8`。
- 描述文件到期时间仍为 2026-07-20 12:32:52 UTC（北京时间 2026-07-20 20:32:52）。

## 当前真机状态与下一步

- 目标设备 `dona‘s iPhone` / iPhone 15 / CoreDevice ID `516E99D2-18B6-5DD8-94E1-6993510A036D` 已配对、开发者模式开启，但当前 `tunnelState=unavailable`。
- `xcdevice` 返回设备不可用，并明确要求设备解锁、连接数据线或与 Mac 处于同一局域网；当前 USB 枚举也未发现 iPhone。
- 设备恢复在线后的下一条操作：用 `devicectl device install app` 覆盖安装 2.2.1，再用 `devicectl device process launch --terminate-existing` 启动并核对设备安装数据库版本和进程。
- 真机安装/启动完成前不得把本轮交付写成“已安装”；当前最强证据为源码/算法/100 局审计/NativeAOT/签名 App，尚差设备外部状态这一层。

---

# 2026-07-17 左上工具栏 iOS 触控稳定性修复与 2.2.2 安装

## 用户原始反馈

- “现在这个版本，安装到手机上，左上角的按钮（缩放和内部的3个按钮），还是有时候能按动，有时候按不动。”
- 本轮 P0 是展开/缩进入口，以及内部 AI 提示、难度、明牌三个按钮在 iPhone 上每次点击都只执行一次且确实生效。

## 根因与修复

- `MainSceneV2._input` 原先同时接收 `InputEventScreenTouch` 与 `InputEventMouseButton`，两者都会直接调用左上工具栏的显式点击分发。
- iOS 会为同一次物理触摸再生成模拟鼠标事件，导致开关执行两次；展开后立即缩进、AI/明牌开后立即关，视觉上表现为偶发按不动；难度则可能一次跳两档。
- 修复在 `scripts/game/MainSceneV2.gd`：仅对短时间内同按钮的 touch/mouse 跨来源第二事件去重并消费；连续同来源真实点击不节流。
- `tests/current/SichuanRestoredUiRegressionRunner.gd` 新增 iOS 双事件压力合同和连续真实触摸合同。
- 修复前新合同稳定失败：`iOS 双事件第 1 轮：展开按钮被同一触摸执行两次`；修复后通过。

## 验证

- Restored UI 单进程内部连续 12 轮覆盖展开、AI 提示、难度、明牌、缩进；独立启动 10 次全部 exit 0，共 600 次双事件按钮动作。
- 连续真实触摸两次均立即生效，没有被去重误伤。
- TableTouchTarget、UiSixIssues 7/7、TableMotion、TableLayout、PremiumVisual 五组相关回归全部 exit 0。
- `dotnet build SichuanMahjong.Godot.sln -c Release --nologo`：0 warning，0 error。
- 版本更新为 2.2.2；Android 配置为 222/2.2.2，但本轮未导出 Android。
- Godot iOS Xcode/NativeAOT 导出成功，Xcode Release `BUILD SUCCEEDED`。
- 签名 App 版本/构建号 2.2.2/2.2.2，Bundle ID `com.chendong.sichuanmahjong.iosdev`，主程序和 C# framework 均为 arm64，`codesign --deep --strict` 通过。
- Xcode 工程 PCK 与 App 内 PCK SHA-256 一致：`e38edbc499f26cd5ca5a3d5ee4a26f53228c249ca3dfec9652d921a85adf8e7d`。

## 真机交付

- 已覆盖安装到 `dona‘s iPhone`（iPhone 15，CoreDevice ID `516E99D2-18B6-5DD8-94E1-6993510A036D`）。
- `devicectl device info apps` 已确认真机安装数据库为版本/构建号 2.2.2/2.2.2。
- 自动启动首次验证被 iOS 明确拒绝，原因是设备锁屏；解锁后需重试启动并记录 PID/进程。
- 个人开发描述文件到期为 2026-07-20 12:32:52 UTC（北京时间 20:32:52）。
- 最后仍需用户在真机连续点击展开/缩进与三个内部按钮，并完成一局人工手感验收。

---

# 2026-08-10 老手 AI 牌墙后验、碰杠反事实改进与 2.6.36 iPhone 交付

## 本轮完成

- 公平 `bone_ash` 继续作为默认老手基线；`hell` 保持透视围剿挑战语义。
- 增加严格守恒到墙余张数的牌墙后验，禁止把对手暗手当成可摸活张；墙空直接进入精确查叫终值。
- 修正过、碰、杠后距离自己下一摸的座次时序，各动作共用同一批确定性牌墙样本和统一动作净值。
- 弃牌层接入墙约束活张、互斥近优牌形、四/五对子路线弹性与尾盘受限前瞻；前瞻不再污染早中盘。
- CLI/iOS Top-K 诊断增加统一动作净值与策略残差。

## 验证与交付

- C# 完整 smoke 通过，19 组 PDF 黄金牌例通过，131,841 精确牌型穷举无失败。
- Godot 规则 34/34、C# runtime 合同 29/29、独立裁判回归通过；实际 runtime 5 局全部自然结束、0 强制结束。
- 两组独立 500 状态配对擂台均通过 promotion；盲测弃牌后悔值 0.1728 对基线 0.1990，碰杠后悔值 0.4223 对基线 1.5875，一致率 100%。
- iOS 2.6.36 Release 已构建，App 主程序和 NativeAOT 均为 arm64；IPA 完整性通过。
- 已覆盖安装到局域网内已配对 iPhone 15，设备数据库回读版本/构建号 2.6.36/2.6.36，并成功启动。
- 个人开发描述文件有效至 2026-08-14 20:36:34 CST。
- 证据：`evidence/2.6.36_ios_release_20260810/ai_verification_report.md`、`release_report.md`。

## 仍需长期观察

- 真机真人多局净分与争议动作仍应通过公开信息 trace 做离线盲测；不要以单局输赢在线调权。
- 本轮未新增用户未指定的固定回归清单。

---

# 2026-08-10 老手 AI 交叉验证进化闭环与 2.6.37

## 本轮完成

- `bone_ash` 继续作为公平老手基线，`hell` 继续隔离为透视围剿挑战。
- 增加不读取线上候选分数的交叉验证动作裁判；所有弃牌、过、碰、杠和胡候选共享同一批后验暗手/牌墙粒子并按权重聚合，选择与验证样本分离。
- 冻结策略入口不再先执行当前策略，避免回合大脑和缓存污染；线上同速近分弃牌改由精确互斥牌形损失收口，并禁止近分回退增加拆对子。
- 新增 ridge 影子训练、冻结策略联盟、单座候选对三家冻结基线的四座轮换同牌墙擂台与完整账本核验；影子模型未通过盲测，保持离线。

## 最终验证

- 加权 500 状态盲测：弃牌后悔值 0.09033 对 0.09049，改善 0.000155，95% CI [-0.00105, 0.001905]，通过 0.002 非劣界；碰/过/杠后悔值 0.22751 对 0.79498；严重错误 0，一致率 100%。
- 160 局整局擂台全部自然结束、账本错误 0；候选平均净分 +0.14375，95% CI [-0.39375, 0.68125]，趋势为正但未达到统计显著。
- 512 状态 ridge 影子模型后悔值 0.05807，高于现有 0.05449；Top-1 61.7%，低于现有 66.4%，已拒绝晋级。
- C# 构建 0 warning / 0 error；AI Core smoke、19 组 PDF 黄金听口、131,841 精确牌形穷举通过；Godot 规则 34/34、C# 合同 29/29、独立裁判与版本一致性通过。
- 证据：`evidence/2.6.37_ai_evolution_20260810/ai_verification_report.md`。

## 外部研究

- 四个用户指定 GitHub/Gist 项目已下载到 `research/external_mahjong_ai_20260810/` 并完成静态学习；两篇论文按公开官方题录、摘要和目录核验，未把登录受限全文内容冒充已读。
- 结论见 `research/external_mahjong_ai_20260810/学习结论.md`；第三方源码不并入产品，未授权项目不复制代码。
- 本轮未新增用户未指定的固定回归清单。
