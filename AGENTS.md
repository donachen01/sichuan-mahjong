# 四川麻将新版工程 Agent 规则

本文件保存四川麻将新版工程专属规则。全局 `/Users/chendong/.codex/AGENTS.md` 只保留跨项目通用协作和验证纪律；内江麻将工程的 iOS 改造经验可借鉴，但必须先改写为四川麻将规则、AI 和交付边界，不能直接照搬内江玩法逻辑。

## iOS 真机自用包借鉴规则

本节从内江麻将 iOS 改造复盘提炼而来，用于后续四川麻将 iOS 自用包开发。目标是支持 iPhone 通过 Xcode 个人开发者模式安装使用，不走 App Store，不购买 Apple 开发者年费。

### 需求边界

- iOS 交付不是“导出一个文件”就结束，而是“当前四川麻将新版工程在 iPhone 上可安装、可启动、可玩关键流程”。验收必须至少覆盖：Xcode 个人开发者签名、iPhone 开发者模式、真机安装、真机启动、中文 UI、基础出牌、碰/杠/胡/过等反应、四川规则结算、AI 同步决策、不买年费、个人开发者证书周期限制。
- 个人开发者安装使用 `Apple Development` 签名，不是 App Store/Distribution 签名。若 Xcode Release 配置默认 `Apple Distribution`，构建会失败，应明确覆盖或修正签名设置。
- 本机 `xcode-select` 可能仍指向 `/Library/Developer/CommandLineTools`。构建命令必须验证 `xcodebuild -version`，必要时显式设置 `DEVELOPER_DIR=/Users/chendong/Downloads/Xcode.app/Contents/Developer`。
- Godot iOS export template 不能只看路径存在。必须用 `zipinfo` 或 Python `ZipFile` 校验 `ios.zip` 完整性、大小和条目；模板损坏时从官方 Godot release 重新下载。
- iPhone 个人开发者安装通常有短周期授权限制。最终说明要讲清楚重新安装/重新信任/重新授权的现实边界，不能让用户误以为是永久 App Store 包。

### 内江经验哪些可以借鉴

- 可以借鉴成熟工程结构：Godot 前端、C# AI/规则引擎、测试 runner、导出脚本、移动端资源打包、真机验证分层。
- 可以借鉴 iOS 打包流程：清理旧 NativeAOT、导出 Xcode 工程、检查 `ios-arm64` framework、Xcode 真机构建、devicectl 安装和启动检查。
- 可以借鉴同步 AI 边界：实时玩法决策必须以最新状态调用 C# 后端，不允许前端凭旧状态或超时逻辑替代核心 AI 决策。
- 可以借鉴问题矩阵：用户列出多个 UI/玩法/打包问题时，每项必须独立跟踪状态和验证证据。

### 内江经验哪些不能照搬

- 不能把内江麻将的报叫、报杠、两门牌、内江结算、内江地狱挑战策略直接迁移为四川规则。
- 四川麻将要以四川规则为准：定缺、血战到底、查叫、花猪、退杠、呼叫转移、杠分、番型、胡牌限制、流局结算等必须由四川规则文档和四川测试确认。
- 内江的“开局报叫/报杠不卡”测试不能作为四川碰杠胡反应的证据。四川需要自己的定缺、碰、杠、胡、过、血战继续、查叫结算回归。
- 内江 AI 的牌型判断、进攻防守、地狱模式读牌策略只能作为框架参考，不能在未验证四川番型和结算收益的情况下直接复用权重。

### Godot/.NET/iOS 技术边界

- Godot 桌面测试通过不等于 iPhone 可运行。必须分层验证：桌面 Godot 测试、真实 C# runtime 测试、iOS NativeAOT framework 产物检查、Xcode 真机构建、设备安装、设备启动和关键玩法场景。
- C# Godot 项目导出 iOS 时，NativeAOT framework 是关键产物。重新导出前要清理 `.godot/mono/temp` 里的旧 iOS NativeAOT 输出，避免 Xcode 工程打进旧动态库。
- 导出后要用 `strings` 或等价方式检查 `ios-arm64` framework 是否含有本轮关键符号。只看源码或桌面 DLL 不够。
- NativeAOT 下 `System.Text.Json` 反射序列化/反序列化可能在真机暴露问题。关键 iOS C# 入口应优先用源生成或 `JsonDocument` 手动解析，输出用手写紧凑 JSON 或 AOT 安全方案。
- iOS 上缺失或陈旧的 `.NET AOT xcframework` 会导致启动闪退或 C# runtime 入口异常。每次重新打包都要确认 framework 被 Xcode project 嵌入并随 app 签名。

### 四川麻将同步 AI 和玩法验证

- 若项目采用 C# 后端决策，出牌、定缺、碰、杠、胡、过、血战继续、查叫结算等关键动作必须用实时最新状态同步计算；不允许用超时自动动作、旧状态缓存、前端简化逻辑掩盖后端失败。
- 如果同步失败，优先查 payload、Native runtime、AOT 产物、Xcode 打包和最近改动；不要用 fallback 让表面流程跑通。
- 假 AI / mock 只能验证 Godot 执行动作函数，不能证明真实 C# runtime 和 iOS 真机链路可用。关键玩法必须补真实 runtime 场景测试。
- 四川麻将 iOS 回归至少应覆盖：
  - 开局定缺后能进入正常出牌。
  - 玩家和 AI 出牌后，碰/杠/胡/过反应不阻塞。
  - 杠后补牌、抢杠、退杠或呼叫转移规则按四川规则执行。
  - 血战到底有人胡后牌局继续，剩余玩家正常摸打。
  - 流局查叫、花猪、杠分、最终结算能完成并显示。
  - 中文 UI、玩家名、积分、庄/风位、关闭按钮和菜单在 iPhone 上正常显示。

### 四川麻将 iOS 正确工作流

1. 先锁定四川规则边界：采用哪套四川麻将规则、是否血战到底、是否含查叫/花猪/退杠/呼叫转移、AI 决策归属在哪里。
2. 建问题矩阵：UI、规则、AI、打包、真机安装、性能分别跟踪，不得只修其中一项就宣称完成。
3. 建已知可运行基线：如果用户说“上一版能运行”，先确认上一版产物、最近改动和退化点。
4. 先桌面验证，再 iOS 验证：Godot/C# 测试通过后，重新导出 iOS，检查 arm64 AOT 符号，再真机构建、安装、启动。
5. 对玩法 bug，必须区分“执行函数正确”“真实 C# 决策正确”“iOS AOT 返回正确”“真机 UI 可继续操作”四层证据。
6. 最终报告逐项关闭：已修什么、如何验证、最强证据到哪一层、哪些需要用户真机手动触发、哪些需要后续自动化补齐。

## 回归清单策略

- 固定回归清单只能由用户明确指定。用户未指定时，Agent 不得自行创建、扩充、维护或默认执行固定清单，也不得以脚本、CI、计划或文档表格变相重建。
- 保留现有单项测试。每次只选择与当前修改和风险直接相关的聚焦验证，并在执行前说明范围；本次选择不自动成为后续任务的默认清单。
- 历史报告和证据中的既有测试结果是历史事实，不因清单策略变化而删除或改写。
