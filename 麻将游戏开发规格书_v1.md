# 麻将游戏开发规格书 v1

## 文档状态

本文档是项目非常早期的通用麻将规格草案，当前已**不适用于本项目现状**。

主要过期点：

- 本文档按 136 张基础麻将牌编写
- 含风牌、箭牌、吃牌等通用麻将设定
- 与当前项目的“四川麻将血战到底、108 张、不可吃、定缺、查叫等规则”不一致

当前项目请不要再以本文档作为实现依据。

当前应改看：

- 规则基线：`/Users/chendong/Documents/New project/四川麻将项目规则基线_最终版_v1.md`
- AI 规则：`/Users/chendong/Documents/New project/res/docs/ai/四川麻将AI规则_v1.md`
- 规则测试对照：`/Users/chendong/Documents/New project/规则文档_判定器_测试对照清单_v1.md`

## 1. 文档目标

本规格书用于将“麻将游戏项目”整理为可直接进入设计、拆分任务、交付 AI 编程工具生成代码的开发规格。

当前阶段目标：

- 先冻结一套可运行的简化版规则
- 优先保证回合流程、状态机、判定逻辑、结算链路清晰
- 暂不追求地方麻将全覆盖
- 先支持单机对局或本地房间对局，再逐步扩展联网、战绩、商城等能力

本版本默认采用：

- **四人麻将**
- **简化国标/常见家用麻将思路**
- **支持基础吃碰杠胡**
- **不引入复杂番种体系**
- **以“能玩通一局、逻辑稳定、便于 AI 和程序实现”为首要目标**

---

## 2. 版本范围

### 2.1 V1 可运行简化版范围

V1 必须实现：

- 四人一桌
- 136 张牌基础牌库
- 庄家、发牌、摸打轮转
- 吃、碰、明杠、暗杠、补杠、胡
- 听牌判断
- 向听数基础计算
- 单局结算
- 基础 UI
- 三档 AI 行为

V1 暂不实现：

- 地方规则切换
- 复杂番型与番数叠加
- 花牌、财神、癞子、宝牌、买马
- 血流成河、换三张、缺一门等特殊玩法
- 断线重连、多房间大厅、充值、社交系统
- 完整战绩统计、录像回放

### 2.2 V2 预留扩展方向

- 规则配置化：牌库、可否吃、番种、结算倍率
- 多玩法模式：国标、广东、四川、日麻简化等
- 联机房间
- 战绩与回放
- 高级 AI
- 教学模式与托管模式

---

## 3. 核心设计原则

### 3.1 简化版规则优先原则

如果传统麻将规则存在地区差异，V1 优先选择：

- 更容易实现
- 更容易被玩家理解
- 更利于状态机稳定
- 更适合后续规则配置化扩展

### 3.2 统一裁定原则

所有判定应由**服务端逻辑层 / 核心规则引擎**统一裁定，UI 仅负责展示和输入，不得自行决定合法性。

### 3.3 可回放状态原则

每一步操作应形成明确事件记录，便于：

- 调试
- 回放
- AI 训练
- 对战纠纷复盘

---

## 4. 牌库规则

### 4.1 牌库组成

V1 采用 136 张基础麻将牌：

- 万子：1 万 - 9 万，各 4 张，共 36 张
- 筒子：1 筒 - 9 筒，各 4 张，共 36 张
- 条子：1 条 - 9 条，各 4 张，共 36 张
- 风牌：东、南、西、北，各 4 张，共 16 张
- 箭牌：中、发、白，各 4 张，共 12 张

合计：136 张

### 4.2 不纳入 V1 的牌

- 春夏秋冬、梅兰竹菊等花牌
- 癞子牌 / 财神牌
- 宝牌 / 赤宝牌

### 4.3 牌表示规范

建议使用统一编码结构，便于规则引擎与 UI 共用：

- `WAN_1` ~ `WAN_9`
- `TONG_1` ~ `TONG_9`
- `TIAO_1` ~ `TIAO_9`
- `FENG_DONG` / `FENG_NAN` / `FENG_XI` / `FENG_BEI`
- `JIAN_ZHONG` / `JIAN_FA` / `JIAN_BAI`

每张实体牌还应包含唯一实例 ID，例如：

- `tileId: 10001`
- `tileType: WAN_3`

说明：

- `tileType` 用于规则判断
- `tileId` 用于动画、日志、发牌跟踪、调试

### 4.4 洗牌与牌墙

- 每局开始时随机洗牌
- 洗牌结果生成牌墙序列
- 牌墙顺序应可记录并回放
- V1 可不做真实“码牌”动画，仅保留逻辑牌墙

---

## 5. 人数与座位布局

### 5.1 人数

- 固定 4 人

### 5.2 座位定义

统一使用逻辑座位编号：

- `0`: 东位
- `1`: 南位
- `2`: 西位
- `3`: 北位

### 5.3 UI 相对座位映射

对于本地玩家视角：

- 自己固定显示在下方
- 下家显示在右侧
- 对家显示在上方
- 上家显示在左侧

逻辑座位与显示座位必须解耦，避免后期联机时出现混乱。

### 5.4 回合顺序

- 按东 -> 南 -> 西 -> 北顺时针轮转
- 即座位号 `(seat + 1) % 4`

---

## 6. 发牌与庄家规则

### 6.1 首局庄家

V1 默认规则：

- 首局固定东位为庄家

建议实现为配置项：

- `dealerSelectMode = RANDOM | FIXED_EAST`

说明：

- 若后续需要更强房间玩法，可开启 `RANDOM`
- 若以单机调试和规则稳定性优先，建议使用 `FIXED_EAST`

### 6.2 发牌数量

- 庄家初始 14 张
- 其余玩家初始 13 张

### 6.3 发牌完成后的状态

- 庄家先手
- 庄家必须先打一张牌

### 6.4 连庄规则

V1 默认采用简化版：

- 庄家胡牌则连庄
- 流局时庄家连庄
- 非庄家胡牌则下家接庄

接庄顺序：

- 当前庄家结束后，由其下家成为新庄

### 6.5 局数控制

V1 默认支持两种模式：

- 单局模式：仅打一局，适合开发调试
- 东风局模式：东一局到东四局

建议先实现单局模式，再扩展东风局模式。

---

## 7. 摸牌 / 出牌流程

### 7.1 回合主流程

单个玩家回合流程定义如下：

1. 若轮到该玩家正常摸牌，则从牌墙摸 1 张
2. 检查该玩家是否可自摸胡
3. 检查该玩家是否可暗杠或补杠
4. 若未胡且未执行杠，则选择打出 1 张牌
5. 广播出牌事件
6. 其他玩家进入响应阶段：胡、杠、碰、吃
7. 若无人响应，则轮到下一家摸牌

### 7.2 出牌合法性

- 只能打出当前自己手牌中的牌
- 若刚摸牌后处于“待操作状态”，必须在允许动作中选择一个
- V1 不支持超时自动打牌，但应预留倒计时接口

### 7.3 响应优先级

多个玩家同时可响应同一张弃牌时，优先级建议如下：

1. 胡
2. 杠
3. 碰
4. 吃

若同优先级多人同时可胡，V1 建议：

- 支持“一炮多响”作为配置项预留
- 默认简化为**只允许最近优先顺位的一名玩家胡**，以减少流程复杂度

配置建议：

- `allowMultipleWinOnDiscard = false`

### 7.4 吃牌限制

- 仅弃牌者的下一家可吃
- 只能使用顺子组合吃牌
- 字牌不可吃

### 7.5 碰牌限制

- 任意其他玩家打出的牌，若手中有两张相同牌，则可碰

### 7.6 杠牌后补摸

- 明杠、暗杠、补杠成功后，需要从牌墙尾部或杠牌补牌区补摸 1 张
- V1 可简化为统一从“补牌指针”摸 1 张
- 牌墙应同时维护“正常摸牌指针”和“杠后补牌指针”

### 7.7 回合状态机建议

建议核心状态定义如下：

- `WAIT_START`
- `DEALING`
- `TURN_DRAW`
- `TURN_SELF_ACTION`
- `TURN_DISCARD`
- `WAIT_RESPONSE`
- `TURN_MELD_RESOLVE`
- `ROUND_SETTLEMENT`
- `GAME_OVER`

---

## 8. 碰、杠、胡规则

### 8.1 碰规则

触发条件：

- 他家打出一张牌
- 当前玩家手中有两张同牌

执行结果：

- 公开组成 3 张刻子
- 该玩家获得出牌权
- 需立即打出 1 张牌

### 8.2 杠规则分类

V1 支持三类杠：

- 明杠：手中已有三张，他家打出第四张
- 暗杠：自己手中有四张相同牌
- 补杠：已有碰组，再摸到第四张补成杠

### 8.3 明杠规则

触发条件：

- 他家打出一张牌
- 玩家手中有三张相同牌

执行结果：

- 形成公开杠组
- 杠后补摸 1 张

### 8.4 暗杠规则

触发条件：

- 当前玩家回合内，手中有四张相同牌

执行结果：

- 形成暗杠组
- 杠后补摸 1 张

### 8.5 补杠规则

触发条件：

- 当前玩家已有碰组
- 当前玩家在自己回合摸到同一张第四张牌

执行结果：

- 将碰组升级为杠组
- 杠后补摸 1 张

注意：

- V1 暂不实现“抢杠胡”，除非后续确认开启

### 8.6 胡牌基础规则

V1 默认采用最基础可胡结构：

- 4 组面子 + 1 对将

面子包括：

- 顺子：连续 3 张数牌，同花色
- 刻子：3 张相同牌
- 杠子：视作 1 组面子

将牌包括：

- 任意两张相同牌

### 8.7 胡牌类型

V1 支持：

- 自摸胡：自己摸到成胡牌
- 点炮胡：他人打出的牌成胡

### 8.8 暂不纳入 V1 的特殊胡型

- 七对
- 十三幺
- 全不靠
- 组合龙
- 十八罗汉
- 地方特殊胡法

说明：

V1 如直接只支持“标准 4 面子 1 将”，可以显著降低判定复杂度，更适合作为第一阶段冻结规则。

### 8.9 胡牌合法性判定原则

胡牌判定必须基于：

- 当前手牌
- 已副露组
- 当前获得牌（自摸牌或他家弃牌）

并由统一函数完成，不允许 UI 层自行拼凑。

---

## 9. 听牌与向听数相关要求

### 9.1 听牌定义

当玩家当前手牌距离胡牌只差 1 张时，记为“听牌”。

### 9.2 向听数定义

向听数表示当前牌型距离合法胡牌结构还差多少步。

V1 要求：

- 至少支持标准型（4 面子 1 将）的向听数计算
- 不要求支持七对、十三幺等特殊向听

### 9.3 听牌查询能力

规则引擎需支持以下接口能力：

- 输入当前手牌，返回向听数
- 输入当前手牌，返回所有可听的弃牌方案
- 输入某个弃牌方案，返回可胡牌牌张列表

### 9.4 UI 展示建议

V1 可选显示，不作为强制玩家可见功能：

- 调试模式显示当前向听数
- 托管 / AI 模式允许读取听牌结果
- 普通玩家模式默认不直接显示所有最佳解，避免过强辅助

### 9.5 AI 使用要求

AI 行为决策必须可读取：

- 当前向听数
- 每次出牌后的向听变化
- 可听牌列表
- 可胡牌张数（有效牌数）

### 9.6 计算性能要求

- 向听数与听牌枚举必须能在单次决策中快速完成
- 目标：单玩家一次完整决策计算时间在可接受范围内
- V1 不强制极致优化，但应避免指数级暴力无缓存实现

---

## 10. 结算规则

### 10.1 V1 结算目标

V1 结算以“简单、稳定、玩家易懂”为目标，不引入复杂番种表。

### 10.2 建议采用的简化结算方式

默认分值建议：

- 平胡自摸：每家 -2，胡家 +6
- 平胡点炮：点炮者 -6，胡家 +6，其余 0
- 明杠：其余三家各 -1，杠者 +3
- 暗杠：其余三家各 -2，杠者 +6
- 补杠：其余三家各 -1，杠者 +3

说明：

- 所有杠分即时结算
- 胡牌分在本局结束时统一结算
- V1 不区分门清、混一色、清一色、碰碰胡等番型加成

### 10.3 庄家加成

V1 默认不设置庄家额外倍率，避免复杂化。

可预留配置项：

- `dealerScoreMultiplier = 1`

### 10.4 流局结算

默认简化：

- 牌墙摸完仍无人胡牌，则流局
- 流局不额外扣分
- 杠分保留
- 庄家连庄

### 10.5 终局分数

若为单局模式：

- 本局结束即展示最终分数

若为东风局模式：

- 按局累计总分
- 东四局结束后按总分排名

### 10.6 排名规则

- 按总分高低排序
- 同分则按座位顺序或共同名次，作为配置项预留

---

## 11. UI 页面与面板清单

### 11.1 页面总览

V1 建议包含以下页面：

- 启动页
- 主菜单页
- 对局页
- 结算页
- 设置页

### 11.2 启动页

用途：

- 展示 Logo
- 加载资源
- 初始化规则配置

### 11.3 主菜单页

功能项：

- 开始单局
- 开始东风局
- 玩家数量展示（固定 4 人，可灰化）
- AI 难度设置
- 设置入口
- 退出游戏

### 11.4 对局页布局

对局页应包含：

- 中央牌桌区域
- 四家手牌 / 副露区 / 弃牌区
- 当前轮到谁的提示
- 剩余牌数显示
- 庄家标识
- 操作按钮区
- 结算触发提示

### 11.5 玩家手牌区

至少包含：

- 自己手牌明牌展示
- 摸到的新牌高亮
- 可出的牌可点击
- 副露组合展示区

### 11.6 其他玩家区域

至少包含：

- 手牌数量显示
- 弃牌区
- 副露区
- 最近动作提示

### 11.7 出牌区 / 弃牌区

要求：

- 每个玩家独立弃牌展示区域
- 按时间顺序排列
- 被吃碰杠的牌建议保留或特殊标识，作为配置项

### 11.8 操作按钮面板

当玩家存在可执行动作时显示：

- 吃
- 碰
- 杠
- 胡
- 过

当无动作时隐藏或禁用。

### 11.9 信息面板

建议包含：

- 当前局数
- 庄家
- 当前剩余牌数
- 当前玩家分数
- 最近动作日志简表

### 11.10 结算页

必须展示：

- 胡牌者 / 流局结果
- 胡牌方式（自摸 / 点炮）
- 各家本局分数变化
- 各家总分
- 下一局 / 返回菜单

### 11.11 调试面板

开发阶段建议提供可开关调试面板：

- 当前状态机状态
- 当前轮到座位
- 当前可执行动作列表
- 向听数
- 听牌列表
- 牌墙剩余信息

---

## 12. AI 行为层级规划

### 12.1 目标

AI 需覆盖从“能出牌”到“有一定策略”的多个层级，便于先做通流程，再逐步增强体验。

### 12.2 AI-L1：随机合法 AI

特征：

- 只保证动作合法
- 有胡必胡
- 有杠可随机杠
- 出牌随机从合法手牌中选择

用途：

- 打通流程
- 测试规则引擎稳定性

### 12.3 AI-L2：基础效率 AI

特征：

- 优先让向听数不变或下降
- 倾向保留顺子潜力和对子
- 对可碰可杠进行简单收益判断
- 听牌后优先保留有效牌数更高的方案

决策指标：

- 向听数最小
- 有效牌张数最大
- 孤张字牌优先舍弃
- 边张 / 坎张价值低于两面搭子

### 12.4 AI-L3：进阶策略 AI

特征：

- 结合牌池信息判断危险度
- 对碰杠进行局势权衡
- 听牌后有简单攻守选择
- 能减少点炮概率

V1 可只实现接口和框架，具体策略后续增强。

### 12.5 AI 决策输入

AI 决策层至少可读取：

- 自己手牌
- 副露信息
- 所有弃牌信息
- 剩余牌数
- 当前向听数
- 候选打牌后的向听变化
- 候选打牌后的有效牌数量
- 当前可响应动作

### 12.6 AI 决策输出

统一输出结构建议包含：

- `actionType`
- `targetTile`
- `confidence`
- `reasonTags`

用途：

- 调试
- 对局日志
- 教学回放

---

## 13. 技术实现边界

### 13.1 技术目标

当前重点不是立即写复杂代码，而是确定一个适合迭代的技术边界，避免后期重构成本过高。

### 13.2 推荐实现形态

建议采用：

- **核心规则引擎与 UI 分离**
- **事件驱动状态机**
- **配置驱动规则**

### 13.3 推荐技术方案

如果目标是快速产出可运行版本，建议优先选择以下两类路线之一：

方案 A：Web 技术栈

- 前端：TypeScript + React
- 渲染：React + Canvas 或 DOM/UI 组件
- 核心逻辑：TypeScript 纯逻辑模块
- 优点：开发快、调试方便、适合 AI 编程工具生成

方案 B：Unity 技术栈

- 客户端：Unity + C#
- 核心逻辑：独立 C# 规则层
- 优点：更适合后续做动画、移动端发布、联网扩展

对于“先做规则冻结 + 简化版可运行”，推荐优先：

- **TypeScript + React + 纯逻辑规则模块**

原因：

- 状态机和规则计算更容易调试
- 适合快速生成、快速改单
- 更方便做自动化测试
- 更适合作为 AI 编程工具的输入目标

### 13.4 模块边界建议

建议拆分为以下模块：

- `rule-engine`: 牌库、发牌、判定、回合推进、结算
- `game-state`: 全局状态、玩家状态、事件日志
- `ai-engine`: 出牌与响应策略
- `ui-scene`: 页面、牌桌、按钮、动画
- `config`: 规则配置、分值配置、局数配置
- `test`: 单元测试、流程回放测试

### 13.5 必须配置化的内容

以下内容不要写死在 UI 里：

- 牌库组成
- 可否吃碰杠
- 是否支持一炮多响
- 是否支持抢杠胡
- 杠分与胡分
- 局数模式
- 庄家规则

### 13.6 V1 不做的技术内容

- 强联网服务架构
- 分布式房间服务
- 账号系统
- 反作弊系统
- 录像压缩优化
- 复杂动画编辑器

### 13.7 测试要求

V1 必须覆盖的测试类别：

- 牌库数量正确性测试
- 发牌后手牌数量测试
- 回合轮转测试
- 吃碰杠胡合法性测试
- 向听数基本案例测试
- 流局测试
- 结算测试

### 13.8 日志与回放要求

每一步建议记录事件：

- 发牌
- 摸牌
- 出牌
- 吃碰杠
- 胡牌
- 分数变化
- 状态切换

事件日志应可支持最小化回放。

---

## 14. 数据结构建议

以下为规格层面的数据要求，不是最终代码实现。

### 14.1 玩家状态

应至少包含：

- 玩家 ID
- 座位号
- 当前手牌
- 副露组
- 弃牌列表
- 当前分数
- 是否庄家
- 是否已听牌
- 是否为 AI
- AI 等级

### 14.2 对局状态

应至少包含：

- 局号
- 当前庄家座位
- 当前轮到座位
- 牌墙
- 正常摸牌指针
- 补牌指针
- 当前阶段状态
- 最近打出牌
- 待响应玩家集合
- 当前结算信息

### 14.3 副露组结构

应支持区分：

- 吃
- 碰
- 明杠
- 暗杠
- 补杠

同时记录来源：

- 哪张牌来自谁
- 由哪位玩家组成

### 14.4 操作命令结构

建议统一操作模型：

- `DRAW`
- `DISCARD`
- `CHI`
- `PENG`
- `GANG_MING`
- `GANG_AN`
- `GANG_BU`
- `HU`
- `PASS`

---

## 15. 待确认项

以下规则在不同麻将体系中容易冲突，当前建议先确认后再正式冻结：

### 15.1 是否允许吃牌

当前文档默认：

- 允许吃牌

待确认原因：

- 有些麻将玩法不允许吃

### 15.2 是否允许一炮多响

当前文档默认：

- 不允许

待确认原因：

- 实现复杂度与传统习惯差异较大

### 15.3 是否支持抢杠胡

当前文档默认：

- 不支持

待确认原因：

- 会影响补杠流程和响应优先级

### 15.4 是否将七对等特殊胡型纳入 V1

当前文档默认：

- 不纳入

待确认原因：

- 会显著增加判定和向听计算复杂度

### 15.5 流局是否罚听

当前文档默认：

- 不罚听

待确认原因：

- 一些玩法流局会进行听牌罚分

### 15.6 庄家是否有额外倍率

当前文档默认：

- 无额外倍率

待确认原因：

- 不同玩法对庄家加成差异明显

### 15.7 流局后是否连庄

当前文档默认：

- 连庄

待确认原因：

- 需要和局数推进规则一起确认

### 15.8 首局庄家决定方式

当前文档默认：

- 固定东位

待确认原因：

- 若后续要提升房间娱乐性，可改为随机决定

### 15.9 是否展示听牌 / 向听数提示给真人玩家

当前文档默认：

- 正常模式不展示，仅调试模式展示

待确认原因：

- 会影响产品定位是“竞技”还是“教学”

---

## 16. 推荐冻结版结论

为了尽快进入开发，建议先冻结以下版本作为 **V1 正式规则**：

- 四人麻将
- 136 张基础牌
- 允许吃、碰、明杠、暗杠、补杠
- 支持自摸、点炮
- 只支持标准胡型：4 面子 + 1 将
- 不支持七对、十三幺、抢杠胡、一炮多响
- 简化结算，不做番种系统
- 流局不罚听，庄家连庄
- 优先做单局模式，再扩展东风局
- AI 先做 L1 与 L2，L3 留接口
- 技术优先采用 TypeScript + React + 规则模块分层

---

## 17. 交付给 AI 编程工具的任务拆分建议

后续可按以下顺序交给 AI 编程工具逐步实现：

1. 创建项目骨架与模块目录
2. 实现牌定义、牌墙、洗牌、发牌
3. 实现对局状态机与回合推进
4. 实现吃碰杠胡判定
5. 实现标准胡型判定与向听数计算
6. 实现简化结算
7. 实现基础对局 UI
8. 接入 AI-L1
9. 接入 AI-L2
10. 增加调试面板与日志回放

---

## 18. 一句话项目定义

这是一个以“四人基础麻将”为核心、采用“规则引擎与 UI 分离”架构、先做简化版可运行对局、再逐步扩展玩法与 AI 的麻将游戏项目。
