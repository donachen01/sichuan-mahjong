# 骨灰级 AI 升级设计方案 v1

## 文档状态

本文档原本是 AI 升级设计稿。

截至 `2026-05-19`，其中多数核心设计已经落地，不再只是规划。当前建议这样使用：

- 看“已落地现状”：请优先参考 `/Users/chendong/Documents/内江麻将工程_20260502_103823_v2/res/docs/ai/内江麻将AI规则_v1.md`
- 看“为什么这样设计”：继续参考本文档
- 后续若新增 AI 能力，优先先改 AI 规则文档，再视情况补本文档设计章节

当前已落地的重点能力：

- 动态攻守策略引擎
- 出牌评分拆分
- 对手画像判断
- 轻量前瞻
- 碰 / 杠 / 过反应决策
- AI 调参面板
- 自动学习与压测统计
- C# 路线规划：平胡、卡二条平胡、暗七对、龙七对、清七对、青龙七对、大对子、清对、清一色
- 强清色胚子识别：单门 `10+` 且异门少时提前进入清一色路线，不再等完全清一色后才估番
- 归 / 四张同牌保护：四张同牌按高番资源保护，避免“有四张还随手打一张”导致不下叫
- 地狱挑战互动保真：只给碰、不点炮、不点杠且 AI 自己能下叫时，允许打给本家碰，避免全程不给碰的假体验

## 0.1 2026-05-19 实战复盘后的调权原则

本轮实战反馈暴露出两个问题：一是 AI 有强清一色胚子时仍按普通平胡出牌；二是四张同牌没有被当作 `归 / 杠材 / 高番资源`，出现拆四张后自己不下叫的低级选择。

当前补充原则：

- **路线不是死做牌**：清一色、七对、大对子、清对都只是路线权重，最终仍回到综合分排序。
- **强清色要提前识别**：单门 `10+`、异门 `<=3` 即进入清一色主线候选；清异门加分，拆目标花色强降权。
- **四张同牌不能当普通刻子**：打一张虽还剩刻子，但失去归和杠材潜力，前中期需强降权。
- **速度能压过大牌**：如果普通平胡能直接下叫、听口宽、风险低，可以压过伪清一色或未成型大牌路线。
- **防守不能耽误自己下叫**：不给本家碰、不给杠、不给炮都要和 AI 自己成叫放在同一把尺子上；只给碰且自己能下叫时，不应为了“封碰”把自己拖成未叫。

## 1. 目标

本方案用于将当前四川麻将项目中的 3 家电脑玩家，从“中级启发式 AI”升级为“骨灰级实战 AI”。

升级目标：

- 牌理更强，减少低级拆搭与无效出牌
- 会根据巡目、分差、场况切换攻守
- 会识别高危对手与危险花色
- 会在碰、杠、过之间做更像老手的选择
- 在不使用透视作弊的前提下，显著提升对战压迫感

不作为本阶段目标的内容：

- 神经网络训练型 AI
- 完整隐藏信息博弈树
- 联网对战中的服务端 AI 托管
- 全量 MCTS / CFR 级别搜索

---

## 2. 当前项目 AI 现状

> 状态更新：本节中的“当前不足”已部分过时，保留作为历史背景。

当前核心链路：

- AI 出牌入口：`autoload/GameState.gd`
- 弃牌评分器：`scripts/core/discard_advisor.gd`
- 风险评估器：`scripts/core/risk_analyzer.gd`
- 判定聚合器：`scripts/core/mahjong_judge.gd`

项目最初 AI 主要基于单步启发式评分：

- 向听数
- 进张数
- 番型保留
- 风险分

当前优点：

- 已具备稳定的可运行骨架
- 已有 trainer / hint / 风险分析基础
- 适合迭代式增强，不需要推倒重来

最初版本的主要不足：

- 攻守模式固定
- 缺少巡目意识
- 缺少分差意识
- 缺少对手建模
- 缺少轻量前瞻
- 碰杠决策偏浅

---

## 3. 总体升级路线

> 状态更新：本节 4 层升级路线目前已基本全部落地，后续重点从“有没有”转为“够不够强、够不够稳、文档是否同步”。

升级分为 4 层：

### 3.1 第一层：动态弃牌评分

目标：

- 将当前固定公式升级为“多维动态加权评分”

引入新评分维度：

- 速度分
- 听牌质量分
- 番型价值分
- 手牌结构分
- 安全分
- 场况压力分
- 分差策略分

输出目标：

- 每个候选弃牌不只给出一个总分，还给出拆解后的分项

### 3.2 第二层：攻守策略引擎

目标：

- 让 AI 先判断“这手牌应该怎么打”，再决定“打哪张”

建议策略模式：

- 全攻
- 进攻平衡
- 均衡
- 防守平衡
- 全守

判断输入：

- 当前向听
- 巡目
- 听牌质量
- 场上副露数
- 已胡人数
- 当前分差
- 是否需要追分

### 3.3 第三层：反应决策升级

目标：

- 升级碰、杠、过的决策质量

重点场景：

- 碰后是否真正提速
- 杠后收益是否大于暴露和风险
- 落后时是否允许更激进的副露
- 领先时是否应主动收缩

### 3.4 第四层：轻量前瞻搜索

目标：

- 在候选前 3~5 张之间，加入 1~2 步前瞻期望值评估

前瞻方式：

- 候选裁剪
- 抽样摸牌
- 下一轮最优动作评估
- 汇总期望收益

---

## 4. 模块设计

建议新增模块：

- `scripts/core/strategy_engine.gd`
- `scripts/core/reaction_advisor.gd`
- `scripts/core/lookahead_evaluator.gd`
- `scripts/core/opponent_model.gd`
- `scripts/core/ai_weights.gd`

各模块职责：

### 4.1 `strategy_engine.gd`

职责：

- 根据局面判断 AI 当前攻守模式
- 输出动态权重表

输入：

- 当前玩家状态
- 全场玩家状态
- 当前向听数
- 当前听牌集合

输出：

- `mode`
- `mode_label`
- `weights`
- `threat_level`
- `round_stage`
- `score_pressure`
- `reasons`

### 4.2 `reaction_advisor.gd`

职责：

- 评估碰/杠/过是否值得

输出：

- `should_peng`
- `should_gang`
- `reaction_score`
- `reaction_reasons`

### 4.3 `lookahead_evaluator.gd`

职责：

- 对 top 候选进行轻量搜索

输出：

- `expected_value`
- `future_shanten`
- `future_wait_quality`
- `future_risk`

### 4.4 `opponent_model.gd`

职责：

- 为每个对手构建危险度画像

画像字段建议：

- 是否高概率听牌
- 是否偏清一色
- 是否偏对对胡
- 哪一门最危险
- 后巡危险等级

---

## 5. 第一阶段落地范围

第一阶段只做两件事：

- 引入 `strategy_engine.gd`
- 重构 `discard_advisor.gd` 的打分结构

第一阶段不做：

- 碰杠反应重构
- 轻量前瞻
- 作弊级以外的透视增强

第一阶段目标是：

- AI 从“静态中级”提升到“强牌理 + 动态攻守”

---

## 6. 第一阶段评分结构

第一阶段建议统一采用如下结构：

`final_score = tempo + wait + fan + shape + safety + pressure`

其中：

- `tempo_score`
  - 向听越低越高
  - 进张越多越高
- `wait_quality_score`
  - 听牌张数越多越高
  - 听口越宽越高
- `fan_value_score`
  - 保留高价值路线加分
  - 丢失路线扣分
- `shape_score`
  - 保留连张、搭子、对子结构
  - 减少孤张中张堆积
- `safety_score`
  - 风险越高扣分越多
  - 防守模式下扣分更重
- `pressure_score`
  - 场上威胁大时，危险牌额外减分

---

## 7. 策略模式与权重建议

### 7.1 全攻

适用：

- 自己已听牌且听牌质量高
- 自己明显落后，需要追分
- 前中巡局势平稳

权重倾向：

- 速度高
- 听牌质量高
- 番型价值高
- 风险惩罚低

### 7.2 进攻平衡

适用：

- 1~2 向听
- 局势未明显危险

权重倾向：

- 速度中高
- 番型中
- 风险中

### 7.3 均衡

适用：

- 中巡普通场况
- 没有明显领先或落后

### 7.4 防守平衡

适用：

- 中后巡
- 场上已有威胁副露
- 自己未听牌

### 7.5 全守

适用：

- 后巡
- 自己 2 向听以上
- 对手副露明显且危险
- 自己领先较多

---

## 8. 第一阶段数据字段建议

每个候选出牌建议输出：

- `tile`
- `tile_name`
- `shanten`
- `ukeire`
- `ting_tiles`
- `tempo_score`
- `wait_quality_score`
- `fan_value_score`
- `shape_score`
- `safety_score`
- `pressure_score`
- `risk`
- `risk_label`
- `routes_after`
- `route_loss`
- `strategy_mode`
- `score`
- `reasons`

这样后续可以用于：

- AI 调试
- 训练提示面板
- 骨灰级难度回放解释

---

## 9. 第二阶段计划

第二阶段目标：

- 引入 `reaction_advisor.gd`
- 重构 AI 碰、杠、过逻辑

优先级建议：

- 先重构碰
- 再重构补杠 / 明杠
- 再引入局势化反应选择

---

## 10. 第三阶段计划

第三阶段目标：

- 引入轻量前瞻

建议实现方式：

- 只对前 3 候选做搜索
- 每个候选抽样 8~16 张可能摸牌
- 每个样本只看 1 步后续

这样能显著增强“老手感”，又不至于拖慢回合响应。

---

## 11. 难度分层建议

建议最终难度定义：

- 初级：基础向听 + 基础进张
- 中级：加入番型与基础风险
- 高级：加入动态权重
- 骨灰级：加入策略引擎 + 高阶风险 + 轻量前瞻
- 作弊级：允许透视判断

本次第一阶段落地目标：

- 将当前默认电脑对手提升到接近“骨灰级前半段”

---

## 12. 第一阶段实施清单

1. 新增 `strategy_engine.gd`
2. 在 `discard_advisor.gd` 中接入策略引擎
3. 重构弃牌候选的多维评分
4. 输出策略模式与分项得分
5. 将默认 AI 难度切换到更强档位
6. 跑启动验证

---

## 13. 验收标准

第一阶段验收以行为变化为准：

- 电脑更少无脑拆搭
- 电脑后巡更少硬冲危险中张
- 电脑落后时更积极
- 电脑领先时更保守
- 电脑副露多时，其他 AI 会主动提高警惕
- 同样牌局下，高级 AI 明显强于原中级 AI
