# 四川麻将 2.6.35 AI 聚焦验证

## 算法边界

- `bone_ash` 继续作为公平老手基线；`hell` 仅用于透视围剿挑战。
- 过牌保持 13 张等待摸牌；碰牌完成副露后枚举弃牌；杠牌先处理补张，再判断胡牌或弃牌。
- 弃牌、碰、杠和过牌在同尺度净值上比较，使用确定性公共样本；手牌结构使用互斥动态规划分解。
- 本报告只记录本次风险相关验证，不建立固定回归清单。

## C# 精确验证

- AI Core/CLI Release：0 warning，0 error。
- C# Smoke：通过。
- 动作转移张数：`pass=13`、`peng=11`、`gang=10`、`gang replacement=11`。
- 19 组 PDF 黄金听口通过。
- 131,841 个单门精确牌形穷举无失败。
- 四川番型、结算、概率、规则、推断和统一动作聚焦测试通过。

## 配对策略擂台

候选策略为 `bone_ash_action_value_v2`，基线为冻结的 `frozen_hard_tier_v1`。每个状态同时交给两套策略，并由独立裁判计算后悔值；训练种子与盲测种子分离。

### 种子 20260809，500 状态

- 弃牌平均后悔值：候选 `0.152681`，基线 `0.176857`，改善 `0.024176`。
- 改善 95% bootstrap 区间：`[0.009208, 0.038206]`。
- 严重错误率：候选 `1.40%`，基线 `1.40%`。
- 非必要拆对率：候选 `0%`，基线 `1.00%`。
- 碰/过/杠平均后悔值：候选 `0.246922`，基线 `1.766754`。
- 同状态重复一致率：`100%`；晋级门槛通过。

### 盲测种子 20260810，500 状态

- 弃牌平均后悔值：候选 `0.170147`，基线 `0.199027`，改善 `0.028880`。
- 改善 95% bootstrap 区间：`[0.013939, 0.043943]`。
- 严重错误率：候选 `0.40%`，基线 `0.40%`。
- 非必要拆对率：候选 `0%`，基线 `0.60%`。
- 碰/过/杠平均后悔值：候选 `0.196338`，基线 `1.587463`。
- 同状态重复一致率：`100%`；晋级门槛通过。

## Godot 实际链路

- `bone_ash` 真实 C# runtime 压力运行 5 局，全部自然结束：3 局流局、2 局血战结束、强制终止 0。
- 平均每局 63.6 步；共 47 次反应机会，包括胡 7、杠 2、碰 2、过 36；没有反应阻塞。
- 压缩影子日志一局记录 94 个事件，总计 268KB，单事件最大约 3.6KB；隐藏状态非空事件 0，手牌始终为 27 类计数。
- 首次验证发现旧递归日志会写入约 1.7GB，相关临时目录已删除；schema 2 已限制为每会话 1600 事件、最多 4 个会话。

## 证据边界

上述数据证明动作时序、确定性、离线裁判、配对基线和 Godot 实际调用链没有发现回归，并显示当前候选在两组随机状态上优于冻结基线。它不能替代用户在 iPhone 上连续完成多局后的长期平均净分和老手主观复核；后续改进应基于新影子日志的真实争议动作，而不是按少量输赢在线调权。
